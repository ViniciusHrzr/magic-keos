---
phase: 02-qualidade-da-ficha
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - constants/spell-constants.ts   # created
  - app/(tabs)/magia.tsx
  - app/(tabs)/grimorio.tsx
autonomous: true
requirements:
  - CODE-01
tags: [refactor, constants, deduplication, magia, grimorio]

must_haves:
  truths:
    - "A single source-of-truth file exports `COLOR_HEX` and `GRAU_COLORS` for use by spell-related screens"
    - "`app/(tabs)/magia.tsx` imports `COLOR_HEX` and `GRAU_COLORS` from `@/constants/spell-constants` and does not define them locally"
    - "`app/(tabs)/grimorio.tsx` imports `COLOR_HEX` and `GRAU_COLORS` from `@/constants/spell-constants` and does not define them locally"
    - "`getDomainColor` helper function in magia.tsx continues to exist and continues to reference `COLOR_HEX` (now via import)"
    - "The values of `COLOR_HEX` and `GRAU_COLORS` are byte-for-byte identical to the originals in both consumer files — no semantic change to rendering"
  artifacts:
    - path: "constants/spell-constants.ts"
      provides: "Named exports COLOR_HEX (Record<SpellColor, string>) and GRAU_COLORS (string[])"
      contains: "export const COLOR_HEX"
      also_contains: "export const GRAU_COLORS"
      min_lines: 10
    - path: "app/(tabs)/magia.tsx"
      provides: "MagiaScreen importing shared spell constants"
      contains: "from '@/constants/spell-constants'"
      not_contains_local_definition: "const COLOR_HEX: Record<SpellColor, string> = {"
    - path: "app/(tabs)/grimorio.tsx"
      provides: "GrimorioScreen importing shared spell constants"
      contains: "from '@/constants/spell-constants'"
      not_contains_local_definition: "const COLOR_HEX: Record<SpellColor, string> = {"
  key_links:
    - from: "app/(tabs)/magia.tsx"
      to: "constants/spell-constants.ts"
      via: "named import of COLOR_HEX and GRAU_COLORS"
      pattern: "import \\{[^}]*COLOR_HEX[^}]*GRAU_COLORS[^}]*\\} from '@/constants/spell-constants'"
    - from: "app/(tabs)/grimorio.tsx"
      to: "constants/spell-constants.ts"
      via: "named import of COLOR_HEX and GRAU_COLORS"
      pattern: "import \\{[^}]*COLOR_HEX[^}]*GRAU_COLORS[^}]*\\} from '@/constants/spell-constants'"
    - from: "constants/spell-constants.ts"
      to: "data/grimoire.ts (SpellColor type) + constants/theme.ts (RPG palette)"
      via: "type-only import of SpellColor + value import of RPG"
      pattern: "import \\{ RPG \\} from '@/constants/theme';"
---

<objective>
Deduplicar `COLOR_HEX` e `GRAU_COLORS` extraindo-os para um único arquivo de constantes (`constants/spell-constants.ts`). Hoje a mesma definição existe em duas telas:
- `app/(tabs)/magia.tsx` linhas 334–340 (COLOR_HEX) e 347 (GRAU_COLORS)
- `app/(tabs)/grimorio.tsx` linhas 21–27 (COLOR_HEX) e 36 (GRAU_COLORS)

Após este plano, ambas as telas importam de `@/constants/spell-constants`. Nenhuma lógica de renderização muda — apenas a localização das definições. Fecha o requisito CODE-01.

Purpose: Eliminar a duplicação literal de duas constantes idênticas em dois arquivos. Reduz risco de drift (alguém atualizar uma e esquecer a outra) e prepara o terreno para Phase 3 (compartilhamento de `SpellDetailCard` entre as duas telas, GRIM-01).

Output: arquivo novo `constants/spell-constants.ts` exportando as duas constantes; `magia.tsx` e `grimorio.tsx` atualizados para importar do novo arquivo e remover as definições locais.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-qualidade-da-ficha/02-CONTEXT.md
@.planning/phases/02-qualidade-da-ficha/02-PATTERNS.md
@constants/theme.ts
@app/(tabs)/magia.tsx
@app/(tabs)/grimorio.tsx
@data/grimoire.ts

<interfaces>
**Source of truth for the SpellColor type** (data/grimoire.ts line 3):
  export type SpellColor = 'branco' | 'verde' | 'vermelho' | 'preto' | 'azul';

**RPG palette tokens used** (constants/theme.ts lines 21–31):
  RPG.branco          // '#e8e0cc'
  RPG.verdeLight      // '#4aaa4a'
  RPG.vermelhoLight   // '#e03030'
  RPG.pretoLight      // '#6a5882'
  RPG.azulLight       // '#2878e0'
  RPG.gold            // '#c9a84c'
  RPG.goldLight       // '#e8c96a'

**Current local definition in magia.tsx (lines 334–340) — to be DELETED:**
  const COLOR_HEX: Record<SpellColor, string> = {
    branco: RPG.branco,
    verde: RPG.verdeLight,
    vermelho: RPG.vermelhoLight,
    preto: RPG.pretoLight,
    azul: RPG.azulLight,
  };

**Current local definition in magia.tsx (line 347) — to be DELETED:**
  const GRAU_COLORS = ['#888', RPG.gold, RPG.goldLight, '#fff'];

**Function between the two definitions in magia.tsx (lines 342–346) — to be KEPT:**
  function getDomainColor(name: string): string | null {
    const spell = grimoire.find(s => s.dominio === name.trim());
    if (!spell) return null;
    return COLOR_HEX[spell.cor];
  }

**Current local definition in grimorio.tsx (lines 21–27) — to be DELETED:**
  const COLOR_HEX: Record<SpellColor, string> = {
    branco: RPG.branco,
    verde: RPG.verdeLight,
    vermelho: RPG.vermelhoLight,
    preto: RPG.pretoLight,
    azul: RPG.azulLight,
  };

**Current local definition in grimorio.tsx (line 36) — to be DELETED:**
  const GRAU_COLORS = ['#888', RPG.gold, RPG.goldLight, '#fff'];

**Definitions between the two in grimorio.tsx (lines 29–34) — to be KEPT (TYPE_LABELS map):**
  const TYPE_LABELS: Record<SpellType, string> = {
    '[T]': 'Truque',
    '[E]': 'Encantamento',
    '[F]': 'Feitiço',
    '[C]': 'Criatura',
  };

**Convention reference — constants/theme.ts shows the named-export pattern with `import { Platform } from 'react-native'` at top followed by `export const RPG = {...}` and `export const Colors = {...}`. Apply the same shape.**
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create constants/spell-constants.ts with exported COLOR_HEX and GRAU_COLORS</name>
  <files>constants/spell-constants.ts</files>
  <read_first>
    - constants/theme.ts (the structural analog — confirm the named-export, plain-object-literal convention)
    - data/grimoire.ts (confirm the exact `SpellColor` type definition for the typed Record)
    - app/(tabs)/grimorio.tsx lines 21–27 and line 36 (the canonical source for the values to copy)
    - app/(tabs)/magia.tsx lines 334–340 and line 347 (cross-check that the two sets of values are byte-for-byte identical)
    - .planning/phases/02-qualidade-da-ficha/02-PATTERNS.md (section "constants/spell-constants.ts — New file (constants extraction)")
  </read_first>
  <action>
Per D-05, D-06, D-07 in 02-CONTEXT.md. Create a new file at exactly `constants/spell-constants.ts` (kebab-case filename, English — consistent with `constants/theme.ts`). The path MUST be `constants/spell-constants.ts` at the repo root, NOT inside `app/` or anywhere else.

File contents — write exactly:
  1. An import for `RPG` from the theme: `import { RPG } from '@/constants/theme';`
  2. An import for the `SpellColor` type: `import { SpellColor } from '@/data/grimoire';` (do NOT use `import type` — the codebase convention in CONVENTIONS.md uses plain `import` for types alongside values; see grimorio.tsx line 6 which mixes `Spell, SpellColor, SpellType` with the `grimoire` value)
  3. A blank line
  4. `export const COLOR_HEX: Record<SpellColor, string>` with the same five keys and values copied byte-for-byte from `grimorio.tsx` lines 21–27 (branco → RPG.branco, verde → RPG.verdeLight, vermelho → RPG.vermelhoLight, preto → RPG.pretoLight, azul → RPG.azulLight)
  5. A blank line
  6. `export const GRAU_COLORS = ['#888', RPG.gold, RPG.goldLight, '#fff'];` — copied byte-for-byte from `grimorio.tsx` line 36 (and identical to `magia.tsx` line 347)

Do NOT add:
  - `as const` (D-07 leaves typing at discretion; the existing local defs do not use it; mirror the existing shape)
  - Comments (the original local defs had no comments)
  - A default export (named exports only, per the `constants/theme.ts` convention)
  - Any other constant — keep this file scoped to just these two until a future need arises

Use the `@/` path alias for all imports (project-wide convention in CONVENTIONS.md "Import Organization").
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The file `constants/spell-constants.ts` exists
    - The file contains the substring `export const COLOR_HEX: Record<SpellColor, string>`
    - The file contains the substring `export const GRAU_COLORS = ['#888', RPG.gold, RPG.goldLight, '#fff'];`
    - The file contains the import `import { RPG } from '@/constants/theme';`
    - The file contains an import that pulls `SpellColor` from `@/data/grimoire` (e.g. `import { SpellColor } from '@/data/grimoire';`)
    - The file does NOT contain a `default export`
    - The file does NOT contain any constant other than `COLOR_HEX` and `GRAU_COLORS`
    - `npx tsc --noEmit` exits 0
    - The values inside `COLOR_HEX` are exactly: `{ branco: RPG.branco, verde: RPG.verdeLight, vermelho: RPG.vermelhoLight, preto: RPG.pretoLight, azul: RPG.azulLight }` (order and identifiers can be verified by grep)
  </acceptance_criteria>
  <done>
    A standalone, importable constants module exists. No consumers yet — Task 2 wires them.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update magia.tsx and grimorio.tsx to import shared constants and remove local definitions</name>
  <files>
    app/(tabs)/magia.tsx
    app/(tabs)/grimorio.tsx
  </files>
  <read_first>
    - constants/spell-constants.ts (just created in Task 1 — confirm the exact export names)
    - app/(tabs)/magia.tsx (read top imports lines 1–12 and the constants block lines 334–347; the `getDomainColor` function on lines 342–346 must be preserved exactly)
    - app/(tabs)/grimorio.tsx (read top imports lines 1–10 and the constants block lines 21–36; the `TYPE_LABELS` map on lines 29–34 must be preserved exactly)
    - .planning/phases/02-qualidade-da-ficha/02-PATTERNS.md (sections "app/(tabs)/magia.tsx" and "app/(tabs)/grimorio.tsx")
  </read_first>
  <action>
Per D-06 in 02-CONTEXT.md. Update both consumer files to import the shared constants and remove their local definitions. Use the same change pattern in both files to keep the diff symmetric.

**Sub-step A — `app/(tabs)/magia.tsx`:**

1. Add a new named import in the top-of-file import block. Place it after the existing `ErrorBoundary` import (line 12) so it lands in the third group (internal `@/` imports), per the import-organization convention. The exact line to add:
   `import { COLOR_HEX, GRAU_COLORS } from '@/constants/spell-constants';`

2. Delete the local `COLOR_HEX` definition (currently lines 334–340) — the seven-line block starting with `const COLOR_HEX: Record<SpellColor, string> = {` and ending with the closing `};`.

3. Delete the local `GRAU_COLORS` definition (currently line 347) — the single line `const GRAU_COLORS = ['#888', RPG.gold, RPG.goldLight, '#fff'];`.

4. **MUST KEEP** the `getDomainColor` function that sits between the two deletions (currently lines 342–346). This function references `COLOR_HEX[spell.cor]` — after Task 2A, that reference resolves to the IMPORTED `COLOR_HEX`, not a local. The function body does NOT change.

5. Do not touch the rest of magia.tsx — `MagiaScreen` function, `SpellDetailView`, `DomainView`, the modal logic, the styles block, or any other constant (`manaTypes`, `vidaIcon`, etc. — if those exist in magia.tsx, they stay).

**Sub-step B — `app/(tabs)/grimorio.tsx`:**

1. Add the same import line, placed after the existing `ErrorBoundary` import (line 10), in the third import group:
   `import { COLOR_HEX, GRAU_COLORS } from '@/constants/spell-constants';`

2. Delete the local `COLOR_HEX` definition (currently lines 21–27) — the seven-line block.

3. Delete the local `GRAU_COLORS` definition (currently line 36) — the single line.

4. **MUST KEEP** the constants that sit between the two deletions (currently lines 29–34, the `TYPE_LABELS` map). Also keep `COLOR_LABELS` (line 12), `TYPE_LABELS_SHORT` (line 15), `COLORS` (line 18), `TYPES` (line 19) and any other top-of-file declarations unaffected.

5. Do not touch the rest of grimorio.tsx — `GrimorioScreen` function, `SpellDetail`, `StatPill`, the FlatList rendering, the modal, or the styles block.

**Cross-file invariant:** After both sub-steps, every use of `COLOR_HEX` and `GRAU_COLORS` inside the two screen files MUST resolve to the imported binding. No file may have a local re-declaration. The TypeScript compiler will catch any missed reference at `npx tsc --noEmit`.

**SpellColor import in magia.tsx:** the existing import on line 10 is `import { grimoire, Spell, SpellColor } from '@/data/grimoire';`. Keep it — `SpellColor` is still used inline in other parts of the file (notably the `getDomainColor` signature does not need it, but other function signatures in magia.tsx may; do not remove the type).

**SpellColor import in grimorio.tsx:** the existing import on line 6 is `import { grimoire, domains, Spell, SpellColor, SpellType } from '@/data/grimoire';`. Keep it — `SpellColor` is still used by other typed declarations in the file (`COLORS: SpellColor[]` on line 18, plus props/state typing inside the screen).

Do not introduce relative imports. Use `@/` exclusively.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `app/(tabs)/magia.tsx` contains the exact substring `import { COLOR_HEX, GRAU_COLORS } from '@/constants/spell-constants';`
    - `app/(tabs)/magia.tsx` does NOT contain the substring `const COLOR_HEX: Record<SpellColor, string> = {` anywhere
    - `app/(tabs)/magia.tsx` does NOT contain the substring `const GRAU_COLORS = ['#888'` anywhere
    - `app/(tabs)/magia.tsx` STILL contains the function declaration `function getDomainColor(name: string): string | null {` (exact substring)
    - `app/(tabs)/magia.tsx` STILL contains the body line `return COLOR_HEX[spell.cor];` inside `getDomainColor` (exact substring)
    - `app/(tabs)/grimorio.tsx` contains the exact substring `import { COLOR_HEX, GRAU_COLORS } from '@/constants/spell-constants';`
    - `app/(tabs)/grimorio.tsx` does NOT contain the substring `const COLOR_HEX: Record<SpellColor, string> = {` anywhere
    - `app/(tabs)/grimorio.tsx` does NOT contain the substring `const GRAU_COLORS = ['#888'` anywhere
    - `app/(tabs)/grimorio.tsx` STILL contains the substring `const TYPE_LABELS: Record<SpellType, string> = {` (the map between the two deletions is preserved)
    - `app/(tabs)/grimorio.tsx` STILL contains `const COLORS: SpellColor[] = ['branco', 'verde', 'vermelho', 'preto', 'azul'];` (line 18 preserved)
    - `npx tsc --noEmit` exits 0 (catches any orphan reference to `COLOR_HEX` or `GRAU_COLORS` that did not get its import)
    - grep `-c "COLOR_HEX" "app/(tabs)/magia.tsx"` returns at least 1 (the call sites are preserved — there is at least the `getDomainColor` reference; existing other usages remain)
    - grep `-c "GRAU_COLORS" "app/(tabs)/magia.tsx"` returns at least 1 (existing call sites preserved; they now resolve to the import)
    - grep `-c "COLOR_HEX" "app/(tabs)/grimorio.tsx"` returns at least 1 (existing call sites preserved)
    - grep `-c "GRAU_COLORS" "app/(tabs)/grimorio.tsx"` returns at least 1 (existing call sites preserved)
  </acceptance_criteria>
  <done>
    Both consumer files now resolve `COLOR_HEX` and `GRAU_COLORS` via the shared module. No local re-declarations. `getDomainColor` and `TYPE_LABELS` and all other inter-deletion content preserved. TypeScript compiles. Visual rendering unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static asset import (constants module) → render tree | A new internal module is imported by two screens; the module exports plain literal data with no runtime input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering | Drift between two duplicated constant blocks | mitigate | Consolidating into a single source-of-truth file (`constants/spell-constants.ts`) eliminates the class of bug where one consumer is updated and the other is forgotten. Future edits go to one place. |
| T-02-09 | Tampering | Stale references after refactor | mitigate | `npx tsc --noEmit` is run as the automated gate for both tasks. Any orphan reference to `COLOR_HEX` or `GRAU_COLORS` whose import was missed produces a compile error and blocks the plan. |
| T-02-10 | Information Disclosure | Hex colors as game data | accept | Spell-color hex values are decorative game-theme data, not secrets. Moving them to a shared constants module does not change their exposure surface (already bundled in the app binary). |
| T-02-SC | Tampering | npm/pip/cargo installs | n/a | No new packages installed. New file is internal-only and uses existing dependencies (`@/constants/theme`, `@/data/grimoire`). No supply-chain surface. |
</threat_model>

<verification>
Run from the repo root:

1. `npx tsc --noEmit` → exit 0
2. File existence: `constants/spell-constants.ts` exists; `app/(tabs)/magia.tsx` and `app/(tabs)/grimorio.tsx` still exist.
3. grep on `constants/spell-constants.ts`:
   - `export const COLOR_HEX` appears exactly once
   - `export const GRAU_COLORS` appears exactly once
4. grep on the two consumer files:
   - `import { COLOR_HEX, GRAU_COLORS } from '@/constants/spell-constants';` appears exactly once in each
   - `const COLOR_HEX: Record<SpellColor, string> = {` appears zero times in each
   - `const GRAU_COLORS = ['#888'` appears zero times in each
5. Preservation grep:
   - `function getDomainColor` still in `magia.tsx`
   - `const TYPE_LABELS: Record<SpellType, string> = {` still in `grimorio.tsx`
6. Visual sanity (post-merge, optional): open the Grimório and Magia tabs — the spell-color border tints and the grau pip colors should look identical to before the refactor (no shifted hexes).
</verification>

<success_criteria>
- CODE-01 satisfied: `COLOR_HEX` and `GRAU_COLORS` exist in exactly one file (`constants/spell-constants.ts`).
- Both `magia.tsx` and `grimorio.tsx` import from `@/constants/spell-constants`.
- All preserved code (`getDomainColor` in magia, `TYPE_LABELS`/`COLORS`/`TYPES` in grimorio) is intact.
- TypeScript clean.
- No visual regression — the hex values are byte-for-byte identical to the originals.
</success_criteria>

<output>
Create `.planning/phases/02-qualidade-da-ficha/02-03-SUMMARY.md` when done (follow `$HOME/.claude/get-shit-done/templates/summary.md`).

Required content:
  - Frontmatter with phase, plan, subsystem (e.g., `constants, magia, grimorio`), tags, requires/provides/affects (provides: shared-spell-constants; affects: app/(tabs)/magia.tsx, app/(tabs)/grimorio.tsx; created: constants/spell-constants.ts)
  - Tasks completed (one entry each for Task 1 and Task 2)
  - Verification Results table (TypeScript exit, the six grep checks above, plus the preservation greps)
  - Deviations (state "None" if executed as written)
  - Files Changed table: 1 created, 2 modified
  - Commit SHA
  - Self-Check: PASSED block
</output>
