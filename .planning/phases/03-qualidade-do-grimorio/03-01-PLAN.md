---
phase: 03-qualidade-do-grimorio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/rpg/StatPill.tsx
  - app/(tabs)/grimorio.tsx
autonomous: true
requirements:
  - GRIM-01

must_haves:
  truths:
    - "components/rpg/StatPill.tsx exists as a standalone exportable component"
    - "grimorio.tsx imports StatPill from components/rpg/StatPill instead of defining it inline"
    - "The app compiles with no TypeScript errors after the extraction"
  artifacts:
    - path: "components/rpg/StatPill.tsx"
      provides: "Reusable stat badge with label/value/isSymbol props"
      exports: ["StatPill (default export)"]
    - path: "app/(tabs)/grimorio.tsx"
      provides: "Grimório screen (SpellDetail still inline; StatPill now imported)"
      contains: "import StatPill from '@/components/rpg/StatPill'"
  key_links:
    - from: "app/(tabs)/grimorio.tsx"
      to: "components/rpg/StatPill.tsx"
      via: "import default"
      pattern: "import StatPill from"
---

<objective>
Extract the inline `function StatPill` from `grimorio.tsx` into a standalone
`components/rpg/StatPill.tsx` component. Remove the inline definition after
adding the import.

Purpose: StatPill is a small reusable badge used by SpellDetail (to be extracted
in Plan 02). Extracting it first lets Plan 02 import it cleanly without circular
modification of grimorio.tsx.

Output:
- `components/rpg/StatPill.tsx` — new file, prop-driven, default export
- `app/(tabs)/grimorio.tsx` — `function StatPill` removed, import added
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-qualidade-do-grimorio/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create components/rpg/StatPill.tsx</name>
  <files>components/rpg/StatPill.tsx</files>
  <read_first>
    - components/rpg/NumericStepper.tsx — reference pattern: interface Props at top, default export function, StyleSheet.create at bottom, RPG.* tokens only
    - app/(tabs)/grimorio.tsx lines 267–274 — exact source of the StatPill implementation to port (label, value, isSymbol? props; pill/pillLabel/pillValue styles already in grimorio styles)
    - constants/spell-constants.ts — confirms COLOR_HEX/GRAU_COLORS import path (@/constants/spell-constants) for awareness; StatPill itself does not import these
  </read_first>
  <action>
    Create `components/rpg/StatPill.tsx` following the NumericStepper.tsx component pattern exactly:

    1. Imports: `React` from 'react'; `View`, `Text`, `StyleSheet` from 'react-native'; `RPG` from '@/constants/theme'.
    2. Define `interface Props` with fields: `label: string`, `value: string`, `isSymbol?: boolean`.
    3. Export default function `StatPill({ label, value, isSymbol }: Props)`.
    4. Return JSX: a `<View style={styles.pill}>` containing a `<Text style={styles.pillLabel}>{label}</Text>` and a `<Text style={[styles.pillValue, isSymbol && { fontFamily: 'PlanewalkerDings', fontStyle: 'normal', fontSize: 16 }]}>{value}</Text>`.
    5. Define `const styles = StyleSheet.create({...})` at the bottom with:
       - `pill`: `{ alignItems: 'center', paddingHorizontal: 10, paddingVertical: 4, borderWidth: 1, borderColor: RPG.border, backgroundColor: RPG.surfaceAlt, borderRadius: 4 }`
       - `pillLabel`: `{ color: RPG.textMuted, fontSize: 9, textTransform: 'uppercase', letterSpacing: 0.5 }`
       - `pillValue`: `{ color: RPG.text, fontSize: 13, fontWeight: '600' }`

    These style values are copied verbatim from grimorio.tsx styles.pill, styles.pillLabel, styles.pillValue (lines 476–487) — do not change the values.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    - `components/rpg/StatPill.tsx` exists
    - File contains `export default function StatPill(`
    - File contains `interface Props`
    - File contains `isSymbol?: boolean`
    - `npx tsc --noEmit` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove inline StatPill from grimorio.tsx and import the extracted component</name>
  <files>app/(tabs)/grimorio.tsx</files>
  <read_first>
    - app/(tabs)/grimorio.tsx — full file; identify lines 267–274 (the inline function StatPill definition) to delete, and line 11 area (import block) where the new import is added
    - components/rpg/StatPill.tsx — just created; confirm the default export name is `StatPill` before writing the import
  </read_first>
  <action>
    Make two edits to `app/(tabs)/grimorio.tsx`:

    1. ADD import: insert `import StatPill from '@/components/rpg/StatPill';` after the existing `import { COLOR_HEX, GRAU_COLORS } from '@/constants/spell-constants';` line (currently line 11). Place it as the next line in the import block.

    2. DELETE the inline function: remove the entire `function StatPill(...)` block (currently lines 267–274):
       ```
       function StatPill({ label, value, isSymbol }: { label: string; value: string; isSymbol?: boolean }) {
         return (
           <View style={styles.pill}>
             <Text style={styles.pillLabel}>{label}</Text>
             <Text style={[styles.pillValue, isSymbol && { fontFamily: 'PlanewalkerDings', fontStyle: 'normal', fontSize: 16 }]}>{value}</Text>
           </View>
         );
       }
       ```

    Do NOT touch any other code. `SpellDetail` remains inline — that is extracted in Plan 02.
    The `styles.pill`, `styles.pillLabel`, `styles.pillValue` entries in the StyleSheet remain in grimorio.tsx for now — they will be used until Plan 02 either removes SpellDetail or moves its styles (at Plan 02's discretion, since grimorio.tsx won't use them after SpellDetailCard extraction).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    - `app/(tabs)/grimorio.tsx` contains `import StatPill from '@/components/rpg/StatPill'`
    - `app/(tabs)/grimorio.tsx` does NOT contain `function StatPill(`
    - `npx tsc --noEmit` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component props | StatPill receives label/value/isSymbol from parent; all are compile-time typed strings/booleans — no runtime untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | StatPill.tsx styles | accept | Pure presentational component; no state, no network, no storage. Style values copied verbatim from existing grimorio.tsx. Risk: negligible. |
| T-03-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. Existing lockfile unchanged. |
</threat_model>

<verification>
1. `components/rpg/StatPill.tsx` exists at the correct path
2. `grep "export default function StatPill" components/rpg/StatPill.tsx` returns a match
3. `grep "interface Props" components/rpg/StatPill.tsx` returns a match
4. `grep "function StatPill" app/(tabs)/grimorio.tsx` returns no match (inline removed)
5. `grep "import StatPill" app/(tabs)/grimorio.tsx` returns a match
6. `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- StatPill is a standalone component in components/rpg/ with interface Props, default export, StyleSheet.create, and RPG.* tokens
- grimorio.tsx no longer contains an inline function StatPill definition
- grimorio.tsx imports StatPill from @/components/rpg/StatPill
- TypeScript compilation succeeds with no errors
</success_criteria>

<output>
Create `.planning/phases/03-qualidade-do-grimorio/03-01-SUMMARY.md` when done.
</output>
