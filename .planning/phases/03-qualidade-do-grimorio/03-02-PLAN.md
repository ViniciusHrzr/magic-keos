---
phase: 03-qualidade-do-grimorio
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - components/rpg/SpellDetailCard.tsx
  - app/(tabs)/grimorio.tsx
  - app/(tabs)/magia.tsx
autonomous: true
requirements:
  - GRIM-01

must_haves:
  truths:
    - "SpellDetailCard is a shared component used by both grimorio.tsx and magia.tsx"
    - "SpellDetail inline function is removed from grimorio.tsx"
    - "SpellDetailView inline function is removed from magia.tsx"
    - "magia.tsx spell detail modal shows no '+ Mágica' button (onAddMagica not passed)"
    - "grimorio.tsx spell detail modal still has the '+ Mágica' button (onAddMagica passed)"
    - "The app compiles with no TypeScript errors"
  artifacts:
    - path: "components/rpg/SpellDetailCard.tsx"
      provides: "Shared spell detail view with optional onAddMagica"
      exports: ["SpellDetailCard (default export)"]
    - path: "app/(tabs)/grimorio.tsx"
      provides: "Grimório screen — SpellDetail removed, SpellDetailCard imported"
      contains: "import SpellDetailCard from '@/components/rpg/SpellDetailCard'"
    - path: "app/(tabs)/magia.tsx"
      provides: "Magia screen — SpellDetailView removed, SpellDetailCard imported"
      contains: "import SpellDetailCard from '@/components/rpg/SpellDetailCard'"
  key_links:
    - from: "app/(tabs)/grimorio.tsx"
      to: "components/rpg/SpellDetailCard.tsx"
      via: "import default, passed with onAddMagica"
      pattern: "onAddMagica=\\{setMagica\\}"
    - from: "app/(tabs)/magia.tsx"
      to: "components/rpg/SpellDetailCard.tsx"
      via: "import default, no onAddMagica prop"
      pattern: "SpellDetailCard spell=\\{viewSpell\\}"
---

<objective>
Extract the inline `function SpellDetail` from `grimorio.tsx` into a shared
`components/rpg/SpellDetailCard.tsx` component. Replace the duplicate
`function SpellDetailView` in `magia.tsx` with an import of the same component.

Purpose: Eliminates duplicate spell detail implementation across two screens.
SpellDetailCard accepts an optional `onAddMagica` prop — when omitted, the
"+ Mágica" button is not rendered (magia.tsx use case). When provided, the
button appears and functions (grimorio.tsx use case).

Output:
- `components/rpg/SpellDetailCard.tsx` — new shared component
- `app/(tabs)/grimorio.tsx` — inline SpellDetail removed, SpellDetailCard imported
- `app/(tabs)/magia.tsx` — inline SpellDetailView removed, SpellDetailCard imported
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-qualidade-do-grimorio/03-CONTEXT.md
@.planning/phases/03-qualidade-do-grimorio/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create components/rpg/SpellDetailCard.tsx</name>
  <files>components/rpg/SpellDetailCard.tsx</files>
  <read_first>
    - app/(tabs)/grimorio.tsx lines 216–265 — the `function SpellDetail` block: full implementation to port. Note props (spell, onClose, magicas, onAddMagica), useState for feedback, addMagica handler, and JSX structure.
    - app/(tabs)/magia.tsx lines 340–372 — the `function SpellDetailView` block: shows what the view-only version looks like (no feedback state, no "+ Mágica"). This is the MINUS case (no onAddMagica).
    - components/rpg/NumericStepper.tsx — component structure pattern: interface Props, default export, StyleSheet.create, RPG.* tokens.
    - components/rpg/StatPill.tsx — confirms import path (@/components/rpg/StatPill) and that it is a default export.
    - constants/spell-constants.ts — confirms import path (@/constants/spell-constants) for COLOR_HEX.
    - data/grimoire.ts (just the top — enough to confirm Spell, SpellColor, SpellType type names and import path @/data/grimoire).
    - data/spellImages.ts (just enough to see the export shape — `spellImages[spell.nome]` lookup pattern).
  </read_first>
  <action>
    Create `components/rpg/SpellDetailCard.tsx` with the following structure:

    IMPORTS (in this order):
    - `React`, `useState` from 'react'
    - `ScrollView`, `View`, `Text`, `TouchableOpacity`, `Image`, `StyleSheet` from 'react-native'
    - `RPG` from '@/constants/theme'
    - `Spell`, `SpellType` from '@/data/grimoire'
    - `spellImages` from '@/data/spellImages'
    - `COLOR_HEX` from '@/constants/spell-constants'
    - `StatPill` from '@/components/rpg/StatPill'

    PROPS INTERFACE (per D-01 and D-05):
    ```
    interface Props {
      spell: Spell;
      onClose: () => void;
      magicas?: string[];
      onAddMagica?: (idx: number, name: string) => void;
    }
    ```

    TYPE_LABELS constant (local to this file — per Claude's Discretion in CONTEXT.md):
    ```
    const TYPE_LABELS: Record<SpellType, string> = {
      '[T]': 'Truque',
      '[E]': 'Encantamento',
      '[F]': 'Feitiço',
      '[C]': 'Criatura',
    };
    ```

    COMPONENT FUNCTION `export default function SpellDetailCard({ spell, onClose, magicas, onAddMagica }: Props)`:
    - `const [feedback, setFeedback] = useState('')` — local state for add-feedback message (per D-02)
    - `const color = COLOR_HEX[spell.cor]`
    - `const img = spellImages[spell.nome]`
    - `addMagica` handler: only defined/used when `onAddMagica` is present. Logic: find first empty slot in `magicas`, if none call `setFeedback('Todos os slots de mágicas estão cheios!')`, else call `onAddMagica(idx, spell.nome)` and `setFeedback('"${spell.nome}" adicionada às mágicas!')`.
    - JSX: `<ScrollView contentContainerStyle={styles.detailContent}>` wrapping:
      1. `<View style={[styles.detailHeader, { borderBottomColor: color }]}>` — contains optional image, name+meta block, close button (same as SpellDetail in grimorio.tsx).
      2. `<View style={styles.detailStats}>` — three `<StatPill>` calls: `label="Grau" value={String(spell.grau)}`, `label="Tipo" value={TYPE_LABELS[spell.tipo]}`, `label="Custo" value={spell.custo || '—'} isSymbol`.
      3. `<Text style={styles.detailEffect}>{spell.efeito}</Text>`
      4. Conditionally, if `onAddMagica` is provided: `<View style={styles.importRow}><TouchableOpacity ... onPress={addMagica}>+ Mágica</TouchableOpacity></View>` — same styles as grimorio.tsx importRow/importBtn/importBtnAlt/importBtnText.
      5. Conditionally, if `feedback`: `<Text style={styles.feedback}>{feedback}</Text>`

    STYLES via `StyleSheet.create`:
    Copy only the style keys needed by this component from grimorio.tsx's StyleSheet:
    `detailContent`, `detailHeader`, `spellImg`, `detailName`, `detailMeta`, `closeBtn`, `closeBtnText`, `detailStats`, `detailEffect`, `importRow`, `importBtn`, `importBtnAlt`, `importBtnText`, `feedback`.
    Values must be byte-for-byte identical to grimorio.tsx styles. `detailName` in grimorio.tsx uses `fontSize: 20`; use that value (magia.tsx uses 18 — the grimorio.tsx value is authoritative since that is the extraction source per D-01).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    - `components/rpg/SpellDetailCard.tsx` exists
    - File contains `export default function SpellDetailCard(`
    - File contains `interface Props`
    - File contains `onAddMagica?: (idx: number, name: string) => void`
    - File contains `import StatPill from '@/components/rpg/StatPill'`
    - `npx tsc --noEmit` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace SpellDetail in grimorio.tsx with SpellDetailCard import</name>
  <files>app/(tabs)/grimorio.tsx</files>
  <read_first>
    - app/(tabs)/grimorio.tsx — full current state (after Plan 01 edits). Find the `function SpellDetail` block (lines ~216–265 after Plan 01 offset changes), the import block at top, and the Modal JSX that mounts SpellDetail.
    - components/rpg/SpellDetailCard.tsx — just created; confirm prop names spell, onClose, magicas, onAddMagica.
  </read_first>
  <action>
    Make three edits to `app/(tabs)/grimorio.tsx`:

    1. ADD import: insert `import SpellDetailCard from '@/components/rpg/SpellDetailCard';` after the existing `import StatPill from '@/components/rpg/StatPill';` line (added in Plan 01).

    2. DELETE the inline `function SpellDetail(...)` block entirely — the complete function from its first line (`function SpellDetail({`) through its closing `}` (roughly 50 lines).

    3. UPDATE the Modal JSX usage: replace `<SpellDetail` with `<SpellDetailCard` at the call site inside the Modal. The props stay identical:
       `spell={selected}`, `onClose={() => setSelected(null)}`, `magicas={c.magicas}`, `onAddMagica={setMagica}`.

    After these edits the style keys that were only used by the now-deleted SpellDetail function (detailContent, detailHeader, spellImg, detailName, detailMeta, closeBtn, closeBtnText, detailStats, detailEffect, importRow, importBtn, importBtnAlt, importBtnText, feedback) become unused in grimorio.tsx. Remove them from the grimorio.tsx StyleSheet to avoid dead code. Also remove pill, pillLabel, pillValue from grimorio.tsx StyleSheet (they were used by the now-extracted StatPill and the deleted SpellDetail — no longer needed here).

    Do NOT touch filter state, FlatList, or any other part of the screen.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    - `app/(tabs)/grimorio.tsx` contains `import SpellDetailCard from '@/components/rpg/SpellDetailCard'`
    - `app/(tabs)/grimorio.tsx` does NOT contain `function SpellDetail(`
    - `app/(tabs)/grimorio.tsx` contains `<SpellDetailCard` in the Modal JSX
    - `npx tsc --noEmit` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 3: Replace SpellDetailView in magia.tsx with SpellDetailCard import</name>
  <files>app/(tabs)/magia.tsx</files>
  <read_first>
    - app/(tabs)/magia.tsx — full current state. Find lines 340–372 (function SpellDetailView), the import block at top, the Modal JSX that calls SpellDetailView (around line 308), and the DomainView function (lines 374–442) which also calls SpellDetailView internally.
    - components/rpg/SpellDetailCard.tsx — confirm prop interface: spell, onClose are required; magicas and onAddMagica are optional.
  </read_first>
  <action>
    Make three edits to `app/(tabs)/magia.tsx`:

    1. ADD import: insert `import SpellDetailCard from '@/components/rpg/SpellDetailCard';` after the existing `import { ErrorBoundary } from '@/components/rpg/ErrorBoundary';` line. Place it as the next line.

    2. DELETE the inline `function SpellDetailView(...)` block entirely (lines ~340–372 — the complete function including its closing brace).

    3. UPDATE all call sites — there are TWO places where SpellDetailView is used:
       a. In the outer Modal JSX (around line 308): `<SpellDetailView spell={viewSpell} onClose={() => setViewSpell(null)} />` → replace with `<SpellDetailCard spell={viewSpell} onClose={() => setViewSpell(null)} />`. Do NOT add magicas or onAddMagica (per D-03 — view-only, no "+ Mágica" button).
       b. Inside `function DomainView` (around lines 391–393): `return <SpellDetailView spell={selected} onClose={closeSpell} />;` → replace with `return <SpellDetailCard spell={selected} onClose={closeSpell} />;`. Same — no magicas/onAddMagica.

    After deletion, remove ONLY the style keys that were exclusively used by SpellDetailView and are now owned by SpellDetailCard's own StyleSheet. Safe to delete from magia.tsx's StyleSheet: `detailContent`, `spellImg`, `detailName`, `detailMeta`, `detailEffect`, `detailStats`, `pill`, `pillLabel`, `pillValue`.

    KEEP in magia.tsx's StyleSheet: `detailHeader`, `closeBtn`, `closeBtnText` — DomainView uses all three of these (lines ~401–408) for its domain title header and close button. Removing them would break DomainView's layout.

    Do NOT touch DomainView's internal logic, the domain modal, or any other part of the screen.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    - `app/(tabs)/magia.tsx` contains `import SpellDetailCard from '@/components/rpg/SpellDetailCard'`
    - `app/(tabs)/magia.tsx` does NOT contain `function SpellDetailView(`
    - `app/(tabs)/magia.tsx` does NOT contain `<SpellDetailView`
    - `app/(tabs)/magia.tsx` contains `<SpellDetailCard` (at least two occurrences — outer modal + DomainView)
    - `grep "detailHeader:" app/(tabs)/magia.tsx` exits 0 (StyleSheet definition must still exist — DomainView depends on it)
    - `npx tsc --noEmit` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component props | SpellDetailCard receives Spell objects derived from the static grimoire data array — no user-supplied or network-supplied data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | SpellDetailCard feedback state | accept | Local useState; no persistence, no network write. Value is a hardcoded string or spell.nome (static lookup). Risk: negligible. |
| T-03-02-02 | Information Disclosure | spellImages lookup | accept | spellImages is a static local require() map; no dynamic import or network path. Risk: none. |
| T-03-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. Existing lockfile unchanged. |
</threat_model>

<verification>
1. `grep -r "function SpellDetail(" app/` returns no matches (inline removed from grimorio.tsx)
2. `grep -r "function SpellDetailView(" app/` returns no matches (inline removed from magia.tsx)
3. `grep -r "SpellDetailCard" app/(tabs)/grimorio.tsx` returns a match (import + usage)
4. `grep -r "SpellDetailCard" app/(tabs)/magia.tsx` returns a match (import + at least two usages)
5. `grep "onAddMagica" app/(tabs)/magia.tsx` returns no match (view-only, per D-03)
6. `grep "onAddMagica={setMagica}" app/(tabs)/grimorio.tsx` returns a match (grimorio still has the button)
7. `grep "detailHeader:" app/(tabs)/magia.tsx` exits 0 (DomainView's shared style key preserved)
8. `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- SpellDetailCard.tsx is the single shared implementation for displaying spell details
- grimorio.tsx uses SpellDetailCard with onAddMagica (button visible)
- magia.tsx uses SpellDetailCard without onAddMagica (no button, per D-03)
- No inline SpellDetail or SpellDetailView functions remain in any tab file
- magia.tsx StyleSheet retains detailHeader, closeBtn, closeBtnText (used by DomainView)
- TypeScript compilation succeeds with no errors
</success_criteria>

<output>
Create `.planning/phases/03-qualidade-do-grimorio/03-02-SUMMARY.md` when done.
</output>
