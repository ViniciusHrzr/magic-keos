---
phase: 04-ferramentas-de-mesa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - types/character.ts
  - store/CharacterContext.tsx
  - app/(tabs)/regras.tsx
  - app/(tabs)/_layout.tsx
  - components/ui/icon-symbol.tsx
autonomous: true
requirements:
  - MESA-01
must_haves:
  truths:
    - "Ao abrir o app, uma 4ª aba 'Regras' aparece na tab bar após 'Grimório', com ícone visível"
    - "Tocar na aba Regras abre uma tela com 8 seções rolavéis: Mecânica de Dados, Testes, Combate, Condições, Canalização, Descanso, Balizadores, Notas"
    - "Cada seção (1–7) exibe ao menos uma tabela ou lista com conteúdo de regras em PT-BR extraído de GAME_RULES.md"
    - "A 8ª seção 'Notas' contém um TextInput multilinha vinculado a c.notas"
    - "Digitar em Notas, navegar para outra aba e voltar para Regras mostra o texto persistido (sem reset para vazio)"
    - "Fechar e reabrir o app preserva o conteúdo de Notas"
    - "Personagens previamente salvos (sem o campo notas) carregam sem crash; c.notas começa em '' após migração"
  artifacts:
    - path: "types/character.ts"
      provides: "Campo notas: string na interface Character e default ''"
      contains: "notas: string"
    - path: "store/CharacterContext.tsx"
      provides: "setNotas no contexto + migration guard para notas"
      contains: "setNotas"
    - path: "app/(tabs)/regras.tsx"
      provides: "Tela Regras com 8 SectionHeader + tabelas estáticas + Notas TextInput"
      min_lines: 200
    - path: "app/(tabs)/_layout.tsx"
      provides: "Registro <Tabs.Screen name='regras'> após grimorio e antes de explore"
      contains: "name=\"regras\""
    - path: "components/ui/icon-symbol.tsx"
      provides: "Mapping SF Symbol → Material Icon para o ícone da aba Regras"
      contains: "list.bullet"
  key_links:
    - from: "app/(tabs)/regras.tsx"
      to: "store/CharacterContext.tsx"
      via: "useCharacter() → { character: c, setNotas, isLoaded }"
      pattern: "setNotas"
    - from: "app/(tabs)/regras.tsx"
      to: "components/rpg/ErrorBoundary"
      via: "<ErrorBoundary> wrap"
      pattern: "<ErrorBoundary>"
    - from: "app/(tabs)/_layout.tsx"
      to: "components/ui/icon-symbol.tsx"
      via: "<IconSymbol name='list.bullet' .../>"
      pattern: "list.bullet"
---

<objective>
Adicionar uma 4ª aba "Regras" à navegação principal contendo uma referência rolável das mecânicas do sistema Kéos (Mecânica de Dados, Testes, Combate, Condições, Canalização, Descanso, Balizadores) e uma seção "Notas" persistente conectada ao Character model. Estende `types/character.ts` com `notas: string`, adiciona `setNotas` ao `CharacterContext` (com migration guard), registra a aba em `_layout.tsx` com novo SF Symbol mapeado.

Purpose: Atende MESA-01 — jogadores precisam consultar regras durante a sessão sem sair do app, e anotar lembretes pontuais que sobrevivem entre sessões.
Output: 1 arquivo novo (`regras.tsx`), 4 arquivos modificados surgicalmente; aba "Regras" funcional com Notas persistente.
</objective>

<execution_context>
@C:\Users\edysano\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\edysano\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ferramentas-de-mesa/04-CONTEXT.md
@.planning/phases/04-ferramentas-de-mesa/04-PATTERNS.md
@.planning/GAME_RULES.md

<interfaces>
<!-- Contracts the executor needs. Use these directly — no codebase exploration needed. -->

From types/character.ts (current state, lines 9–106):
- interface Character has a flat string field `proficiencias: string` at line 83 and `habilidades: string` at line 84.
- defaultCharacter at line 111 currently sets `proficiencias: ''` at line 161. NO `notas` field exists yet.

From store/CharacterContext.tsx (current state):
- migrate(raw) at lines 13–31 returns `{ ...defaultCharacter, ...parsed }` after several guard blocks.
- Setter pattern (line 135): `const setNome = useCallback((v: string) => update(p => ({ ...p, nome: v })), [update]);`
- Existing setter `setHabilidades` at line 152 follows identical shape and is the closest analog for the new `setNotas`.
- CharacterContextType interface lines 35–65 declares each setter; `setHabilidades: (v: string) => void` at line 48.
- contextValue useMemo at lines 242–259 lists every setter twice (object literal + deps array).

From app/(tabs)/_layout.tsx (current state, lines 28–49):
- `<Tabs.Screen name="grimorio" options={{ title: 'Grimório', tabBarIcon: ... book.fill ... }} />` (lines 42–48)
- `<Tabs.Screen name="explore" options={{ href: null }} />` (line 49) — hidden tab pattern
- IconSymbol import at line 5: `import { IconSymbol } from '@/components/ui/icon-symbol';`

From components/ui/icon-symbol.tsx (lines 16–24):
- MAPPING object — to add a new SF Symbol name, append a key/value pair before the closing `} as IconMapping;`
- Existing keys: 'house.fill', 'paperplane.fill', 'chevron.left.forwardslash.chevron.right', 'chevron.right', 'person.fill', 'sparkles', 'book.fill'

From app/(tabs)/magia.tsx (the analog screen for regras.tsx):
- Outer scaffold (lines 50–56): `if (!isLoaded) return <ActivityIndicator .../>` then `<ErrorBoundary><SafeAreaView style={styles.safe} edges={['top']}><ScrollView style={styles.scroll} contentContainerStyle={styles.content} keyboardShouldPersistTaps="handled">`
- titleBar pattern (lines 58–60): `<View style={styles.titleBar}><Text style={styles.title}>Regras</Text></View>`
- Base styles to mirror (lines 420–439): safe, scroll, content, titleBar, title

From app/(tabs)/index.tsx textArea pattern (lines used as Notas analog):
- `textArea: { backgroundColor: RPG.surface, color: RPG.text, fontSize: 13, padding: 10, minHeight: 100, borderBottomWidth: 1, borderBottomColor: RPG.border, lineHeight: 20, textAlignVertical: 'top' }`

From .planning/GAME_RULES.md — section-to-content mapping (use these sections verbatim, translate tables to React Native View+Text rows):
- §4 Mecânica de Dados → table: cor / dado / característica especial (5 rows: Branco/dW, Verde/dG, Vermelho/dR, Preto/dB, Azul/dU); + Resultados Possíveis (6 rows)
- §9 Testes → Difficulty table (6 rows: Facílimo/5, Fácil/10, Normal/15, Difícil/20, Dificílimo/25, Excepcional/30) with modificadores
- §18 Combate → action lists: Movimentação (3 ações), Operações (3 ações), Reações (5 ações), Manifestações (6 ações)
- §17 Condições → 7 condições + Veneno marker table (5 rows: 2/4/6/8/10 markers)
- §12 Canalização → Como Canalizar table (5 rows: 10+/15+/20+/25+/30+)
- §19 Descanso → Ação de descanso table (9 rows)
- §6 Balizadores → 5 balizadores (Foco/Canalização/Domínios/Memória/Velocidade) with cor-chave + função
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar campo notas ao Character model, migration e setter</name>
  <files>types/character.ts, store/CharacterContext.tsx</files>
  <read_first>
    - types/character.ts (current Character interface, defaultCharacter — verify current ordering at lines 83–84 and 161)
    - store/CharacterContext.tsx (migrate function lines 13–31, setProficiencias/setHabilidades pattern lines 151–152, CharacterContextType lines 35–65, contextValue useMemo lines 242–259)
    - .planning/phases/04-ferramentas-de-mesa/04-CONTEXT.md (D-01 lock)
    - .planning/phases/04-ferramentas-de-mesa/04-PATTERNS.md (Migration block + Setter pattern sections)
  </read_first>
  <action>
    Per D-01: editar `types/character.ts` para inserir `notas: string;` na interface `Character` imediatamente após `habilidades: string;` (linha 84), e adicionar `notas: '',` em `defaultCharacter` imediatamente após `habilidades: '',` (linha 162).

    Em `store/CharacterContext.tsx`, dentro de `migrate(raw)`, inserir o guard `if (!parsed.notas) parsed.notas = '';` imediatamente antes do `return { ...defaultCharacter, ...parsed };` final (após o guard `if (typeof parsed.magicas === 'string')`). Este guard cobre tanto valores `undefined` quanto strings vazias herdadas.

    Adicionar a declaração `setNotas: (v: string) => void;` em `CharacterContextType` imediatamente após `setHabilidades` (linha 48). Adicionar o setter `const setNotas = useCallback((v: string) => update(p => ({ ...p, notas: v })), [update]);` imediatamente após `setHabilidades` (linha 152), espelhando o formato exato (uma linha, mesmo padrão de espaçamento das setters string vizinhas).

    Adicionar `setNotas` em `contextValue` useMemo: incluir no objeto literal (após `setProficiencias, setHabilidades,`) E no array de deps (mesma posição). Ambas ocorrências necessárias — o useMemo lista cada setter duas vezes.

    NÃO alterar mais nada em `types/character.ts` ou `CharacterContext.tsx`. Nenhuma alteração de tipo no campo `proficiencias` neste plano (isso é escopo do 04-02).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -E "(notas|setNotas)" || echo "OK: nenhum erro de tipo relacionado a notas/setNotas"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "notas: string" types/character.ts` retorna exatamente 1 linha dentro do bloco `interface Character`
    - `grep -n "notas: ''" types/character.ts` retorna exatamente 1 linha dentro de `defaultCharacter`
    - `grep -n "if (!parsed.notas) parsed.notas = ''" store/CharacterContext.tsx` retorna 1 linha, posicionada antes do `return { ...defaultCharacter, ...parsed };` final da função `migrate`
    - `grep -n "setNotas" store/CharacterContext.tsx` retorna ao menos 4 linhas (declaração type, definição useCallback, item no objeto useMemo, item no array deps do useMemo)
    - `grep -c "setNotas" store/CharacterContext.tsx | grep -v '^#'` ≥ 4
    - `npx tsc --noEmit` completa sem novos erros relativos a Character.notas ou setNotas
    - Diff de `types/character.ts`: apenas 2 linhas adicionadas (1 na interface + 1 no default). Diff de `store/CharacterContext.tsx`: 5 linhas adicionadas (1 migration + 1 type decl + 1 setter + 1 useMemo obj + 1 useMemo deps).
  </acceptance_criteria>
  <done>Campo `notas` existe no model, migration cria valor padrão `''` para personagens antigos, `setNotas` está disponível via `useCharacter()`.</done>
</task>

<task type="auto">
  <name>Task 2: Criar tela Regras (regras.tsx) com 8 seções e Notas persistente</name>
  <files>app/(tabs)/regras.tsx</files>
  <read_first>
    - app/(tabs)/magia.tsx (outer scaffold lines 50–60, titleBar styles lines 420–439, ScrollView pattern)
    - app/(tabs)/index.tsx (textArea style for Notas — lines 479–489 conforme PATTERNS.md)
    - components/rpg/SectionHeader.tsx (uso: <SectionHeader title="..." />)
    - components/rpg/ErrorBoundary.tsx (uso: wrap externo)
    - constants/theme.ts (RPG.* tokens: bg, surface, surfaceAlt, headerBg, gold, goldDim, border, text, textMuted, textDark)
    - .planning/GAME_RULES.md §4 (Mecânica de Dados + Resultados Possíveis), §6 (Balizadores), §9 (Testes), §12 (Canalização — Como Canalizar), §17 (Condições + Veneno Marcadores), §18 (Combate — Ações), §19 (Descanso)
    - .planning/phases/04-ferramentas-de-mesa/04-CONTEXT.md (D-02 lock — 8 sections in this exact order)
    - .planning/phases/04-ferramentas-de-mesa/04-PATTERNS.md (regras.tsx pattern assignment)
  </read_first>
  <action>
    Criar novo arquivo `app/(tabs)/regras.tsx` exportando default `RegrasScreen` por D-02.

    Imports obrigatórios: React + useState (apenas se usar accordion), ScrollView/View/Text/TextInput/StyleSheet/ActivityIndicator/TouchableOpacity (TouchableOpacity opcional se for accordion) de 'react-native', SafeAreaView de 'react-native-safe-area-context', useCharacter de '@/store/CharacterContext', RPG de '@/constants/theme', SectionHeader default de '@/components/rpg/SectionHeader', ErrorBoundary nomeado de '@/components/rpg/ErrorBoundary'.

    Estrutura externa idêntica a magia.tsx (linhas 50–56): guard `if (!isLoaded) return <ActivityIndicator size="large" color={RPG.gold} style={{ flex: 1, backgroundColor: RPG.bg }} />;` seguido de `<ErrorBoundary><SafeAreaView style={styles.safe} edges={['top']}><ScrollView style={styles.scroll} contentContainerStyle={styles.content} keyboardShouldPersistTaps="handled">`.

    Destruturar do useCharacter: `{ character: c, setNotas, isLoaded }`.

    titleBar com título "Regras" usando o padrão de magia.tsx (linhas 58–60).

    Renderizar as 8 seções NESTA ORDEM (D-02), cada uma precedida por `<SectionHeader title="..." />`:
    1. **"Mecânica de Dados"** — duas tabelas: (a) 5 linhas Cor/Dado/Característica (Branco/dW/Progressão (≥14 dobra bônus), Verde/dG/2d10 + pares, Vermelho/dR/Crítico 19-20 expansível, Preto/dB/+1d4 se ≤5, Azul/dU/Rerolar em ×5); (b) Resultados Possíveis (6 linhas: SUCESSO, FRACASSO, SUCESSO CRÍTICO, FRACASSO CRÍTICO, SUCESSO MAS, FRACASSO MAS) com Condição → Efeito.
    2. **"Testes"** — tabela de Dificuldade × Valor × Modificador (6 linhas: Facílimo/5/[+X], Fácil/10/vantagem, Normal/15/—, Difícil/20/[-X], Dificílimo/25/desvantagem, Excepcional/30/—).
    3. **"Combate"** — texto introdutório "Ações por turno: 1 movimentação + 1 operação + 1 reação + extras de Velocidade" + 4 sub-blocos: Movimentação (3), Operações (3), Reações (5), Manifestações (6). Cada sub-bloco pode ser um sub-título (sem usar SectionHeader para não confundir hierarquia) com linhas Ação | Custo | Efeito.
    4. **"Condições"** — lista de 7 condições (Alado, Congelado, Envenenado, Incendiado, Molhado, Morrendo, Necrosado) com efeito + tabela "Veneno — Marcadores" (5 linhas: 2/4/6/8/10).
    5. **"Canalização"** — tabela "Como Canalizar" (5 linhas: 10+/1d4, 15+/1d6, 20+/1d8, 25+/1d10, 30+/1d12) com Resultado | Mana obtido. Incluir nota "Teste: Vontade [Comunhão] em descanso".
    6. **"Descanso"** — tabela Ação | Teste | Resultado (9 linhas: Repousar, Praticar, Canalizar, Coletar, Produzir, Improvisar, Inscrever, Fabricar, Reparar).
    7. **"Balizadores"** — 5 balizadores (Foco/Branco, Canalização/Verde, Domínios/Preto, Memória/Azul, Velocidade/Vermelho) com Função.
    8. **"Notas"** — TextInput multiline conectado a `c.notas`/`setNotas`, com props: `style={styles.textArea}`, `value={c.notas}`, `onChangeText={setNotas}`, `multiline`, `placeholder="Anotações de sessão..."`, `placeholderTextColor={RPG.textDark}`, `textAlignVertical="top"`.

    Para tabelas, usar shape `<View style={styles.tableRow}><Text style={styles.tableKey}>{label}</Text><Text style={styles.tableVal}>{value}</Text></View>` agrupado em `<View style={styles.tableBlock}>`. Para tabelas de 3 colunas (ex.: Ação/Custo/Efeito ou Resultado/Mana), adicionar uma 3ª `Text` style `tableMid`.

    StyleSheet copiando safe/scroll/content/titleBar/title de magia.tsx (linhas 420–439) e textArea de index.tsx (referência em PATTERNS.md). Adicionar styles novos: tableBlock (background RPG.surface, paddingVertical 8, paddingHorizontal 12, borderBottomWidth 1, borderBottomColor RPG.border), tableRow (flexDirection 'row', paddingVertical 4, gap 8), tableKey (color RPG.gold, fontSize 12, fontWeight '600', minWidth 80), tableMid (color RPG.text, fontSize 12, flex 1), tableVal (color RPG.textMuted, fontSize 12, flex 2), sectionIntro (color RPG.textMuted, fontSize 12, fontStyle 'italic', paddingHorizontal 12, paddingVertical 6).

    Conteúdo das tabelas: o texto exato deve ser fiel a GAME_RULES.md (PT-BR). Não inventar valores. Se um cell precisar de uma única palavra/abreviação curta, usar a forma que aparece em GAME_RULES.md (ex.: "Facílimo" não "Muito Fácil").

    Discrição (D-02 Claude's Discretion): pode optar por scroll flat (sem accordion) — simpler para uma primeira entrega. Não use Modal para tabelas (manter inline).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -E "regras\.tsx" || echo "OK: regras.tsx sem erros de tipo"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `app/(tabs)/regras.tsx` existe
    - `grep -c "SectionHeader" app/\(tabs\)/regras.tsx` ≥ 8 (uma por seção)
    - `grep -n "title=\"Mecânica de Dados\"" app/\(tabs\)/regras.tsx` retorna 1 linha
    - `grep -n "title=\"Testes\"" app/\(tabs\)/regras.tsx` retorna 1 linha
    - `grep -n "title=\"Combate\"" app/\(tabs\)/regras.tsx` retorna 1 linha
    - `grep -n "title=\"Condições\"" app/\(tabs\)/regras.tsx` retorna 1 linha
    - `grep -n "title=\"Canalização\"" app/\(tabs\)/regras.tsx` retorna 1 linha
    - `grep -n "title=\"Descanso\"" app/\(tabs\)/regras.tsx` retorna 1 linha
    - `grep -n "title=\"Balizadores\"" app/\(tabs\)/regras.tsx` retorna 1 linha
    - `grep -n "title=\"Notas\"" app/\(tabs\)/regras.tsx` retorna 1 linha
    - `grep -n "value={c.notas}" app/\(tabs\)/regras.tsx` retorna 1 linha
    - `grep -n "onChangeText={setNotas}" app/\(tabs\)/regras.tsx` retorna 1 linha
    - `grep -n "ErrorBoundary" app/\(tabs\)/regras.tsx` retorna ≥ 2 (open + close tag) E o JSX raiz é envolvido por `<ErrorBoundary>`
    - `grep -n "SafeAreaView" app/\(tabs\)/regras.tsx` retorna ≥ 1 com `edges={['top']}`
    - `grep -n "isLoaded" app/\(tabs\)/regras.tsx` retorna ≥ 1 (guard ActivityIndicator)
    - `wc -l app/(tabs)/regras.tsx` ≥ 200 linhas (8 seções com tabelas)
    - `npx tsc --noEmit` completa sem erros novos em `app/(tabs)/regras.tsx`
  </acceptance_criteria>
  <done>Tela Regras renderiza 8 seções na ordem D-02, Notas TextInput conecta a `c.notas`/`setNotas`, segue scaffold ErrorBoundary+SafeAreaView+ScrollView idêntico ao restante das telas, conteúdo das tabelas vem de GAME_RULES.md.</done>
</task>

<task type="auto">
  <name>Task 3: Registrar aba Regras no layout e mapear ícone SF Symbol</name>
  <files>app/(tabs)/_layout.tsx, components/ui/icon-symbol.tsx</files>
  <read_first>
    - app/(tabs)/_layout.tsx (current Tabs.Screen entries lines 28–49 — note grimorio at 42–48 and explore at 49)
    - components/ui/icon-symbol.tsx (MAPPING object lines 16–24)
    - .planning/phases/04-ferramentas-de-mesa/04-CONTEXT.md (D-03 lock — icon name choice "list.bullet" or "book.pages.fill")
    - .planning/phases/04-ferramentas-de-mesa/04-PATTERNS.md (_layout.tsx + icon-symbol.tsx pattern blocks)
  </read_first>
  <action>
    Per D-03: adicionar uma entrada `<Tabs.Screen name="regras" options={{ title: 'Regras', tabBarIcon: ({ color }) => <IconSymbol size={26} name="list.bullet" color={color} /> }} />` em `app/(tabs)/_layout.tsx`, posicionada IMEDIATAMENTE após o `<Tabs.Screen name="grimorio" .../>` (que termina na linha 48) e ANTES de `<Tabs.Screen name="explore" options={{ href: null }} />` (linha 49). Mirror props formatting do bloco grimorio: title como string PT-BR ('Regras'), tabBarIcon como arrow function com `({ color })` desestruturado retornando IconSymbol com size=26 e mesma prop color.

    Em `components/ui/icon-symbol.tsx`, adicionar dentro do `MAPPING` (entre as linhas existentes e o fechamento `} as IconMapping;` linha 24) uma nova entrada `'list.bullet': 'list',`. Manter alinhamento/indentação do objeto. Não remover nem reordenar entradas existentes.

    Usando "list.bullet" e Material Icon "list" (escolha de D-03 — alternativa book.pages.fill seria 'menu-book-fill' mas como 'book.fill' já está mapeado para 'menu-book' poderia confundir; "list.bullet" → "list" é distinto e legível).

    NÃO alterar `tabBarStyle`, `screenOptions` ou a ordem das demais telas.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -E "(_layout|icon-symbol)" || echo "OK: layout/icon sem erros de tipo"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "name=\"regras\"" app/\(tabs\)/_layout.tsx` retorna 1 linha
    - `grep -n "title: 'Regras'" app/\(tabs\)/_layout.tsx` retorna 1 linha
    - `grep -n "list.bullet" app/\(tabs\)/_layout.tsx` retorna 1 linha
    - `grep -B2 -A1 "name=\"regras\"" app/\(tabs\)/_layout.tsx` mostra que a entrada está APÓS o bloco grimorio e ANTES de `name="explore"`
    - `grep -n "'list.bullet': 'list'" components/ui/icon-symbol.tsx` retorna 1 linha dentro do bloco MAPPING
    - O array de keys em `components/ui/icon-symbol.tsx` ainda contém todas as 7 entradas originais (`house.fill`, `paperplane.fill`, `chevron.left.forwardslash.chevron.right`, `chevron.right`, `person.fill`, `sparkles`, `book.fill`) — `grep -c "':" components/ui/icon-symbol.tsx | grep -v '^#'` ≥ 8 (7 originais + 1 nova)
    - `npx tsc --noEmit` completa sem erros novos
  </acceptance_criteria>
  <done>Aba Regras aparece na tab bar entre Grimório e (oculto) explore com label "Regras" e ícone de lista; o nome SF Symbol "list.bullet" é resolvido pelo MAPPING em runtime.</done>
</task>

</tasks>

<verification>
**Manual smoke test após execução automatizada:**
1. `npx tsc --noEmit` — zero erros
2. `npx expo start` → abrir no device → verificar 4ª aba "Regras" visível com ícone de lista após "Grimório"
3. Tocar em Regras → 8 sections rolavéis aparecem na ordem: Mecânica de Dados → Testes → Combate → Condições → Canalização → Descanso → Balizadores → Notas
4. Digitar "teste persistência" em Notas → trocar para Ficha → voltar para Regras → texto permanece
5. Fechar app (kill process) → reabrir → Notas ainda contém "teste persistência"
6. Limpar AsyncStorage → criar personagem novo → Notas começa vazio (sem crash de migration)
</verification>

<success_criteria>
- MESA-01 ✓: Aba "Regras" existe com referência das 7 mecânicas (§4/§6/§9/§12/§17/§18/§19) + Notas persistente
- Personagens antigos (sem campo `notas`) carregam sem crash após migration guard
- Nenhuma regressão em tabs Ficha/Magia/Grimório
- ROADMAP success criterion #1 satisfeito (aba Regras com 7 seções + Notas que persiste entre sessões)
</success_criteria>

<output>
Criar `.planning/phases/04-ferramentas-de-mesa/04-01-SUMMARY.md` ao concluir.
</output>
