---
phase: 04-ferramentas-de-mesa
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - types/character.ts
  - store/CharacterContext.tsx
  - data/proficiencias.ts
  - components/rpg/ProficienciasSection.tsx
  - app/(tabs)/index.tsx
autonomous: true
requirements:
  - MESA-02
  - MESA-03
must_haves:
  truths:
    - "Na aba Ficha, a seção 'Proficiências' renderiza chips togláveis agrupados por perícia (CORPO/MENTE/ESPÍRITO), não um TextInput livre"
    - "Tocar em um chip não-selecionado adiciona a entrada `pericia:nome` a c.proficiencias[] e o chip exibe estilo dourado"
    - "Tocar em um chip selecionado remove a entrada de c.proficiencias[] e o chip volta ao estilo muted"
    - "Fechar/reabrir o app preserva a lista de chips selecionados"
    - "Personagens existentes salvos com `proficiencias: 'texto livre'` carregam sem crash e c.proficiencias passa a ser [] (sem perda do resto do personagem)"
    - "Personagens existentes salvos com `proficiencias: [...]` (caso de re-importação) preservam o array intacto"
    - "Cada perícia listada em data/proficiencias.ts (15 perícias: 5 CORPO + 5 MENTE + 5 ESPÍRITO) tem seu nome exibido como cabeçalho de grupo"
    - "Todas as proficiências do CONTEXT.md `<specifics>` aparecem como chips na ordem indicada"
  artifacts:
    - path: "types/character.ts"
      provides: "Campo proficiencias com tipo string[] e default []"
      contains: "proficiencias: string[]"
    - path: "store/CharacterContext.tsx"
      provides: "setProficiencias com nova assinatura (v: string[]) → void + migration guard"
      contains: "if (typeof parsed.proficiencias === 'string')"
    - path: "data/proficiencias.ts"
      provides: "Estrutura tipada com 15 perícias e suas proficiências (PT-BR, verbatim do CONTEXT.md)"
      min_lines: 60
    - path: "components/rpg/ProficienciasSection.tsx"
      provides: "Componente default export com props { selected: string[]; onChange: (v: string[]) => void }"
      min_lines: 100
    - path: "app/(tabs)/index.tsx"
      provides: "Substituição do TextInput de proficiências por <ProficienciasSection />"
      contains: "ProficienciasSection"
  key_links:
    - from: "app/(tabs)/index.tsx"
      to: "components/rpg/ProficienciasSection"
      via: "default import + JSX <ProficienciasSection selected={c.proficiencias} onChange={setProficiencias} />"
      pattern: "ProficienciasSection"
    - from: "components/rpg/ProficienciasSection.tsx"
      to: "data/proficiencias.ts"
      via: "named import { proficiencias } from '@/data/proficiencias'"
      pattern: "from '@/data/proficiencias'"
    - from: "store/CharacterContext.tsx"
      to: "types/character.ts"
      via: "setProficiencias signature alinhada com Character['proficiencias'] = string[]"
      pattern: "setProficiencias = useCallback\\(\\(v: string\\[\\]\\)"
---

<objective>
Substituir o campo livre `proficiencias: string` por um seletor estruturado de chips togláveis agrupados por perícia. Atualizar o tipo `Character.proficiencias` para `string[]`, adicionar migration guard que converte strings antigas em array vazio, criar arquivo de dados `data/proficiencias.ts` com a taxonomia completa CORPO/MENTE/ESPÍRITO × 15 perícias, criar componente `ProficienciasSection` consumindo esses dados, e trocar o TextInput em `app/(tabs)/index.tsx` pelo novo componente.

Purpose: Atende MESA-02 (UI estruturada) e MESA-03 (migration sem perda) — elimina input livre que tornava difícil consultar/contabilizar proficiências durante a sessão.
Output: 3 arquivos modificados surgicalmente, 2 arquivos novos. Tela Ficha ganha picker de chips no lugar do TextInput.
</objective>

<execution_context>
@C:\Users\edysano\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\edysano\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ferramentas-de-mesa/04-CONTEXT.md
@.planning/phases/04-ferramentas-de-mesa/04-PATTERNS.md
@.planning/phases/04-ferramentas-de-mesa/04-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Use these directly — no codebase exploration needed. -->

From types/character.ts (state AFTER 04-01 — notas já adicionado):
- `proficiencias: string` at line 83 (within Character interface) — must change to `proficiencias: string[]`
- `proficiencias: ''` em defaultCharacter (linha ~161) — must change to `proficiencias: []`

From store/CharacterContext.tsx (state AFTER 04-01 — setNotas já existe):
- Existing `setProficiencias: (v: string) => void;` na interface (linha 47). Mudar para `setProficiencias: (v: string[]) => void;` (mesmo nome, nova assinatura por D-04).
- Existing useCallback line 151: `const setProficiencias = useCallback((v: string) => update(p => ({ ...p, proficiencias: v })), [update]);` — mudar parâmetro para `(v: string[])`.
- migrate() function — adicionar guard `if (typeof parsed.proficiencias === 'string') parsed.proficiencias = [];` antes do return final, junto com (e após) o guard de notas adicionado em 04-01.

From data/grimoire.ts (estrutura analog para data/proficiencias.ts):
- Pure data exports (`export type ...`, `export interface ...`, `export const ...`). Sem default export. Sem side-effects além de transformação JSON.

From components/rpg/NumericStepper.tsx (estrutura de componente mínimo):
- Pattern: `interface Props { ... }`; `export default function Name({ ... }: Props) { ... }`; `const styles = StyleSheet.create({ ... });`

From app/(tabs)/grimorio.tsx (chip toggle visual analog — filterBtn2/filterBtn2Active lines ~282–291):
- Toggle pattern: `style={[styles.filterBtn2, isActive && styles.filterBtn2Active]}` onPress: `() => { const next = isActive ? null : value; setValue(next); }`
- Style tokens: paddingHorizontal 8, paddingVertical 3, borderWidth 1, borderColor RPG.border, borderRadius 10 (chip base) / borderColor RPG.gold + backgroundColor RPG.goldDim + '33' (active)
- Expandable section row pattern (lines ~92–106): TouchableOpacity row with title + chevron (▲/▼); state via `useState<Set<string>>` toggling membership

From app/(tabs)/index.tsx (current Proficiências TextInput, lines 177–188):
- Block to replace: `<SectionHeader title="Proficiências" />\n<TextInput style={styles.textArea} value={c.proficiencias} onChangeText={setProficiencias} multiline placeholder="Liste as proficiências do personagem..." placeholderTextColor={RPG.textDark} textAlignVertical="top" />`
- `setProficiencias` já destruturado de useCharacter na linha 26 — não precisa adicionar/remover do destructure
- Import a adicionar: `import ProficienciasSection from '@/components/rpg/ProficienciasSection';` próximo aos demais imports de components/rpg (linhas 6–17)

From CONTEXT.md `<specifics>` — proficiências VERBATIM (D-05 lock):
CORPO:
  - artesMarciais: ["Derrubar", "Desarmar", "Desviar", "Fintar", "Imobilizar", "Aparar (reação lv.2)"]
  - atletismo: ["Investida", "Prontidão", "Fôlego", "Disparar (reação lv.1)"]
  - esgrima: ["Armas Leves", "Uma Mão", "Duas Mãos", "Especialização", "Mestria", "Contra-atacar (reação lv.2)"]
  - furtividade: ["Ataque Furtivo", "Ataque Letal", "Ataque Silencioso", "Esquivar (reação lv.1)"]
  - pontaria: ["Arcos", "Arremesso", "Condutores", "Especialização", "Mestria", "Mirar (reação lv.2)"]
MENTE:
  - alquimia: ["Herbologia", "Mineralogia", "Zoologia", "Poções (lv.1)"]
  - criatividade: ["Recapitular", "Reciclar", "Reforçar", "Repartir", "Replicar", "Solução (lv.1)"]
  - investigacao: ["Selo de Feitiço", "Encantamento", "Invocação", "Leitura (lv.1)"]
  - mecanica: ["Artesão", "Feiticeiro", "Ferreiro", "Artefatos (lv.1)"]
  - sobrevivencia: ["Acampamento", "Harmonização", "Forrageamento", "Manufaturação", "Treinamento", "Coleta (lv.1)"]
ESPÍRITO:
  - comunhao: ["Provocar (ação livre lv.3)"]
  - diplomacia: ["Coordenar (ação livre lv.3)"]
  - expressao: ["Inspirar (ação livre lv.3)"]
  - intimidacao: ["Amedrontar (ação livre lv.3)"]
  - labia: ["Distrair (ação livre lv.3)"]

Pericia labels (PT-BR display names): artesMarciais→"Artes Marciais", atletismo→"Atletismo", esgrima→"Esgrima", furtividade→"Furtividade", pontaria→"Pontaria", alquimia→"Alquimia", criatividade→"Criatividade", investigacao→"Investigação", mecanica→"Mecânica", sobrevivencia→"Sobrevivência", comunhao→"Comunhão", diplomacia→"Diplomacia", expressao→"Expressão", intimidacao→"Intimidação", labia→"Lábia".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar Character.proficiencias para string[], migration guard e setter</name>
  <files>types/character.ts, store/CharacterContext.tsx</files>
  <read_first>
    - types/character.ts (após 04-01: notas já existe; proficiencias ainda é string na linha 83; default '' na linha ~161)
    - store/CharacterContext.tsx (após 04-01: setNotas adicionado; setProficiencias na linha 151 ainda usa (v: string); migrate() já tem guard para notas; interface line 47 ainda diz `setProficiencias: (v: string) => void`)
    - .planning/phases/04-ferramentas-de-mesa/04-CONTEXT.md (D-04 lock)
    - .planning/phases/04-ferramentas-de-mesa/04-PATTERNS.md (Migration block + Setter pattern sections)
  </read_first>
  <action>
    Per D-04: editar `types/character.ts` mudando exatamente o tipo do campo `proficiencias`:
    - Linha 83: trocar `proficiencias: string;` por `proficiencias: string[];`
    - Linha ~161 (em defaultCharacter): trocar `proficiencias: '',` por `proficiencias: [],`
    Nenhuma outra alteração no arquivo. Não remover nem renomear `proficiencias`.

    Em `store/CharacterContext.tsx`:
    - Em `migrate(raw)`, adicionar o guard `if (typeof parsed.proficiencias === 'string') parsed.proficiencias = [];` IMEDIATAMENTE antes ou após o guard `if (!parsed.notas) parsed.notas = '';` (adicionado em 04-01), e ANTES do `return { ...defaultCharacter, ...parsed };` final. Posicionamento exato: depois do bloco `if (typeof parsed.magicas === 'string') { parsed.magicas = Array(20).fill(''); }`.
    - Em `CharacterContextType` (linha 47): trocar `setProficiencias: (v: string) => void;` por `setProficiencias: (v: string[]) => void;`. Nome do setter idêntico — apenas o tipo do parâmetro muda.
    - Em `setProficiencias` useCallback (linha 151): trocar `(v: string)` por `(v: string[])`. O corpo `update(p => ({ ...p, proficiencias: v }))` permanece igual — TypeScript inferirá a partir da nova interface.
    - NÃO mexer no useMemo `contextValue` (`setProficiencias` já está listado lá; a referência permanece válida).

    Verificar manualmente no diff que NÃO há outras ocorrências de `proficiencias` que precisem ajuste (ex.: nenhum outro setter, nenhum método importJson manipulando proficiencias como string).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -E "proficiencias" || echo "OK: proficiencias sem erros de tipo"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "proficiencias: string\\[\\]" types/character.ts` retorna 1 linha (dentro da interface Character)
    - `grep -n "proficiencias: \\[\\]" types/character.ts` retorna 1 linha (dentro de defaultCharacter)
    - `grep -n "proficiencias: string;" types/character.ts` retorna 0 linhas (tipo antigo eliminado)
    - `grep -n "proficiencias: ''," types/character.ts` retorna 0 linhas (default antigo eliminado)
    - `grep -n "if (typeof parsed.proficiencias === 'string')" store/CharacterContext.tsx` retorna 1 linha, posicionada antes do return final de migrate()
    - `grep -n "setProficiencias: (v: string\\[\\]) => void" store/CharacterContext.tsx` retorna 1 linha (interface declaration)
    - `grep -n "setProficiencias = useCallback((v: string\\[\\])" store/CharacterContext.tsx` retorna 1 linha
    - `grep -n "setProficiencias: (v: string) => void" store/CharacterContext.tsx` retorna 0 linhas
    - `npx tsc --noEmit` reporta erros APENAS em `app/(tabs)/index.tsx` (que será corrigido na Task 3) — nenhum erro em types/character.ts ou store/CharacterContext.tsx
  </acceptance_criteria>
  <done>Tipo de `proficiencias` é `string[]`, default é `[]`, migration converte strings antigas, setter aceita arrays. Erros de tipo restantes ficam apenas em `index.tsx` até Task 3 substituir o TextInput.</done>
</task>

<task type="auto">
  <name>Task 2: Criar data/proficiencias.ts e components/rpg/ProficienciasSection.tsx</name>
  <files>data/proficiencias.ts, components/rpg/ProficienciasSection.tsx</files>
  <read_first>
    - data/grimoire.ts (estrutura analog: type exports, interface, const tipado)
    - components/rpg/NumericStepper.tsx (estrutura de componente: imports, Props interface, default export, StyleSheet)
    - components/rpg/SectionHeader.tsx (visualizar uso — provavelmente NÃO usado dentro de ProficienciasSection; usar header customizado)
    - app/(tabs)/grimorio.tsx (chip filterBtn2 styles ~282–291, domain expand/collapse pattern ~92–106 com chevron ▲▼, useState<Set<string>> pattern linha 49)
    - constants/theme.ts (RPG.gold, RPG.goldDim, RPG.goldLight, RPG.border, RPG.text, RPG.textMuted, RPG.surface, RPG.surfaceAlt)
    - .planning/phases/04-ferramentas-de-mesa/04-CONTEXT.md (D-05 dados verbatim, D-06 contrato de componente)
    - .planning/phases/04-ferramentas-de-mesa/04-PATTERNS.md (proficiencias.ts + ProficienciasSection.tsx pattern blocks)
  </read_first>
  <action>
    **Arquivo 1: `data/proficiencias.ts`** (D-05).

    Exportações obrigatórias:
    - `export type InstanciaKey = 'corpo' | 'mente' | 'espirito';`
    - `export type PericiaKey =` união literal das 15 chaves: 'artesMarciais' | 'atletismo' | 'esgrima' | 'furtividade' | 'pontaria' | 'alquimia' | 'criatividade' | 'investigacao' | 'mecanica' | 'sobrevivencia' | 'comunhao' | 'diplomacia' | 'expressao' | 'intimidacao' | 'labia'
    - `export interface PericiaData { label: string; proficiencias: string[]; }`
    - `export type ProficienciasMap = Record<InstanciaKey, Partial<Record<PericiaKey, PericiaData>>>;` (Partial porque nem toda perícia pertence a toda instância)
    - `export const proficiencias: ProficienciasMap = { corpo: {...}, mente: {...}, espirito: {...} };`

    Conteúdo de `proficiencias`: as 15 perícias com seus arrays de proficiências EXATAMENTE como listado no bloco `<interfaces>` deste plano (que é cópia verbatim do CONTEXT.md `<specifics>`). Cada entrada PericiaData tem:
    - `label`: PT-BR display name (mapping no `<interfaces>`)
    - `proficiencias`: array de strings na ordem dada

    NÃO inventar proficiências, NÃO traduzir strings, NÃO reorganizar. Strings com parênteses (ex.: `"Aparar (reação lv.2)"`) devem ser preservadas exatamente, incluindo acentos.

    Adicionalmente exportar uma lista ordenada para iteração:
    - `export const periciaOrdem: { instancia: InstanciaKey; periciaKey: PericiaKey }[] = [...]` com as 15 perícias na ordem CORPO (5) → MENTE (5) → ESPÍRITO (5) como aparecem no CONTEXT.md.

    Sem default export. Sem JSX. Sem React imports.

    **Arquivo 2: `components/rpg/ProficienciasSection.tsx`** (D-06).

    Imports: React + useState, View/Text/TouchableOpacity/StyleSheet de 'react-native', RPG de '@/constants/theme', SectionHeader default de '@/components/rpg/SectionHeader', e named imports de '@/data/proficiencias' (proficiencias, periciaOrdem, PericiaKey, InstanciaKey).

    Props interface conforme D-06:
    `interface Props { selected: string[]; onChange: (v: string[]) => void; }`

    `export default function ProficienciasSection({ selected, onChange }: Props)`:
    - Render externo: começa com `<SectionHeader title="Proficiências" />` e em seguida envolve as 15 perícias em `<View style={styles.container}>`.
    - Estado local: `const [expanded, setExpanded] = useState<Set<string>>(new Set());` com helper `const toggle = (k: string) => setExpanded(prev => { const n = new Set(prev); n.has(k) ? n.delete(k) : n.add(k); return n; });`
    - Iterar `periciaOrdem` — para cada `{ instancia, periciaKey }`:
      * Lookup: `const data = proficiencias[instancia][periciaKey]; if (!data) return null;`
      * Computar `const isOpen = expanded.has(periciaKey);`
      * Render row `<TouchableOpacity style={styles.sectionRow} onPress={() => toggle(periciaKey)} activeOpacity={0.75}>` com:
        - `<Text style={styles.instanceTag}>{instancia.toUpperCase()}</Text>` (mostra "CORPO", "MENTE", "ESPÍRITO" como mini-label muted)
        - `<Text style={styles.sectionTitle}>{data.label}</Text>`
        - `<Text style={styles.chevron}>{isOpen ? '▲' : '▼'}</Text>`
      * Quando `isOpen`, renderizar `<View style={styles.chipsWrap}>` mapeando `data.proficiencias.map(profNome => { ... })`:
        - `const key = ${periciaKey}:${profNome};` (template literal — chave estável para storage)
        - `const isSelected = selected.includes(key);`
        - `<TouchableOpacity key={key} style={[styles.chip, isSelected && styles.chipActive]} onPress={() => onChange(isSelected ? selected.filter(k => k !== key) : [...selected, key])} activeOpacity={0.7}><Text style={[styles.chipText, isSelected && styles.chipTextActive]}>{profNome}</Text></TouchableOpacity>`

    Toggle behavior por D-06: se `key` está em `selected` → remover; se não → adicionar. NÃO usar Set internamente para o array `selected` — preservar tipo `string[]` em onChange.

    StyleSheet (valores absolutos, sem fenced blocks):
    - container: padding 0 (deixar SectionHeader gerenciar border) — ou backgroundColor RPG.surface, padding 8
    - sectionRow: flexDirection 'row', alignItems 'center', paddingHorizontal 12, paddingVertical 10, gap 8, borderBottomWidth 1, borderBottomColor RPG.border, backgroundColor RPG.surface
    - instanceTag: color RPG.textMuted, fontSize 9, letterSpacing 0.5, minWidth 50, fontWeight '600'
    - sectionTitle: flex 1, color RPG.gold, fontSize 13, fontWeight '600'
    - chevron: color RPG.gold, fontSize 11
    - chipsWrap: flexDirection 'row', flexWrap 'wrap', gap 6, padding 10, backgroundColor RPG.surfaceAlt, borderBottomWidth 1, borderBottomColor RPG.border
    - chip: paddingHorizontal 8, paddingVertical 3, borderWidth 1, borderColor RPG.border, borderRadius 10, backgroundColor 'transparent'
    - chipActive: borderColor RPG.gold, backgroundColor RPG.goldDim + '33'
    - chipText: fontSize 11, color RPG.textMuted
    - chipTextActive: color RPG.goldLight, fontWeight '600'

    NÃO usar AsyncStorage diretamente — todo estado vem de props.
    NÃO usar useMemo para `selected.includes` (15 perícias, máx ~60 proficiências; cost trivial).
    NÃO declarar nenhum global module-level (sem padrão `_filter` do grimorio.tsx — esse padrão é específico de filtros voláteis).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -E "(proficiencias\.ts|ProficienciasSection)" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `data/proficiencias.ts` existe; arquivo `components/rpg/ProficienciasSection.tsx` existe
    - `grep -c "export" data/proficiencias.ts` ≥ 5 (InstanciaKey, PericiaKey, PericiaData, ProficienciasMap, proficiencias, periciaOrdem)
    - `grep -c "label:" data/proficiencias.ts` = 15 (uma por perícia)
    - `grep -n "Aparar (reação lv.2)" data/proficiencias.ts` retorna 1 linha (string verbatim preservada)
    - `grep -n "Selo de Feitiço" data/proficiencias.ts` retorna 1 linha (acentos preservados)
    - `grep -n "Provocar (ação livre lv.3)" data/proficiencias.ts` retorna 1 linha
    - `wc -l data/proficiencias.ts` ≥ 60
    - `grep -c "proficiencias: \\[" data/proficiencias.ts` = 15 (uma array por perícia, todas declaradas)
    - `grep -n "export default function ProficienciasSection" components/rpg/ProficienciasSection.tsx` retorna 1 linha
    - `grep -n "selected: string\\[\\]" components/rpg/ProficienciasSection.tsx` retorna 1 linha (Props.selected)
    - `grep -n "onChange: (v: string\\[\\]) => void" components/rpg/ProficienciasSection.tsx` retorna 1 linha (Props.onChange)
    - `grep -n "from '@/data/proficiencias'" components/rpg/ProficienciasSection.tsx` retorna 1 linha
    - `grep -E "selected\\.filter\\(k *=> *k *!== *key\\)|isSelected *\\? *selected\\.filter" components/rpg/ProficienciasSection.tsx` retorna ≥ 1 linha (remoção)
    - `grep -E "\\[\\.\\.\\.selected, *key\\]" components/rpg/ProficienciasSection.tsx` retorna ≥ 1 linha (adição)
    - `wc -l components/rpg/ProficienciasSection.tsx` ≥ 100
    - `npx tsc --noEmit` reporta APENAS o erro residual em `app/(tabs)/index.tsx` (string vs string[] mismatch ainda presente até Task 3) — sem erros novos em data/proficiencias.ts ou ProficienciasSection.tsx
  </acceptance_criteria>
  <done>Estrutura de dados completa e tipada com as 15 perícias verbatim; componente ProficienciasSection renderiza chips togláveis usando o contrato exato definido em D-06.</done>
</task>

<task type="auto">
  <name>Task 3: Substituir TextInput de proficiências por ProficienciasSection em index.tsx</name>
  <files>app/(tabs)/index.tsx</files>
  <read_first>
    - app/(tabs)/index.tsx (bloco proficiências lines 177–188; import block lines 6–17; useCharacter destructure line 21–27 — note `setProficiencias` já está lá)
    - components/rpg/ProficienciasSection.tsx (após Task 2: confirmar default export e Props {selected, onChange})
    - .planning/phases/04-ferramentas-de-mesa/04-CONTEXT.md (D-07 lock)
    - .planning/phases/04-ferramentas-de-mesa/04-PATTERNS.md (index.tsx surgical edit block)
  </read_first>
  <action>
    Per D-07: em `app/(tabs)/index.tsx`:

    1. Adicionar `import ProficienciasSection from '@/components/rpg/ProficienciasSection';` junto aos imports de components/rpg (após linha 17 onde fica `ErrorBoundary` — manter agrupamento lógico).

    2. Substituir o bloco proficiências inteiro (linhas 177–188) — o `<SectionHeader title="Proficiências" />` E o `<TextInput style={styles.textArea} value={c.proficiencias} onChangeText={setProficiencias} multiline placeholder="Liste as proficiências do personagem..." placeholderTextColor={RPG.textDark} textAlignVertical="top" />` — por uma única linha: `<ProficienciasSection selected={c.proficiencias} onChange={setProficiencias} />`. NOTE: o próprio `ProficienciasSection` já renderiza seu SectionHeader internamente (decidido na Task 2), então o SectionHeader externo deve ser REMOVIDO para não duplicar título.

    3. NÃO remover o destructure de `setProficiencias` (linha 26) — ele permanece em uso.
    4. NÃO alterar o bloco HABILIDADES (linhas 190–199) — ele continua com TextInput.
    5. NÃO remover o style `textArea` do StyleSheet, pois é usado pelo bloco habilidades.

    Confirmar visualmente o diff: 1 linha de import adicionada, ~11 linhas removidas (bloco SectionHeader+TextInput original), 1 linha adicionada (`<ProficienciasSection />`). Net: ~-9 linhas no arquivo.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -v "node_modules" | grep -E "(error|Cannot)" || echo "OK: zero erros de tipo no projeto"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "import ProficienciasSection" app/\(tabs\)/index.tsx` retorna 1 linha
    - `grep -n "ProficienciasSection selected={c.proficiencias} onChange={setProficiencias}" app/\(tabs\)/index.tsx` retorna 1 linha
    - `grep -n "value={c.proficiencias}" app/\(tabs\)/index.tsx` retorna 0 linhas (TextInput antigo eliminado)
    - `grep -n "onChangeText={setProficiencias}" app/\(tabs\)/index.tsx` retorna 0 linhas
    - `grep -n "Liste as proficiências do personagem" app/\(tabs\)/index.tsx` retorna 0 linhas (placeholder antigo eliminado)
    - `grep -n "value={c.habilidades}" app/\(tabs\)/index.tsx` retorna 1 linha (TextInput de Habilidades preservado)
    - `grep -n "textArea" app/\(tabs\)/index.tsx` retorna ≥ 2 (style ainda usado por habilidades + declaração no StyleSheet)
    - `npx tsc --noEmit` completa com ZERO erros em todo o projeto
    - Diff de `app/(tabs)/index.tsx`: +1 import, -1 SectionHeader proficiências, -8 linhas TextInput, +1 linha ProficienciasSection
  </acceptance_criteria>
  <done>Tela Ficha exibe ProficienciasSection no lugar do TextInput; setProficiencias agora aceita string[]; toda a cadeia de tipos compila sem erros.</done>
</task>

</tasks>

<verification>
**Manual smoke test após execução automatizada:**
1. `npx tsc --noEmit` — ZERO erros em todo o projeto
2. `npx expo start` → abrir no device → aba Ficha → rolar até "Proficiências"
3. Verificar 15 perícias listadas como rows colapsadas (com tag instância à esquerda, label centro, chevron ▼ direita)
4. Tocar em "Artes Marciais" → expande → 6 chips visíveis (Derrubar, Desarmar, Desviar, Fintar, Imobilizar, Aparar (reação lv.2))
5. Tocar em "Derrubar" → chip fica dourado
6. Trocar para outra aba e voltar → "Derrubar" continua selecionado
7. Fechar app (kill) → reabrir → "Derrubar" ainda selecionado
8. Tocar em "Derrubar" novamente → chip volta ao estilo muted
9. Para personagem novo: AsyncStorage clear → criar personagem → c.proficiencias começa como [] sem crash
10. Para personagem migrado: settar manualmente um AsyncStorage com `proficiencias: "texto antigo"` → reabrir app → personagem carrega, c.proficiencias = [], sem perda de outros campos
</verification>

<success_criteria>
- MESA-02 ✓: Chips togláveis substituem TextInput em proficiências; agrupados por perícia com cabeçalho exibindo instância (CORPO/MENTE/ESPÍRITO)
- MESA-03 ✓: Migration `proficiencias: string → []` executa sem perda do restante do personagem
- ROADMAP success criterion #2 satisfeito (chips togláveis agrupados por perícia atualizam c.proficiencias[] imediatamente)
- ROADMAP success criterion #3 satisfeito (personagens existentes migram automaticamente)
- Nenhum erro de TypeScript em todo o projeto
- Bloco HABILIDADES e demais campos da ficha permanecem intactos
</success_criteria>

<output>
Criar `.planning/phases/04-ferramentas-de-mesa/04-02-SUMMARY.md` ao concluir.
</output>
