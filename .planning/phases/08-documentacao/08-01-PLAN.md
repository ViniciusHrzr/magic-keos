---
phase: "08-documentacao"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/GAME_RULES.md
  - app/(tabs)/regras.tsx
  - data/proficiencias.ts
  - data/habilidades.ts
autonomous: true
requirements:
  - FIDE-19

must_haves:
  truths:
    - "GAME_RULES.md §5 e §7 sincronizados com conteúdo do docx v0.4"
    - "regras.tsx §5 importa habilidades de habilidades.ts (sem HabBlock hardcoded desatualizados)"
    - "proficiencias.ts e habilidades.ts (exceto Grimório) verificados contra docx v0.4 — todas discrepâncias corrigidas"
  artifacts:
    - path: ".planning/GAME_RULES.md"
      provides: "§5 e §7 sincronizados com docx v0.4"
      contains: "Selo de Encantamento | Selo de Invocação | metade do deslocamento | 1d4 · 1d6 | bastões | alvos adjacentes | ao morrer pode ser reativada | sofrer dano.*falhar | alvo de ataque"
    - path: "app/(tabs)/regras.tsx"
      provides: "§5 Habilidades dinâmico — importado de habilidades.ts"
      contains: "import { habilidades } from '@/data/habilidades'"
---

<objective>
Finalizar fidelidade ao docx v0.4 em três frentes:
1. Auditar GAME_RULES.md com python-docx e corrigir todas as discrepâncias (§5 + §7 + qualquer outra)
2. Refatorar regras.tsx §5 para importar habilidades de habilidades.ts em vez de HabBlocks hardcoded desatualizados
3. Revisar proficiencias.ts e habilidades.ts (exceto Grimório) contra docx v0.4 — corrigir qualquer discrepância restante

Purpose: Após as correções das Phases 5–7 nos arquivos de dados, (a) GAME_RULES.md ficou desatualizado, (b) regras.tsx §5 tem 6 efeitos stale vs habilidades.ts corrigido, e (c) pode haver discrepâncias menores não capturadas nas phases anteriores. Esta phase fecha os três gaps de uma vez.
</objective>

<execution_context>
@C:/Users/edysano/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/edysano/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/GAME_RULES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditar GAME_RULES.md via python-docx e corrigir discrepâncias</name>
  <files>.planning/GAME_RULES.md</files>

  <read_first>
    - .planning/GAME_RULES.md — completo (focar §5 Proficiências e §7 Habilidades)
    - data/proficiencias.ts — valores corretos para comparação
    - data/habilidades.ts — valores corretos para comparação
  </read_first>

  <action>
**Passo 1 — Extrair conteúdo do DOCX:**

Executar script Python para ler `magic keos/Magic no Universo Kéos v.0.4.docx`:

```python
from docx import Document

doc = Document("magic keos/Magic no Universo Kéos v.0.4.docx")
for i, para in enumerate(doc.paragraphs):
    text = para.text.strip()
    if text:
        print(f"{i:04d}: [{para.style.name}] {text}")
```

Para tabelas (proficiências e habilidades geralmente estão em tabelas):

```python
from docx import Document

doc = Document("magic keos/Magic no Universo Kéos v.0.4.docx")
for ti, table in enumerate(doc.tables):
    print(f"\n=== TABLE {ti} ===")
    for row in table.rows:
        cells = [c.text.strip() for c in row.cells]
        print(" | ".join(cells))
```

**Passo 2 — Comparar e corrigir:**

Comparar seções §5 e §7 do GAME_RULES.md contra o output do DOCX.

Known fixes necessários (confirmar strings exatas antes de editar via grep):

**§5 Investigação — prefixo "Selo de":**
old: `Selo de Feitiço, Encantamento, Invocação`
new: `Selo de Feitiço, Selo de Encantamento, Selo de Invocação`

**§7 Alcance — efeito de sucesso:**
old: `quando alvo se move`
new: `quando alvo avança; sucesso = metade do deslocamento`

**§7 Destreza — dados reais + bastões:**
old: `dano desarmado sobe (dW I–V)`
new: `dano desarmado e bastões sobe (1d4 · 1d6 · 1d8 · 1d10 · 1d12)`

**§7 Golpe Duplo — alvos + restrição:**
old: `dois ataques consecutivos` (sozinho no fim da célula)
new: `dois ataques consecutivos; até dois alvos adjacentes; não se aplica a armas de duas mãos`

**§7 Modelagem — reativação:**
old: `ressurge na cena`
new: `ao morrer pode ser reativada na mesma cena`

**§7 Fúria — triggers:**
old: `Enfurecer [1]: +1 ação padrão`
new: `Enfurecer [1]: aciona ao sofrer dano, falhar em combate ou presenciar aliado cair; +1 ação padrão`

**§7 Toque Mortífero — trigger:**
old: `Abater [1]: testa VON[INT]`
new: `Abater [1]: aciona quando for alvo de ataque; testa VON[INT]`

Se o DOCX revelar discrepâncias adicionais não listadas acima, corrigir também.

Usar Edit (old_string → new_string). NUNCA usar Write. NÃO tocar linhas não afetadas.
  </action>

  <verify>
    <automated>grep -n "Selo de Encantamento" .planning/GAME_RULES.md</automated>
    <automated>grep -n "Selo de Invocação" .planning/GAME_RULES.md</automated>
    <automated>grep -n "metade do deslocamento" .planning/GAME_RULES.md</automated>
    <automated>grep -n "1d4.*1d6.*1d8" .planning/GAME_RULES.md</automated>
    <automated>grep -n "bastões" .planning/GAME_RULES.md</automated>
    <automated>grep -n "alvos adjacentes" .planning/GAME_RULES.md</automated>
    <automated>grep -n "ao morrer pode ser reativada" .planning/GAME_RULES.md</automated>
    <automated>grep -n "sofrer dano" .planning/GAME_RULES.md</automated>
    <automated>grep -n "alvo de ataque" .planning/GAME_RULES.md</automated>
    Strings antigas removidas:
    <automated>grep -n "dW I–V" .planning/GAME_RULES.md</automated>
    <automated>grep -n "quando alvo se move" .planning/GAME_RULES.md</automated>
    <automated>grep -n "ressurge na cena" .planning/GAME_RULES.md</automated>
  </verify>

  <acceptance_criteria>
    - §5 Investigação contém "Selo de Encantamento" e "Selo de Invocação" (com prefixo)
    - §7 Alcance contém "metade do deslocamento"
    - §7 Destreza contém "1d4 · 1d6 · 1d8 · 1d10 · 1d12" e "bastões"
    - §7 Golpe Duplo contém "alvos adjacentes" e "não se aplica a armas de duas mãos"
    - §7 Modelagem contém "ao morrer pode ser reativada na mesma cena"
    - §7 Fúria contém "sofrer dano" e "falhar em combate"
    - §7 Toque Mortífero contém "alvo de ataque"
    - Nenhuma das strings antigas (dW I–V, quando alvo se move, ressurge na cena) persiste
    - Todas as outras linhas de GAME_RULES.md inalteradas
  </acceptance_criteria>

  <done>GAME_RULES.md auditado via python-docx e sincronizado com docx v0.4. FIDE-19 (§5+§7) satisfeito.</done>
</task>

<task type="auto">
  <name>Task 2: Refatorar regras.tsx §5 — importar de habilidades.ts</name>
  <files>app/(tabs)/regras.tsx</files>

  <read_first>
    - app/(tabs)/regras.tsx — linha 14 (imports existentes), linhas 214–238 (§5 hardcoded)
    - data/habilidades.ts — campos: nome, instancia, prerequisito, custo, teste
  </read_first>

  <action>
**Passo 1 — Adicionar import:**

old_string: `import { proficiencias, periciaOrdem } from '@/data/proficiencias';`
new_string: `import { proficiencias, periciaOrdem } from '@/data/proficiencias';
import { habilidades } from '@/data/habilidades';`

**Passo 2 — Helper de custo (adicionar após os imports, antes da linha `// -----------`):**

old_string: `// ---------------------------------------------------------------------------
// Base table helpers`
new_string: `function habCusto(custo: string): string {
  return custo.replace(/ SAB.*$/, '').replace(/ \/ /g, '/');
}

// ---------------------------------------------------------------------------
// Base table helpers`

Exemplos de output de habCusto:
- "5 SAB" → "5" (renderizado: "5 SAB")
- "5 / 10 / 15 SAB (3 níveis)" → "5/10/15"
- "2 / 3 / 4 / 5 SAB (4 níveis)" → "2/3/4/5"
- "5 SAB + 1 (gr.1) / 3 (gr.2) / 6 (gr.3) por magia" → "5"
- "6 SAB" → "6"

**Passo 3 — Substituir §5 hardcoded pelo mapeamento dinâmico:**

old_string (bloco inteiro da section §5):
```
          {/* ── 5. HABILIDADES ── */}
          <Section num="5" title="Habilidades">
            <Note text="Adquiridas com Sabedoria. Requerem pré-requisitos. Concedem reações especiais ou bônus fixos." />
            <Sub text="Corporais" />
            <HabBlock nome="Alcance" prereq="REF(2); Pontaria(+5)" custo="5" efeito="Impedir [1]: ataque à distância quando alvo se move" />
            <HabBlock nome="Ameaçar" prereq="REF(2); Furtividade(+5)" custo="5" efeito="— (passiva): alvo engajado não pode reagir" />
            <HabBlock nome="Atropelar" prereq="FOR(3); AM(+5) ou Esg(+5)" custo="5/10/15" efeito="— (passiva): +1dX dano, distribuível em adjacentes" />
            <HabBlock nome="Destreza" prereq="FOR; AM(+2/+4/+6/+8)" custo="2/3/4/5" efeito="— (passiva): dano desarmado sobe (dW I–V)" />
            <HabBlock nome="Golpe Duplo" prereq="REF(3); AM/Esg/Pont(+5)" custo="5" efeito="— (passiva): dois ataques consecutivos" />
            <HabBlock nome="Iniciativa" prereq="REF(2); Esgrima(+5)" custo="5" efeito="Impugnar [1]: ataca antes do atacante" />
            <HabBlock nome="Ímpeto" prereq="REF(2); Atletismo(+5)" custo="5" efeito="— (passiva): age primeiro + Velocidade para ações padrão" />
            <HabBlock nome="Vigilância" prereq="REF(2); Perícia Corporal(+5)" custo="5" efeito="— (passiva): reações ilimitadas (custa 1 ação cada)" />
            <Sub text="Mentais" />
            <HabBlock nome="Fetiche" prereq="SEN(1)+CON(1); Criatividade(+5)" custo="5/10/15" efeito="Reforça mágicas de 1 domínio sem custo de mana (até 3 fetiches)" />
            <HabBlock nome="Grimório" prereq="RAZ(1)+CON(1); Investigação(+5)" custo="5 (+ 1/3/6 por magia)" efeito="Aprende mágicas fora do domínio autodidata" />
            <HabBlock nome="Mixologia" prereq="RAZ(1)+SEN(1); Alquimia(+5)" custo="5 (+ 1/3 por receita)" efeito="Cria poções mágicas personalizadas" />
            <HabBlock nome="Modelagem" prereq="RAZ(1)+CON(1); Mecânica(+5)" custo="5 (+ 1–7 por modelo)" efeito="Cria criatura artefato que ressurge na cena" />
            <HabBlock nome="Travessia" prereq="SEN(1)+CON(1); Sobrevivência(+5)" custo="5/10/15" efeito="Vantagem em todos os testes no terreno escolhido" />
            <Sub text="Espirituais" />
            <HabBlock nome="Fúria" prereq="VON(3); Expressão(+6)" custo="6" efeito="Enfurecer [1]: +1 ação padrão de ataque/turno, sem reações" />
            <HabBlock nome="Regenerar" prereq="VON(3); Comunhão(+6)" custo="6" efeito="Regenerar [1]: testa VON[COM] vs. agressor; sucesso = cura total +Xd6" />
            <HabBlock nome="Salvaguarda" prereq="INT(3); Diplomacia(+6)" custo="6" efeito="Resguardar [1]: testa INT[DIP] vs. mago; sucesso = imune à mágica" />
            <HabBlock nome="Toque Mortífero" prereq="VON(3); Intimidação(+6)" custo="6" efeito="Abater [1]: testa VON[INT] vs. criatura ≤ Classe B; destrói" />
            <HabBlock nome="Vidência" prereq="INT(3); Lábia(+6)" custo="6" efeito="Antever [1]: testa INT[LAB] vs. IP Esp.; desvantagem na ação declarada" />
          </Section>
```

new_string:
```
          {/* ── 5. HABILIDADES ── */}
          <Section num="5" title="Habilidades">
            <Note text="Adquiridas com Sabedoria. Requerem pré-requisitos. Concedem reações especiais ou bônus fixos." />
            {(['corpo', 'mente', 'espirito'] as const).map(inst => {
              const labels: Record<string, string> = { corpo: 'Corporais', mente: 'Mentais', espirito: 'Espirituais' };
              return (
                <React.Fragment key={inst}>
                  <Sub text={labels[inst]} />
                  {habilidades
                    .filter(h => h.instancia === inst)
                    .map(h => (
                      <HabBlock
                        key={h.nome}
                        nome={h.nome}
                        prereq={h.prerequisito}
                        custo={habCusto(h.custo)}
                        efeito={h.teste}
                      />
                    ))}
                </React.Fragment>
              );
            })}
          </Section>
```

NÃO alterar HabBlock component nem nenhuma outra seção.
  </action>

  <verify>
    <automated>grep -n "import { habilidades }" "app/(tabs)/regras.tsx"</automated>
    <automated>grep -n "habCusto" "app/(tabs)/regras.tsx"</automated>
    <automated>grep -n "h\.teste" "app/(tabs)/regras.tsx"</automated>
    Verificar que hardcoded stale foram removidos:
    <automated>grep -n "dW I–V" "app/(tabs)/regras.tsx"</automated>
    <automated>grep -n "quando alvo se move" "app/(tabs)/regras.tsx"</automated>
    <automated>grep -n "ressurge na cena" "app/(tabs)/regras.tsx"</automated>
    Verificar que §5 ainda existe:
    <automated>grep -n "title=\"Habilidades\"" "app/(tabs)/regras.tsx"</automated>
  </verify>

  <acceptance_criteria>
    - regras.tsx importa habilidades de habilidades.ts
    - §5 usa mapeamento dinâmico (sem HabBlock hardcoded)
    - Nenhum dos 6 textos stale (dW I–V, quando alvo se move, ressurge na cena, dois ataques consecutivos$, Enfurecer [1]: \+1 ação, Abater \[1\]: testa) persiste em §5
    - TypeScript compila sem erros (npx tsc --noEmit)
    - Todas as outras seções (1–4, 6–20) inalteradas
  </acceptance_criteria>

  <done>regras.tsx §5 refatorado: habilidades importadas de habilidades.ts. Efeitos sempre sincronizados com os dados.</done>
</task>

<task type="auto">
  <name>Task 3: Revisar proficiencias.ts e habilidades.ts vs docx (exceto Grimório)</name>
  <files>data/proficiencias.ts, data/habilidades.ts</files>

  <read_first>
    - data/proficiencias.ts — todas as proficiências (Phases 5–6)
    - data/habilidades.ts — todas as habilidades (Phase 7)
    - Output dos scripts python das tasks anteriores
  </read_first>

  <action>
**Passo 1 — Extrair habilidades do DOCX:**

```python
from docx import Document

doc = Document("magic keos/Magic no Universo Kéos v.0.4.docx")
print("=== PARÁGRAFOS ===")
for i, para in enumerate(doc.paragraphs):
    text = para.text.strip()
    if text:
        print(f"{i:04d}: [{para.style.name}] {text}")

print("\n=== TABELAS ===")
for ti, table in enumerate(doc.tables):
    print(f"\n--- TABLE {ti} ({len(table.rows)} rows) ---")
    for ri, row in enumerate(table.rows):
        cells = [c.text.strip().replace('\n', ' ') for c in row.cells]
        print(f"  R{ri}: {' | '.join(cells)}")
```

**Passo 2 — Comparar sistematicamente:**

Para cada habilidade em habilidades.ts (exceto Grimório):
- Localizar no DOCX pelo nome
- Comparar: prerequisito, custo, descricao, teste
- Se discrepância: documentar e corrigir com Edit

Para proficiências prioritárias (as que mais mudaram nas Phases 5–6):
- Artes Marciais: Derrubar, Desarmar, Desviar, Fintar, Imobilizar, Aparar
- Atletismo: Prontidão, Fôlego, Investida, Disparar
- Esgrima: Armas Leves, Uma Mão, Duas Mãos, Especialização, Mestria
- Furtividade: Ataque Furtivo, Ataque Letal, Ataque Silencioso
- Pontaria: Especialização, Mestria, Mirar
- Investigação: Selo de Feitiço, Selo de Encantamento, Selo de Invocação, Leitura

**Passo 3 — Corrigir discrepâncias:**

Usar Edit (old_string → new_string) para cada correção encontrada.
NUNCA usar Write. NÃO tocar Grimório em habilidades.ts.

Se nenhuma discrepância for encontrada, documentar na SUMMARY que a auditoria confirmou fidelidade.
  </action>

  <verify>
    TypeScript ainda compila:
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - Auditoria cobriu habilidades.ts (exceto Grimório) e proficiências prioritárias vs docx v0.4
    - Todas as discrepâncias encontradas foram corrigidas
    - Grimório (habilidades.ts) NÃO foi tocado
    - TypeScript compila sem erros
  </acceptance_criteria>

  <done>Auditoria completa vs docx v0.4. proficiencias.ts e habilidades.ts (exceto Grimório) verificados e corrigidos. Phase 8 completa.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| T-08-01 | Tampering | GAME_RULES.md — linha errada editada | mitigate | Acceptance criteria com greps de strings únicas |
| T-08-02 | Tampering | regras.tsx — outra seção afetada | mitigate | grep verifica que só §5 mudou; outras seções grep-checked |
| T-08-03 | Spoofing | habCusto() retorna string errada | mitigate | Exemplos documentados na task; teste visual via tsc |
| T-08-04 | Tampering | Grimório editado por engano | mitigate | Acceptance criteria explícito: Grimório NÃO tocado |
| T-08-05 | Info Disclosure | python-docx falha ao abrir DOCX | mitigate | Lock file (~$) não é o DOCX real — usar `magic keos/Magic no Universo Kéos v.0.4.docx` |
</threat_model>

<verification>
Verificação final após as 3 tasks:

1. GAME_RULES.md §5: `grep -c "Selo de Encantamento\|Selo de Invocação" .planning/GAME_RULES.md` — 2 matches
2. GAME_RULES.md §7: `grep -c "metade do deslocamento\|1d4.*1d6\|alvos adjacentes\|ao morrer\|sofrer dano\|alvo de ataque" .planning/GAME_RULES.md` — 6 matches
3. regras.tsx import: `grep "import { habilidades }" "app/(tabs)/regras.tsx"` — 1 match
4. regras.tsx stale removido: `grep -c "dW I–V\|quando alvo se move\|ressurge na cena" "app/(tabs)/regras.tsx"` — 0 matches
5. TypeScript: `npx tsc --noEmit` — 0 errors
</verification>

<success_criteria>
- GAME_RULES.md: §5 + §7 sincronizados com docx v0.4 (7 fixes confirmados + qualquer adicional do DOCX)
- regras.tsx: §5 usa habilidades.ts como fonte única — 6 efeitos stale eliminados
- proficiencias.ts + habilidades.ts (exceto Grimório): auditados e corrigidos vs docx v0.4
- FIDE-19 satisfeito. Phase 8 completa.
</success_criteria>

<output>
Criar `.planning/phases/08-documentacao/08-01-SUMMARY.md` ao concluir a execução deste plano.
</output>
