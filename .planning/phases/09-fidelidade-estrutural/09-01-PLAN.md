---
phase: "09-fidelidade-estrutural"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(tabs)/regras.tsx
  - .planning/GAME_RULES.md
autonomous: true
requirements:
  - FIDE-20

must_haves:
  truths:
    - "Zero discrepâncias entre docx v0.4 e regras.tsx §1-3 e §6-20"
    - "Zero discrepâncias entre docx v0.4 e GAME_RULES.md todas as seções"
  artifacts:
    - path: "app/(tabs)/regras.tsx"
      provides: "Conteúdo hardcoded §1-3,§6-20 100% fiel ao docx v0.4"
    - path: ".planning/GAME_RULES.md"
      provides: "Todas as seções 100% fiéis ao docx v0.4"
    - path: ".planning/phases/09-fidelidade-estrutural/09-01-AUDIT.md"
      provides: "Relatório de auditoria: seção, discrepância, ação tomada"
---

<objective>
Auditoria completa docx v0.4 vs todo conteúdo hardcoded em regras.tsx (§1-3, §6-20) e GAME_RULES.md (todas as seções não auditadas anteriormente). Corrigir TODAS as discrepâncias encontradas.

Escopo: seções §1-3 e §6-20 de regras.tsx (§4 e §5 já são dinâmicos); GAME_RULES.md seções §1-4 e §6-fim (§5+§7 já foram corrigidos na Phase 8).

Output obrigatório: 09-01-AUDIT.md com tabela de discrepâncias encontradas e ações tomadas.
</objective>

<execution_context>
@C:/Users/edysano/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/edysano/.claude/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Extrair conteúdo completo do DOCX via python-docx</name>
  <files>.planning/phases/09-fidelidade-estrutural/09-01-AUDIT.md</files>

  <action>
Executar os dois scripts para extrair todo o conteúdo do docx:

```python
from docx import Document

doc = Document("magic keos/Magic no Universo Kéos v.0.4.docx")

print("=== PARÁGRAFOS ===")
for i, para in enumerate(doc.paragraphs):
    text = para.text.strip()
    if text:
        print(f"{i:04d}: [{para.style.name}] {text}")

print("\n=== TABELAS ===")
for ti, table in enumerate(doc.tables):
    print(f"\n--- TABLE {ti} ({len(table.rows)} linhas) ---")
    for ri, row in enumerate(table.rows):
        cells = [c.text.strip().replace('\n', ' ') for c in row.cells]
        if any(cells):
            print(f"  R{ri}: {' | '.join(cells)}")
```

Salvar output para referência durante Tasks 2 e 3.
  </action>
</task>

<task type="auto">
  <name>Task 2: Auditar e corrigir regras.tsx §1-3 e §6-20</name>
  <files>app/(tabs)/regras.tsx</files>

  <read_first>
    - app/(tabs)/regras.tsx — linhas 124–144 (§1), 146–172 (§2), 174–189 (§3), 241–263 (§6), 251–263 (§7), 265–281 (§8), 283–293 (§9), 295–323 (§10), 325–424 (§11), 426–466 (§12), 469–541 (§13), 543–582 (§14), 584–595 (§15), 597–634 (§16), 636–652 (§17), 654–682 (§18), 684–697 (§19), 699–719 (§20)
  </read_first>

  <action>
Para cada seção (§1-3, §6-20):
1. Localizar no output do docx (Task 1) a seção correspondente
2. Comparar CADA linha/célula hardcoded contra o docx
3. Registrar discrepâncias na tabela do 09-01-AUDIT.md
4. Corrigir usando Edit (old_string → new_string) — NUNCA Write
5. NÃO tocar §4 (Perícias) e §5 (Habilidades) — já dinâmicos

Criar `.planning/phases/09-fidelidade-estrutural/09-01-AUDIT.md` com estrutura:

```markdown
# Audit Report — Phase 9 Plan 01

| Seção | Arquivo | Campo | Valor atual | Valor correto (docx) | Status |
|-------|---------|-------|-------------|----------------------|--------|
| §1 | regras.tsx | ... | ... | ... | CORRIGIDO / OK |
```

Registrar TODAS as entradas auditadas, não só as discrepâncias.
  </action>

  <acceptance_criteria>
    - 09-01-AUDIT.md existe com tabela completa (todas seções §1-3, §6-20 auditadas)
    - Todas as linhas com status DISCREPÂNCIA foram corrigidas e remarcadas CORRIGIDO
    - TypeScript compila: npx tsc --noEmit → 0 erros
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Auditar e corrigir GAME_RULES.md seções não auditadas</name>
  <files>.planning/GAME_RULES.md</files>

  <read_first>
    - .planning/GAME_RULES.md — todas as seções (§1-4 e §6-fim; §5 e §7 já foram corrigidos na Phase 8)
  </read_first>

  <action>
Para cada seção de GAME_RULES.md não auditada na Phase 8:
1. Localizar no output do docx (Task 1) a seção correspondente
2. Comparar cada linha/tabela
3. Adicionar discrepâncias ao 09-01-AUDIT.md (coluna Arquivo = GAME_RULES.md)
4. Corrigir usando Edit — NUNCA Write

NÃO tocar §5 (Investigação) e §7 (habilidades) — já corrigidos na Phase 8 e verificados.
  </action>

  <acceptance_criteria>
    - GAME_RULES.md auditado completo vs docx
    - Todas as discrepâncias corrigidas
    - 09-01-AUDIT.md inclui seções de GAME_RULES.md
  </acceptance_criteria>

  <done>Auditoria completa concluída. FIDE-20 satisfeito. regras.tsx §1-3,§6-20 e GAME_RULES.md 100% fiéis ao docx v0.4.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` → 0 erros
2. 09-01-AUDIT.md existe e tem ≥ 18 seções auditadas
3. Todas as linhas de AUDIT.md com DISCREPÂNCIA → status CORRIGIDO
</verification>

<success_criteria>
- Zero discrepâncias não corrigidas entre docx v0.4 e regras.tsx §1-3,§6-20
- Zero discrepâncias não corrigidas entre docx v0.4 e GAME_RULES.md todas seções
- 09-01-AUDIT.md como evidência auditável
- FIDE-20 satisfeito
</success_criteria>

<output>
Criar `.planning/phases/09-fidelidade-estrutural/09-01-SUMMARY.md` ao concluir.
</output>
