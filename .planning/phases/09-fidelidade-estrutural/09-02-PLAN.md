---
phase: "09-fidelidade-estrutural"
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - data/regras/cores.ts
  - data/regras/personagens.ts
  - data/regras/balizadores.ts
  - data/regras/pontos.ts
  - data/regras/dados.ts
  - data/regras/magicas.ts
  - data/regras/dominios.ts
  - data/regras/equipamentos.ts
  - data/regras/alquimia.ts
  - data/regras/artefatos.ts
  - data/regras/criaturas.ts
  - data/regras/combate.ts
  - app/(tabs)/regras.tsx
autonomous: true
requirements:
  - FIDE-21

must_haves:
  truths:
    - "data/regras/ contém todos os dados das 18 seções hardcoded"
    - "regras.tsx importa de data/regras/ — zero hardcoded game data"
    - "Conteúdo idêntico ao pós-09-01 (nenhum dado alterado, só movido)"
  artifacts:
    - path: "data/regras/"
      provides: "Single source of truth para todo conteúdo de regras"
    - path: "app/(tabs)/regras.tsx"
      provides: "100% dinâmico — importa tudo de data/regras/"
---

<objective>
Mover todo conteúdo hardcoded de regras.tsx (§1-3, §6-20) para arquivos TypeScript em data/regras/.
regras.tsx vira 100% dinâmico. Nenhum dado de regra muda — só a localização.

Pré-requisito: 09-01 concluído (conteúdo já está correto vs docx).
</objective>

<execution_context>
@C:/Users/edysano/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/edysano/.claude/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Criar data/regras/ com arquivos TypeScript por seção</name>
  <files>data/regras/cores.ts, data/regras/personagens.ts, data/regras/balizadores.ts, data/regras/pontos.ts, data/regras/dados.ts, data/regras/magicas.ts, data/regras/dominios.ts, data/regras/equipamentos.ts, data/regras/alquimia.ts, data/regras/artefatos.ts, data/regras/criaturas.ts, data/regras/combate.ts</files>

  <read_first>
    - app/(tabs)/regras.tsx — completo (fonte dos dados a extrair)
  </read_first>

  <action>
Extrair o conteúdo hardcoded de cada seção de regras.tsx para arquivos TypeScript tipados.

**Estrutura de tipos genéricos (usar em todos os arquivos):**

```typescript
export interface R2Row { a: string; b: string }
export interface R3Row { a: string; b: string; c: string }
export interface TableSection {
  headers: string[]
  widths?: number[]
  rows: (R2Row | R3Row)[]
}
```

**Mapeamento seção → arquivo:**

| Seções | Arquivo |
|--------|---------|
| §1 As Cinco Cores | data/regras/cores.ts |
| §2-3 Personagens + Atributos | data/regras/personagens.ts |
| §6 Balizadores | data/regras/balizadores.ts |
| §7 Outros Pontos | data/regras/pontos.ts |
| §8-9 Mecânica de Dados + Testes | data/regras/dados.ts |
| §10 Sistema de Mágicas | data/regras/magicas.ts |
| §11 Domínios (45 domínios, 5 cores) | data/regras/dominios.ts |
| §12 Canalização e Mana | data/regras/canalização.ts |
| §13 Equipamentos | data/regras/equipamentos.ts |
| §14 Matérias-primas e Alquimia | data/regras/alquimia.ts |
| §15 Artefatos | data/regras/artefatos.ts |
| §16-17 Criaturas + Condições | data/regras/criaturas.ts |
| §18-20 Combate + Descanso + Evolução | data/regras/combate.ts |

Cada arquivo exporta constantes tipadas com os dados.
Exemplo (cores.ts):

```typescript
export interface CorRow { cor: string; valores: string; aliadas: string; inimigas: string }
export interface GuildaRow { combo: string; nome: string }

export const cores: CorRow[] = [
  { cor: 'Branco', valores: 'Ordem, lei, proteção, cura, comunidade', aliadas: 'Verde, Azul', inimigas: 'Verm, Preto' },
  // ...
]

export const guildas: GuildaRow[] = [
  { combo: 'B+V', nome: 'Selesnya — Comunidade' },
  // ...
]
```

Copiar EXATAMENTE os valores de regras.tsx — não inventar, não resumir.
  </action>

  <acceptance_criteria>
    - Todos os 13 arquivos em data/regras/ existem
    - Cada arquivo exporta os dados da(s) seção(ões) correspondente(s)
    - npx tsc --noEmit → 0 erros nos arquivos de dados
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Criar data/regras/index.ts (barrel export)</name>
  <files>data/regras/index.ts</files>

  <action>
Criar barrel export que re-exporta tudo de data/regras/:

```typescript
export * from './cores'
export * from './personagens'
export * from './balizadores'
export * from './pontos'
export * from './dados'
export * from './magicas'
export * from './dominios'
export * from './canalizacao'
export * from './equipamentos'
export * from './alquimia'
export * from './artefatos'
export * from './criaturas'
export * from './combate'
```
  </action>
</task>

<task type="auto">
  <name>Task 3: Refatorar regras.tsx para 100% dinâmico</name>
  <files>app/(tabs)/regras.tsx</files>

  <read_first>
    - app/(tabs)/regras.tsx — completo
    - data/regras/index.ts — exports disponíveis
  </read_first>

  <action>
1. Adicionar import de data/regras/:
   `import { cores, guildas, ... } from '@/data/regras'`

2. Para cada seção (§1-3, §6-20): substituir arrays/strings hardcoded por referências às constantes importadas.

3. Padrão para seções com R3 rows (ex: §1 Cores):
```tsx
{cores.map((row, i) => (
  <R3 key={i} a={row.cor} b={row.valores} c={row.aliadas + ' · vs ' + row.inimigas} />
))}
```

4. Seções com estrutura inline (ex: §2 tabela de pontos por instância): converter para dados tipados e mapear.

5. §11 Domínios (45 itens por cor): mapear sobre array de domínios.

Regra: ZERO strings de conteúdo de jogo hardcoded em regras.tsx após esta task.
Permitido hardcoded: structure JSX (Section num/title, TH cols, Sub text para labels de seção).

NÃO alterar HabBlock, ProfBlock, R2, R3, TH, Sub, Section, styles — só o conteúdo injetado.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
    Verificar que nenhum dado de jogo permanece hardcoded:
    <automated>grep -n "Selesnya\|Gruul\|Rakdos" "app/(tabs)/regras.tsx"</automated>
    <automated>grep -n "1d4\|1d6\|1d8\|1d10\|1d12" "app/(tabs)/regras.tsx"</automated>
    <automated>grep -n "Adaga\|Espada\|Montante" "app/(tabs)/regras.tsx"</automated>
  </verify>

  <acceptance_criteria>
    - regras.tsx não contém dados de jogo hardcoded — só imports e JSX estrutural
    - Grepping por dados de jogo específicos retorna 0 matches em regras.tsx
    - TypeScript compila: npx tsc --noEmit → 0 erros
    - App funciona: todas as 20 seções renderizam corretamente
  </acceptance_criteria>

  <done>regras.tsx 100% dinâmico. data/regras/ é single source of truth para todo conteúdo de regras. FIDE-21 satisfeito.</done>
</task>

</tasks>

<threat_model>
| ID | Categoria | Risco | Mitigação |
|----|-----------|-------|-----------|
| T-09-02-01 | Tampering | Dado alterado ao mover (copiar errado) | Conteúdo de 09-01 é a baseline — grep de valores específicos verifica integridade |
| T-09-02-02 | Tampering | §4 ou §5 (dinâmicos) afetados | Grep verifica que habilidades e proficiencias imports permanecem inalterados |
| T-09-02-03 | Availability | TypeScript type errors bloqueiam build | npx tsc --noEmit como gate obrigatório |
</threat_model>

<verification>
1. `npx tsc --noEmit` → 0 erros
2. `grep -rn "Selesnya\|Adaga\|1d4.*dano" app/(tabs)/regras.tsx` → 0 matches
3. `ls data/regras/` → 14 arquivos (13 + index.ts)
4. Cada seção de regras.tsx renderiza (verificação visual ou grep de imports)
</verification>

<success_criteria>
- data/regras/ com 13 arquivos de dados + index.ts
- regras.tsx 100% dinâmico — zero game data hardcoded
- TypeScript compila
- FIDE-21 satisfeito
</success_criteria>

<output>
Criar `.planning/phases/09-fidelidade-estrutural/09-02-SUMMARY.md` ao concluir.
</output>
