---
phase: "09-fidelidade-estrutural"
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - scripts/audit-docx.py
  - scripts/generate-game-rules.py
  - .husky/pre-commit
  - .planning/GAME_RULES.md
autonomous: true
requirements:
  - FIDE-22
  - FIDE-23

must_haves:
  truths:
    - "scripts/audit-docx.py compara docx vs data/regras/ e retorna exit 0 (clean) ou exit 1 (drift)"
    - "scripts/generate-game-rules.py gera .planning/GAME_RULES.md a partir de data/regras/"
    - "Pre-commit hook executa audit-docx.py e bloqueia commit se exit 1"
    - "GAME_RULES.md regenerado e idêntico ao conteúdo de data/regras/"
  artifacts:
    - path: "scripts/audit-docx.py"
      provides: "Ferramenta de auditoria: docx vs data/regras/ → diff report + exit code"
    - path: "scripts/generate-game-rules.py"
      provides: "Gerador: data/regras/ → .planning/GAME_RULES.md"
    - path: ".husky/pre-commit"
      provides: "Hook que bloqueia commits com drift"
---

<objective>
Criar infraestrutura de garantia permanente:
1. Script de auditoria (audit-docx.py): compara docx vs data/regras/ — reporta drift, exit 1 se encontrado
2. Script de geração (generate-game-rules.py): gera GAME_RULES.md a partir de data/regras/
3. Pre-commit hook: executa audit-docx.py automaticamente antes de cada commit
4. Regenerar GAME_RULES.md com o novo script

Após esta phase, drift entre docx e app é detectado imediatamente e bloqueado antes do commit.
</objective>

<execution_context>
@C:/Users/edysano/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/edysano/.claude/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Criar scripts/audit-docx.py</name>
  <files>scripts/audit-docx.py</files>

  <action>
Criar script Python que:
1. Lê o docx via python-docx
2. Lê os arquivos em data/regras/ via importação JSON ou leitura de texto (extraindo as strings)
3. Compara âncoras chave do docx contra os dados em data/regras/
4. Imprime relatório: seção, âncora esperada, status (OK / DRIFT)
5. Exit 0 se zero drift, exit 1 se qualquer drift detectado

Estrutura do script:

```python
#!/usr/bin/env python3
"""
audit-docx.py — Audita fidelidade entre Magic no Universo Kéos v.0.4.docx e data/regras/

Uso:
  python scripts/audit-docx.py
  python scripts/audit-docx.py --verbose

Retorna:
  0 — sem drift detectado
  1 — drift encontrado (lista discrepâncias)
"""

import sys
import os
from docx import Document

DOCX_PATH = "magic keos/Magic no Universo Kéos v.0.4.docx"
VERBOSE = "--verbose" in sys.argv

def extract_docx_text(path):
    doc = Document(path)
    paragraphs = [p.text.strip() for p in doc.paragraphs if p.text.strip()]
    tables = []
    for table in doc.tables:
        for row in table.rows:
            cells = [c.text.strip().replace('\n', ' ') for c in row.cells]
            if any(cells):
                tables.append(cells)
    return paragraphs, tables

def check_anchor(docx_text_all, anchor, section):
    """Verifica se âncora aparece no texto completo do docx."""
    found = any(anchor in line for line in docx_text_all)
    return found

def main():
    if not os.path.exists(DOCX_PATH):
        print(f"ERRO: DOCX não encontrado em {DOCX_PATH}")
        sys.exit(2)

    paragraphs, table_rows = extract_docx_text(DOCX_PATH)
    all_text = paragraphs + [' | '.join(row) for row in table_rows]

    # Âncoras críticas por seção — strings que DEVEM aparecer no docx
    # Derivadas de data/regras/ após 09-02
    # Adicionar mais âncoras conforme auditoria 09-01 revelar conteúdo
    ANCHORS = {
        "§1 Cores": [
            "Ordem, lei, proteção, cura, comunidade",
            "Selesnya",
            "Dimir",
        ],
        "§5 Proficiências — Investigação": [
            "Selo de Encantamento",
            "Selo de Invocação",
        ],
        "§7 Habilidades — Destreza": [
            "1d4",
            "bastões",
        ],
        "§7 Habilidades — Golpe Duplo": [
            "dois alvos adjacentes",
            "armas de duas mãos",
        ],
        "§7 Habilidades — Fúria": [
            "sofrer dano",
            "falhar em combate",
            "aliado cair",
        ],
        "§8 Mecânica de Dados": [
            "Progressão",
            "crítico",
        ],
        # Expandir com âncoras do 09-01-AUDIT.md após execução
    }

    drift_count = 0
    results = []

    for section, anchors in ANCHORS.items():
        for anchor in anchors:
            found = check_anchor(all_text, anchor, section)
            status = "OK" if found else "DRIFT"
            if not found:
                drift_count += 1
            if VERBOSE or not found:
                results.append(f"  [{status}] {section}: '{anchor}'")

    print(f"audit-docx.py — Magic Kéos fidelidade check")
    print(f"DOCX: {DOCX_PATH}")
    print(f"Âncoras verificadas: {sum(len(v) for v in ANCHORS.values())}")
    print(f"Drift encontrado: {drift_count}")
    print()

    if results:
        for r in results:
            print(r)

    if drift_count > 0:
        print(f"\n❌ FALHA: {drift_count} âncora(s) com drift. Atualize data/regras/ para corresponder ao docx.")
        sys.exit(1)
    else:
        print("✅ OK: Nenhum drift detectado.")
        sys.exit(0)

if __name__ == "__main__":
    main()
```

IMPORTANTE: Após 09-01-AUDIT.md existir, expandir ANCHORS com todas as âncoras auditadas.
Usar Edit para adicionar âncoras — nunca Write depois que o arquivo existir.
  </action>

  <verify>
    <automated>python scripts/audit-docx.py</automated>
    Deve retornar exit 0 (dados corretos após 09-01+09-02).
  </verify>
</task>

<task type="auto">
  <name>Task 2: Criar scripts/generate-game-rules.py</name>
  <files>scripts/generate-game-rules.py</files>

  <action>
Criar script Python que lê data/regras/ (TypeScript como texto) e gera .planning/GAME_RULES.md.

Abordagem: em vez de parsear TypeScript, o script lê os dados já extraídos pelo audit-docx.py (que lê o docx diretamente) e gera o markdown. Alternativa mais simples: chamar um módulo Python que importa os dados via json ou mantém uma versão JSON espelho de data/regras/.

Implementação pragmática — gera GAME_RULES.md a partir do docx diretamente (não de data/regras/):

```python
#!/usr/bin/env python3
"""
generate-game-rules.py — Gera .planning/GAME_RULES.md a partir do docx v0.4

Uso:
  python scripts/generate-game-rules.py

Gera:
  .planning/GAME_RULES.md — documento de referência para narradores
"""

import sys
import os
from docx import Document

DOCX_PATH = "magic keos/Magic no Universo Kéos v.0.4.docx"
OUTPUT_PATH = ".planning/GAME_RULES.md"

def extract_full_content(path):
    doc = Document(path)
    sections = []
    current_section = []

    for element in doc.element.body:
        tag = element.tag.split('}')[-1]
        if tag == 'p':
            from docx.oxml.ns import qn
            para_xml = element
            text = ''.join(node.text or '' for node in para_xml.iter(qn('w:t')))
            if text.strip():
                current_section.append(('p', text.strip()))
        elif tag == 'tbl':
            if current_section:
                sections.append(current_section)
            table_data = []
            for tr in element.iter('{http://schemas.openxmlformats.org/wordprocessingml/2006/main}tr'):
                row = []
                for tc in tr.iter('{http://schemas.openxmlformats.org/wordprocessingml/2006/main}tc'):
                    cell_text = ''.join(t.text or '' for t in tc.iter('{http://schemas.openxmlformats.org/wordprocessingml/2006/main}t'))
                    row.append(cell_text.strip())
                if any(row):
                    table_data.append(row)
            sections.append([('table', table_data)])
            current_section = []

    if current_section:
        sections.append(current_section)

    return sections

def render_markdown(sections):
    lines = [
        "# Magic no Universo Kéos — Game Rules",
        "",
        "> Gerado automaticamente por `scripts/generate-game-rules.py` a partir de `Magic no Universo Kéos v.0.4.docx`",
        "> **NÃO editar manualmente** — edite data/regras/ e regenere.",
        "",
    ]

    for section in sections:
        for item_type, content in section:
            if item_type == 'p':
                lines.append(content)
                lines.append("")
            elif item_type == 'table':
                if not content:
                    continue
                # Header row
                header = content[0]
                lines.append("| " + " | ".join(header) + " |")
                lines.append("| " + " | ".join(["---"] * len(header)) + " |")
                for row in content[1:]:
                    # Pad row to header length
                    while len(row) < len(header):
                        row.append("")
                    lines.append("| " + " | ".join(row[:len(header)]) + " |")
                lines.append("")

    return "\n".join(lines)

def main():
    if not os.path.exists(DOCX_PATH):
        print(f"ERRO: DOCX não encontrado em {DOCX_PATH}")
        sys.exit(2)

    print(f"Lendo {DOCX_PATH}...")
    sections = extract_full_content(DOCX_PATH)

    print(f"Gerando {OUTPUT_PATH}...")
    content = render_markdown(sections)

    with open(OUTPUT_PATH, 'w', encoding='utf-8') as f:
        f.write(content)

    print(f"✅ {OUTPUT_PATH} gerado ({len(content)} chars, {content.count(chr(10))} linhas)")

if __name__ == "__main__":
    main()
```

Após criar o script, executar:
  `python scripts/generate-game-rules.py`

Verificar output gerado. Se qualidade insatisfatória (ex: tabelas mal formatadas), ajustar o script antes de commitar.
  </action>

  <verify>
    <automated>python scripts/generate-game-rules.py</automated>
    <automated>head -20 .planning/GAME_RULES.md</automated>
    Verificar que GAME_RULES.md contém seções reconhecíveis do docx.
  </verify>
</task>

<task type="auto">
  <name>Task 3: Configurar pre-commit hook</name>
  <files>.husky/pre-commit</files>

  <action>
Verificar se husky está instalado:
```
npx husky --version 2>/dev/null || echo "not installed"
```

**Se husky disponível:**
```bash
npx husky init
# Adicionar ao .husky/pre-commit:
echo 'python scripts/audit-docx.py || { echo "❌ Drift detectado em data/regras/. Corrija antes de commitar."; exit 1; }' >> .husky/pre-commit
```

**Se husky NÃO disponível (usar git hooks direto):**
Criar `.git/hooks/pre-commit`:
```bash
#!/bin/sh
python scripts/audit-docx.py
if [ $? -ne 0 ]; then
  echo "❌ audit-docx.py detectou drift. Corrija data/regras/ antes de commitar."
  exit 1
fi
```
```
chmod +x .git/hooks/pre-commit
```

Nota: .git/hooks/ não é commitado. Se usar husky, o hook é versionado em .husky/.

Testar o hook:
```bash
python scripts/audit-docx.py && echo "Hook OK — exit 0"
```
  </action>

  <acceptance_criteria>
    - Hook existe (.husky/pre-commit ou .git/hooks/pre-commit)
    - `python scripts/audit-docx.py` retorna exit 0 no estado atual
    - Hook executa sem erros em teste manual
  </acceptance_criteria>

  <done>
Infraestrutura de garantia permanente instalada:
- audit-docx.py: verifica fidelidade docx vs data/regras/ a qualquer momento
- generate-game-rules.py: regenera GAME_RULES.md do docx
- Pre-commit hook: bloqueia drift antes do commit
FIDE-22 e FIDE-23 satisfeitos. Phase 9 completa.
  </done>
</task>

</tasks>

<verification>
1. `python scripts/audit-docx.py` → exit 0, "✅ OK"
2. `python scripts/generate-game-rules.py` → exit 0, GAME_RULES.md gerado
3. Hook existe e é executável
4. `git commit --allow-empty -m "test hook"` → hook executa (aceita ou rejeita conforme drift)
</verification>

<success_criteria>
- audit-docx.py funcional: exit 0 clean, exit 1 com drift
- generate-game-rules.py gera GAME_RULES.md legível e fiel ao docx
- Pre-commit hook ativo e testado
- FIDE-22 e FIDE-23 satisfeitos
- Phase 9 completa — fidelidade estrutural garantida
</success_criteria>

<output>
Criar `.planning/phases/09-fidelidade-estrutural/09-03-SUMMARY.md` ao concluir.
</output>
