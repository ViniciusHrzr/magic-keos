---
phase: 10-estrutura-tipografia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(tabs)/regras.tsx
autonomous: true
requirements:
  - REG-01
  - REG-02
  - REG-06
  - REG-07
  - REG-08
  - REG-09

must_haves:
  truths:
    - "Cada seção aparece como um card destacado sobre o fundo escuro (RPG.bg visível nas laterais e entre cards como gutter)"
    - "O header de seção fechada tem fundo mais escuro (RPG.headerBg) diferente do header aberto (RPG.surface)"
    - "O header aberto tem um divisor dourado fino separando-o do body"
    - "Título de seção (sectionTitle) é legível a ≥16px bold gold"
    - "Sub-seções (subTitle) aparecem em 12px goldLight semibold uppercase com espaçamento de letras"
    - "Texto de corpo tem lineHeight 18 — leitura confortável"
    - "Labels meta (profReq, habPrereq) são uppercase gold sem itálico"
  artifacts:
    - path: "app/(tabs)/regras.tsx"
      provides: "Tela de Regras completa com hierarquia visual e tipografia redesenhadas"
      contains: "sectionHeaderOpen"
  key_links:
    - from: "Section component (linha 122)"
      to: "styles.sectionHeaderOpen"
      via: "style={[styles.sectionHeader, open && styles.sectionHeaderOpen]}"
      pattern: "sectionHeaderOpen"
    - from: "sectionWrap"
      to: "RPG.surface / RPG.goldDim"
      via: "backgroundColor + borderTopColor"
      pattern: "backgroundColor: RPG.surface"
---

<objective>
Redesenhar a hierarquia visual e o sistema tipográfico da tela de Regras (app/(tabs)/regras.tsx).

Purpose: Transformar a tela de Regras de texto plano em uma interface com cards separados visualmente, headers com estado aberto/fechado, e tipografia hierarquizada e escaneável — alinhada à vibe dark/gold/premium da ficha existente.

Output: app/(tabs)/regras.tsx com todos os styles D-01 a D-11 aplicados, sectionHeaderOpen novo style, Section JSX com conditional style array, e TypeScript válido.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-estrutura-tipografia/10-CONTEXT.md

<interfaces>
<!-- Tokens exatos de constants/theme.ts — não inferir valores, usar esses. -->
RPG.bg          = '#0a0806'      // fundo do ScrollView — aparece como gutter entre cards
RPG.surface     = '#181210'      // fundo do card (sectionWrap) e body expandido
RPG.headerBg    = '#0d0b08'      // header fechado (mais escuro que surface)
RPG.gold        = '#c9a84c'      // gold principal — sectionTitle, profReq/habPrereq após D-11
RPG.goldLight   = '#e8c96a'      // goldLight — subTitle após D-08
RPG.goldDim     = '#7a6020'      // goldDim — divisor borderColor, sectionNum
RPG.border      = '#3d2e18'      // border padrão (não usar em sectionWrap — substituído por goldDim)
RPG.textMuted   = '#a89070'      // texto de corpo (profDesc, habEfeito)
RPG.azulLight   = '#2878e0'      // profTeste — NÃO TOCAR (D-10)

<!-- Estrutura atual do Section component (linha 122-134 de regras.tsx): -->
function Section({ num, title, children }) {
  const [open, setOpen] = useState(false);
  return (
    <View style={styles.sectionWrap}>
      <TouchableOpacity style={styles.sectionHeader} onPress={() => setOpen(o => !o)} activeOpacity={0.7}>
        <Text style={styles.sectionNum}>{num}</Text>
        <Text style={styles.sectionTitle}>{title}</Text>
        <Text style={styles.chevron}>{open ? '▲' : '▼'}</Text>
      </TouchableOpacity>
      {open && <View style={styles.sectionBody}>{children}</View>}
    </View>
  );
}

<!-- Styles actuais relevantes (linha 570-800 de regras.tsx): -->
sectionWrap:   { borderBottomWidth: 1, borderBottomColor: RPG.border }
sectionHeader: { flexDirection: 'row', alignItems: 'center', backgroundColor: RPG.headerBg, paddingVertical: 10, paddingHorizontal: 14, gap: 8 }
sectionNum:    { color: RPG.goldDim, fontSize: 11, fontWeight: '700', minWidth: 20, textAlign: 'right' }
sectionTitle:  { flex: 1, color: RPG.gold, fontSize: 14, fontWeight: '700', letterSpacing: 0.5 }
subTitle:      { color: RPG.goldDim, fontSize: 10, fontWeight: '700', letterSpacing: 1, textTransform: 'uppercase', paddingTop: 10, paddingBottom: 2 }
sectionIntro:  { color: RPG.textMuted, fontSize: 11, fontStyle: 'italic', paddingVertical: 5, lineHeight: 16 }
profDesc:      { color: RPG.textMuted, fontSize: 11, lineHeight: 16, paddingTop: 2 }
profReq:       { color: RPG.goldDim, fontSize: 10, fontStyle: 'italic' }
habEfeito:     { color: RPG.text, fontSize: 11, lineHeight: 16, paddingTop: 2 }
habPrereq:     { color: RPG.goldDim, fontSize: 10, fontStyle: 'italic', paddingTop: 1 }
tableVal:      { color: RPG.textMuted, fontSize: 11, flex: 2 }   // sem lineHeight atualmente
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Card anatomy e conditional header styles (D-01 a D-07)</name>
  <files>app/(tabs)/regras.tsx</files>

  <read_first>
  - app/(tabs)/regras.tsx — leitura completa obrigatória antes de qualquer edição (especialmente linhas 122-134 do Section component e linhas 598-635 dos styles sectionWrap/sectionHeader/sectionNum/sectionTitle)
  - constants/theme.ts — confirmar valores exatos de RPG.surface, RPG.headerBg, RPG.goldDim, RPG.gold
  </read_first>

  <action>
  Fazer três grupos de mudanças em app/(tabs)/regras.tsx:

  GRUPO A — Section JSX (linha 126): mudar o style do TouchableOpacity de estático para array condicional.

  OLD: `style={styles.sectionHeader}`
  NEW: `style={[styles.sectionHeader, open && styles.sectionHeaderOpen]}`

  Esta é a única mudança JSX da task. Não alterar nenhum outro JSX.

  GRUPO B — Substituir sectionWrap no StyleSheet.create.

  OLD (linhas 599-601):
  ```
  sectionWrap: {
    borderBottomWidth: 1,
    borderBottomColor: RPG.border,
  },
  ```

  NEW:
  ```
  sectionWrap: {
    backgroundColor: RPG.surface,
    borderTopWidth: 2,
    borderTopColor: RPG.goldDim,
    marginHorizontal: 8,
    marginVertical: 6,
  },
  ```

  Remover `borderBottomWidth` e `borderBottomColor: RPG.border` completamente — não os manter como comentário. Adicionar as quatro novas propriedades per D-01.

  GRUPO C — Atualizar sectionHeader, sectionNum, sectionTitle e adicionar sectionHeaderOpen.

  sectionHeader (atualizar apenas fontSize/fontWeight dos filhos de numeração — o objeto sectionHeader em si não muda backgroundColor, isso fica estático em RPG.headerBg como base):
  Nenhuma mudança em sectionHeader style object além do que já está — backgroundColor: RPG.headerBg permanece como valor base.

  sectionNum — mudar fontSize de 11 para 12 (D-05):
  OLD: `fontSize: 11,`
  NEW: `fontSize: 12,`

  sectionTitle — mudar fontSize de 14 para 16 (D-04):
  OLD: `fontSize: 14,`
  NEW: `fontSize: 16,`

  Adicionar sectionHeaderOpen logo após o fechamento de sectionHeader (manter adjacência visual no código):
  ```
  sectionHeaderOpen: {
    backgroundColor: RPG.surface,
    borderBottomWidth: 1,
    borderBottomColor: RPG.goldDim,
  },
  ```

  Este novo style é adicionado DENTRO do bloco StyleSheet.create, após sectionHeader. TypeScript inferirá o tipo automaticamente — não é necessário declarar tipo separado.

  ATENÇÃO — o que NÃO mudar:
  - sectionBody: backgroundColor: RPG.surface permanece (D-02, sem mudança)
  - chevron style: não tocar
  - titleBar, title, titleSub: não tocar
  - Nenhum JSX além do TouchableOpacity style no Section
  - Nenhum arquivo data/regras/
  </action>

  <verify>
    <automated>
    # 1. sectionHeaderOpen existe no arquivo
    grep -n "sectionHeaderOpen" /c/Users/edysano/Desktop/magic-keos/app/\(tabs\)/regras.tsx

    # 2. Section JSX usa array condicional
    grep -n "open && styles.sectionHeaderOpen" /c/Users/edysano/Desktop/magic-keos/app/\(tabs\)/regras.tsx

    # 3. sectionTitle tem 16px
    grep -n "fontSize: 16" /c/Users/edysano/Desktop/magic-keos/app/\(tabs\)/regras.tsx

    # 4. sectionNum tem 12px (procurar na vizinhança de sectionNum)
    grep -A5 "sectionNum:" /c/Users/edysano/Desktop/magic-keos/app/\(tabs\)/regras.tsx | grep "fontSize: 12"

    # 5. sectionWrap tem marginHorizontal: 8
    grep -n "marginHorizontal: 8" /c/Users/edysano/Desktop/magic-keos/app/\(tabs\)/regras.tsx

    # 6. sectionWrap NÃO tem mais borderBottomWidth: 1 (o borderBottomWidth do sectionWrap foi removido — tableHeaderRow ainda tem borderBottomWidth: 1 então resultado pode ser > 0, mas sectionWrap especificamente não deve aparecer)
    grep -n "borderBottomColor: RPG.border" /c/Users/edysano/Desktop/magic-keos/app/\(tabs\)/regras.tsx

    # 7. TypeScript compila sem erros
    cd /c/Users/edysano/Desktop/magic-keos && npx tsc --noEmit 2>&1 | head -20
    </automated>
  </verify>

  <acceptance_criteria>
  - grep "sectionHeaderOpen" retorna pelo menos 2 linhas (a key no StyleSheet e o uso no JSX)
  - grep "open && styles.sectionHeaderOpen" retorna exatamente 1 linha
  - grep "fontSize: 16" retorna linha contendo sectionTitle area (linha ~620)
  - grep de sectionNum block mostra fontSize: 12
  - grep "marginHorizontal: 8" retorna 1 linha (sectionWrap)
  - grep "borderBottomColor: RPG.border" retorna 0 linhas (a bordinha antiga do sectionWrap foi removida)
  - npx tsc --noEmit sai com código 0 (zero erros TypeScript)
  </acceptance_criteria>

  <done>
  Section component usa conditional style array para sectionHeader. sectionWrap é um card com background RPG.surface, borderTop gold de 2px, margins. sectionNum é 12px, sectionTitle é 16px. sectionHeaderOpen existe no StyleSheet com backgroundColor RPG.surface e borderBottom goldDim. TypeScript compila limpo.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Sistema tipográfico — Sub, lineHeight universal, meta labels (D-08 a D-11)</name>
  <files>app/(tabs)/regras.tsx</files>

  <read_first>
  - app/(tabs)/regras.tsx — ler o StyleSheet.create completo novamente após Task 1 (arquivo foi modificado). Focar nas linhas dos styles: subTitle, sectionIntro, profDesc, profReq, profTeste, habEfeito, habPrereq, tableVal.
  - constants/theme.ts — confirmar RPG.goldLight (#e8c96a), RPG.gold (#c9a84c), RPG.goldDim (#7a6020), RPG.azulLight (#2878e0)
  </read_first>

  <action>
  Fazer quatro grupos de mudanças no StyleSheet.create de app/(tabs)/regras.tsx:

  GRUPO A — subTitle (D-08): Sub-seções de 10px bold goldDim → 12px semibold goldLight, manter uppercase + textTransform, adicionar letterSpacing 0.8.

  OLD:
  ```
  subTitle: {
    color: RPG.goldDim,
    fontSize: 10,
    fontWeight: '700',
    letterSpacing: 1,
    textTransform: 'uppercase',
    paddingTop: 10,
    paddingBottom: 2,
  },
  ```

  NEW:
  ```
  subTitle: {
    color: RPG.goldLight,
    fontSize: 12,
    fontWeight: '600',
    letterSpacing: 0.8,
    textTransform: 'uppercase',
    paddingTop: 10,
    paddingBottom: 2,
  },
  ```

  Mudanças: color goldDim→goldLight, fontSize 10→12, fontWeight '700'→'600', letterSpacing 1→0.8. paddingTop/paddingBottom permanecem.

  GRUPO B — lineHeight 16→18 em quatro styles (D-09). Cada um muda apenas o valor de lineHeight:

  sectionIntro: `lineHeight: 16,` → `lineHeight: 18,`
  profDesc:     `lineHeight: 16,` → `lineHeight: 18,`
  habEfeito:    `lineHeight: 16,` → `lineHeight: 18,`
  tableVal:     não tem lineHeight — ADICIONAR `lineHeight: 18,` ao objeto (inserir após `flex: 2,`)

  GRUPO C — profReq (D-11): remover italic, mudar goldDim→gold, adicionar uppercase + letterSpacing 0.5.

  OLD:
  ```
  profReq: {
    color: RPG.goldDim,
    fontSize: 10,
    fontStyle: 'italic',
  },
  ```

  NEW:
  ```
  profReq: {
    color: RPG.gold,
    fontSize: 10,
    fontWeight: '600',
    textTransform: 'uppercase',
    letterSpacing: 0.5,
  },
  ```

  Remover `fontStyle: 'italic'` completamente. Adicionar fontWeight '600', textTransform 'uppercase', letterSpacing 0.5. Mudar color para RPG.gold.

  GRUPO D — habPrereq (D-11 + discretion): mesmo tratamento que profReq.

  OLD:
  ```
  habPrereq: {
    color: RPG.goldDim,
    fontSize: 10,
    fontStyle: 'italic',
    paddingTop: 1,
  },
  ```

  NEW:
  ```
  habPrereq: {
    color: RPG.gold,
    fontSize: 10,
    fontWeight: '600',
    textTransform: 'uppercase',
    letterSpacing: 0.5,
    paddingTop: 1,
  },
  ```

  Remover `fontStyle: 'italic'`. Adicionar fontWeight '600', textTransform 'uppercase', letterSpacing 0.5. Mudar color goldDim→gold. Manter paddingTop: 1.

  ATENÇÃO — o que NÃO mudar:
  - profTeste: manter `color: RPG.azulLight` e `fontStyle: 'italic'` intactos (D-10 — cor semântica protegida)
  - habMagicasLabel: já está em conformidade com REG-09 (9px gold bold letterSpacing 0.6) — não tocar
  - Nenhum dado em data/regras/ — zero mudanças de conteúdo
  - Nenhum outro style não listado acima
  </action>

  <verify>
    <automated>
    # 1. subTitle tem fontSize 12 e color goldLight
    grep -A10 "subTitle:" /c/Users/edysano/Desktop/magic-keos/app/\(tabs\)/regras.tsx | grep "fontSize: 12"
    grep -A10 "subTitle:" /c/Users/edysano/Desktop/magic-keos/app/\(tabs\)/regras.tsx | grep "goldLight"

    # 2. Nenhum lineHeight: 16 restante no arquivo (os quatro foram trocados para 18)
    grep -c "lineHeight: 16" /c/Users/edysano/Desktop/magic-keos/app/\(tabs\)/regras.tsx

    # 3. Quatro ocorrências de lineHeight: 18
    grep -c "lineHeight: 18" /c/Users/edysano/Desktop/magic-keos/app/\(tabs\)/regras.tsx

    # 4. profReq não tem mais italic, tem gold e uppercase
    grep -A8 "profReq:" /c/Users/edysano/Desktop/magic-keos/app/\(tabs\)/regras.tsx | grep "italic"
    grep -A8 "profReq:" /c/Users/edysano/Desktop/magic-keos/app/\(tabs\)/regras.tsx | grep "uppercase"
    grep -A8 "profReq:" /c/Users/edysano/Desktop/magic-keos/app/\(tabs\)/regras.tsx | grep "RPG.gold,"

    # 5. habPrereq não tem mais italic, tem gold e uppercase
    grep -A8 "habPrereq:" /c/Users/edysano/Desktop/magic-keos/app/\(tabs\)/regras.tsx | grep "italic"
    grep -A8 "habPrereq:" /c/Users/edysano/Desktop/magic-keos/app/\(tabs\)/regras.tsx | grep "uppercase"

    # 6. profTeste mantém azulLight (não tocado)
    grep -A5 "profTeste:" /c/Users/edysano/Desktop/magic-keos/app/\(tabs\)/regras.tsx | grep "azulLight"

    # 7. TypeScript compila sem erros
    cd /c/Users/edysano/Desktop/magic-keos && npx tsc --noEmit 2>&1 | head -20
    </automated>
  </verify>

  <acceptance_criteria>
  - grep de subTitle block mostra fontSize: 12 e color: RPG.goldLight
  - grep -c "lineHeight: 16" retorna 0 (todos substituídos)
  - grep -c "lineHeight: 18" retorna 4 (sectionIntro, profDesc, habEfeito, tableVal)
  - grep de profReq block não retorna nenhuma linha com "italic"
  - grep de profReq block retorna linha com "uppercase"
  - grep de profReq block retorna linha com "RPG.gold," (não goldDim)
  - grep de habPrereq block não retorna nenhuma linha com "italic"
  - grep de profTeste block retorna linha com "azulLight" (azulLight preservado)
  - npx tsc --noEmit sai com código 0
  </acceptance_criteria>

  <done>
  Sistema tipográfico completo: Sub-seções em 12px goldLight semibold uppercase. Todos os textos de corpo têm lineHeight 18. profReq e habPrereq são uppercase gold 10px sem itálico. profTeste mantém azulLight. TypeScript compila limpo.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dados/regras/ → regras.tsx | Conteúdo estático importado — imutável nesta fase, risco zero |
| StyleSheet.create → RN renderer | Estilos aplicados pelo renderer nativo — sem input de usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | StyleSheet.create values | accept | Mudanças puramente visuais em valores de estilo; sem lógica de negócio, sem input de usuário, sem dados persistidos. Risco zero de exploração. |
| T-10-02 | Denial of Service | Section component open state | accept | useState local — sem risco; o open/closed state não persiste além da sessão de renderização. |
</threat_model>

<verification>
Após Tasks 1 e 2 completas, verificar visualmente no simulador/device:

1. Tela de Regras abre sem crash (ActivityIndicator desaparece, ScrollView aparece)
2. Cards de seção são visíveis com fundo RPG.surface e borda top gold — fundo escuro RPG.bg aparece nas laterais/entre cards como gutter
3. Tocar no header de uma seção: header muda de fundo escuro (fechado) para fundo igual ao body (aberto) + divisor dourado aparece abaixo do header
4. Sub-seções (Sub) aparecem visivelmente maiores e em cor mais clara (goldLight ao invés de goldDim)
5. Texto de corpo tem espaçamento entre linhas confortável (lineHeight 18)
6. Labels "Req:" em ProfBlock e "Req:" em HabBlock aparecem em gold uppercase sem itálico
7. "Teste: ..." em ProfBlock mantém cor azul (azulLight)
8. npx tsc --noEmit retorna 0 erros
</verification>

<success_criteria>
- sectionWrap é um card com backgroundColor RPG.surface, borderTop 2px goldDim, marginHorizontal 8, marginVertical 6 (REG-01)
- sectionHeader mostra sectionHeaderOpen style quando open=true (borderBottom goldDim + backgroundColor RPG.surface) (REG-02, REG-06)
- sectionTitle em 16px bold gold (REG-06)
- sectionNum em 12px goldDim (REG-06)
- subTitle em 12px goldLight fontWeight '600' uppercase letterSpacing 0.8 (REG-07)
- sectionIntro, profDesc, habEfeito, tableVal têm lineHeight: 18 (REG-08)
- profReq: color RPG.gold, uppercase, letterSpacing 0.5, sem italic (REG-09)
- habPrereq: color RPG.gold, uppercase, letterSpacing 0.5, sem italic (REG-09)
- profTeste: color RPG.azulLight preservado intacto (D-10)
- npx tsc --noEmit passa com 0 erros
- Nenhum arquivo em data/regras/ foi modificado
</success_criteria>

<output>
Criar `.planning/phases/10-estrutura-tipografia/10-01-SUMMARY.md` ao concluir, com os campos padrão do template summary.md.
</output>
