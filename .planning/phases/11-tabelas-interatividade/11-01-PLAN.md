---
phase: 11-tabelas-interatividade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(tabs)/regras.tsx
autonomous: true
requirements:
  - REG-03
  - REG-04
  - REG-05
  - REG-10
  - REG-11
  - REG-12

must_haves:
  truths:
    - "Toda tabela na tela de Regras exibe o cabeçalho com fundo distinto (RPG.surfaceAlt) e texto bold/gold"
    - "As linhas de dados de tabela têm separação visível por borda inferior (StyleSheet.hairlineWidth em RPG.border)"
    - "Colunas de tabela permanecem alinhadas de forma consistente entre todas as seções (R2/R3 e rows inline)"
    - "O chevron rotaciona suavemente 180° ao expandir/colapsar uma seção"
    - "O cabeçalho colapsável tem aparência visualmente distinta entre estado aberto e fechado (Phase 10 sectionHeaderOpen + transição animada por LayoutAnimation)"
    - "Tocar no cabeçalho de uma seção dispara feedback háptico leve via expo-haptics"
  artifacts:
    - path: "app/(tabs)/regras.tsx"
      provides: "Section component com animação + haptic; tableHeaderRow e tableRow restilizados"
      contains: "Haptics.impactAsync, Animated.Text, LayoutAnimation.configureNext, RPG.surfaceAlt em tableHeaderRow, StyleSheet.hairlineWidth em tableRow"
  key_links:
    - from: "app/(tabs)/regras.tsx (Section.onPress)"
      to: "expo-haptics"
      via: "Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light)"
      pattern: "Haptics\\.impactAsync"
    - from: "app/(tabs)/regras.tsx (Section toggle)"
      to: "react-native Animated + LayoutAnimation"
      via: "Animated.timing(rotation) + LayoutAnimation.configureNext"
      pattern: "LayoutAnimation\\.configureNext"
    - from: "styles.tableHeaderRow"
      to: "constants/theme.ts (RPG.surfaceAlt)"
      via: "backgroundColor token"
      pattern: "tableHeaderRow:[\\s\\S]*?backgroundColor: RPG\\.surfaceAlt"
---

<objective>
Tornar as tabelas da tela de Regras visualmente coerentes com a vibe RPG dark/gold (header distinto, linhas separadas, colunas alinhadas) e dar feedback de interatividade nas seções colapsáveis (chevron animado, transição suave do header, haptic leve no toque).

Purpose: Fechar os 6 requirements restantes da v1.2 (REG-03, REG-04, REG-05, REG-10, REG-11, REG-12) sem novas dependências, em um único arquivo. Após esta phase, a tela de Regras está visualmente alinhada com a vibe da Ficha e responde a toque com feedback claro.

Output: `app/(tabs)/regras.tsx` modificado — Section component com animação + haptic, e StyleSheet refatorado para `tableHeaderRow` e `tableRow`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-tabelas-interatividade/RESEARCH.md
@.planning/phases/10-estrutura-tipografia/10-CONTEXT.md
@app/(tabs)/regras.tsx
@constants/theme.ts

<interfaces>
<!-- Estrutura atual EXATA dos componentes e styles que serão modificados. Extraído por leitura direta. -->
<!-- Executor: NÃO releia o arquivo para descobrir contratos — use os trechos abaixo como ground truth. -->

Imports atuais (regras.tsx linhas 1-9):
```typescript
import React, { useState } from 'react';
import {
  ScrollView,
  View,
  Text,
  StyleSheet,
  ActivityIndicator,
  TouchableOpacity,
} from 'react-native';
```

Section component atual (linhas 122-134):
```typescript
function Section({ num, title, children }: { num: string; title: string; children: React.ReactNode }) {
  const [open, setOpen] = useState(false);
  return (
    <View style={styles.sectionWrap}>
      <TouchableOpacity style={[styles.sectionHeader, open && styles.sectionHeaderOpen]} onPress={() => setOpen(o => !o)} activeOpacity={0.7}>
        <Text style={styles.sectionNum}>{num}</Text>
        <Text style={styles.sectionTitle}>{title}</Text>
        <Text style={styles.chevron}>{open ? '▲' : '▼'}</Text>
      </TouchableOpacity>
      {open && <View style={styles.sectionBody}>{children}</View>}
    </View>
  );
}
```

Styles atuais relevantes (linhas 605-680):
```typescript
sectionHeader: {
  flexDirection: 'row',
  alignItems: 'center',
  backgroundColor: RPG.headerBg,
  paddingVertical: 10,
  paddingHorizontal: 14,
  gap: 8,
},
sectionHeaderOpen: {
  backgroundColor: RPG.surface,
  borderBottomWidth: 1,
  borderBottomColor: RPG.goldDim,
},
chevron: {
  color: RPG.goldDim,
  fontSize: 11,
},
tableHeaderRow: {
  borderBottomWidth: 1,
  borderBottomColor: RPG.goldDim,
  paddingBottom: 4,
  marginBottom: 2,
},
tableRow: {
  flexDirection: 'row',
  paddingVertical: 4,
  gap: 6,
  alignItems: 'flex-start',
},
```

Tokens RPG.* relevantes (verificados em constants/theme.ts via RESEARCH.md):
```
RPG.surfaceAlt = '#221a12'    // novo background do tableHeaderRow
RPG.border     = '#3d2e18'    // borderBottomColor das data rows
RPG.goldDim    = '#7a6020'    // já em uso no tableHeaderRow
RPG.surface    = '#181210'    // header aberto (Phase 10) e body
RPG.headerBg   = '#0d0b08'    // header fechado
```

Padrão de uso do expo-haptics (components/haptic-tab.tsx — VERIFIED):
```typescript
import * as Haptics from 'expo-haptics';
Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light);
```
(Sem guard de Platform.OS — segue o padrão do projeto.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Restilizar tabelas (REG-03, REG-04, REG-05)</name>
  <files>app/(tabs)/regras.tsx</files>
  <read_first>
    - app/(tabs)/regras.tsx (estrutura completa, especialmente o bloco `const styles = StyleSheet.create({...})` no final do arquivo)
    - constants/theme.ts (confirmar valores exatos de RPG.surfaceAlt, RPG.border, RPG.goldDim)
  </read_first>
  <behavior>
    Comportamento esperado após a mudança:
    - Test visual 1 (REG-03): `tableHeaderRow` tem `backgroundColor: RPG.surfaceAlt`. Header de tabela aparece visivelmente mais escuro/destacado que as data rows abaixo, com texto em gold bold (já existente em `tableKey`).
    - Test visual 2 (REG-04): `tableRow` tem `borderBottomWidth: StyleSheet.hairlineWidth` e `borderBottomColor: RPG.border`. Cada linha de dados é separada por uma linha sutil cor `#3d2e18`.
    - Test visual 3 (REG-05): Colunas alinham nas seções com `widths` prop e nas seções com rows inline (seções 2, 11, 13, 16) — sem desalinhamento entre header e data rows. (Phase 10 já garantiu isso por design; verificação puramente visual.)
    - Test estático: TypeScript compila clean (`npx tsc --noEmit` exit 0).
    - Test estático: ESLint passa (`npx expo lint` sem erros novos introduzidos por esta task).
    - Test estático: Zero hex hardcoded em valores novos — apenas `RPG.*` e `StyleSheet.hairlineWidth`.
  </behavior>
  <action>
    Modificar APENAS o bloco `const styles = StyleSheet.create({...})` em `app/(tabs)/regras.tsx`. Sem mudanças no JSX de `TH`, `R2`, `R3`, ou em qualquer Section.

    Mudança 1 — `tableHeaderRow` (REG-03):
    Adicionar `backgroundColor: RPG.surfaceAlt` como primeira propriedade do objeto, mantendo as demais propriedades existentes (`borderBottomWidth: 1`, `borderBottomColor: RPG.goldDim`, `paddingBottom: 4`, `marginBottom: 2`).

    Mudança 2 — `tableRow` (REG-04):
    Adicionar `borderBottomWidth: StyleSheet.hairlineWidth` e `borderBottomColor: RPG.border` ao final do objeto, mantendo todas as propriedades existentes (`flexDirection: 'row'`, `paddingVertical: 4`, `gap: 6`, `alignItems: 'flex-start'`).

    Mudança 3 — REG-05 (verificação apenas):
    Não modificar nada de código. Confirmar visualmente que os `widths` do `TH` batem com os `flex` dos rows manuais nas seções 2, 11, 13, 16 (RESEARCH.md confirma que já estão alinhados). Documentar resultado em SUMMARY como "REG-05 verified, no code change required".

    Restrições:
    - Apenas StyleSheet — sem inline styles novos.
    - Todas as cores via `RPG.*` (sem hex hardcoded).
    - Não introduzir hex transparency (`+ '55'` etc.) — usar tokens sólidos.
    - Não tocar em `TH`/`R2`/`R3`/`Section` ou qualquer JSX.
    - Não tocar em outros styles (ex: `tableKey`, `tableVal`, `colFirst`, etc.) — REG-03 satisfaz o texto bold/gold via `tableKey` já existente.

    Commit message: `feat(11-01): estilo RPG nas tabelas — header surfaceAlt + data rows hairline border (REG-03,REG-04,REG-05)`.
  </action>
  <verify>
    <automated>
      cd C:/Users/edysano/Desktop/magic-keos &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -E "(error|Error)" | wc -l
      # Deve retornar 0

      # REG-03: tableHeaderRow tem backgroundColor RPG.surfaceAlt
      grep -E "tableHeaderRow:" app/\(tabs\)/regras.tsx -A 6 | grep -c "backgroundColor: RPG.surfaceAlt"
      # Deve retornar 1

      # REG-04: tableRow tem borderBottomWidth hairline e borderBottomColor RPG.border
      grep -E "^  tableRow:" app/\(tabs\)/regras.tsx -A 7 | grep -c "borderBottomWidth: StyleSheet.hairlineWidth"
      # Deve retornar 1
      grep -E "^  tableRow:" app/\(tabs\)/regras.tsx -A 7 | grep -c "borderBottomColor: RPG.border"
      # Deve retornar 1

      # Nenhum hex hardcoded novo (escape #221a12, #3d2e18 etc.)
      grep -vE "^\s*//" app/\(tabs\)/regras.tsx | grep -cE "'#221a12'|'#3d2e18'"
      # Deve retornar 0 (cores via tokens RPG.*)
    </automated>
  </verify>
  <done>
    - `tableHeaderRow.backgroundColor` é `RPG.surfaceAlt`
    - `tableRow` tem `borderBottomWidth: StyleSheet.hairlineWidth` + `borderBottomColor: RPG.border`
    - `TH`, `R2`, `R3`, `Section` e demais componentes JSX inalterados
    - `npx tsc --noEmit` retorna 0 erros
    - Commit criado com mensagem `feat(11-01): estilo RPG nas tabelas — header surfaceAlt + data rows hairline border (REG-03,REG-04,REG-05)`
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Animação + Haptic no Section colapsável (REG-10, REG-11, REG-12)</name>
  <files>app/(tabs)/regras.tsx</files>
  <read_first>
    - app/(tabs)/regras.tsx (Section component linhas 122-134; imports linhas 1-32)
    - components/haptic-tab.tsx (padrão exato de chamada do Haptics.impactAsync)
  </read_first>
  <behavior>
    Comportamento esperado após a mudança:
    - Test visual 1 (REG-10): Ao tocar no header de uma Section, o chevron `▼` rotaciona suavemente 180° (vira `▲` visualmente) em ~200ms via `Animated.timing` com `useNativeDriver: true`.
    - Test visual 2 (REG-10): A expansão/contração do `sectionBody` é animada via `LayoutAnimation.configureNext(LayoutAnimation.Presets.easeInEaseOut)` — não é troca instantânea.
    - Test visual 3 (REG-11): A transição entre `sectionHeader` fechado (backgroundColor `RPG.headerBg`) e `sectionHeaderOpen` (backgroundColor `RPG.surface` + borderBottom) é perceptível durante a animação. (Visual estático já estabelecido pela Phase 10.)
    - Test visual 4 (REG-12): Em device físico iOS, tocar no header produz haptic leve (impact Light). Em Android com motor háptico, vibração discreta. Em simulador, no-op silencioso (degradação aceitável).
    - Test estático: TypeScript compila clean (`npx tsc --noEmit` exit 0).
    - Test estático: ESLint passa.
    - Test estático: Imports adicionados: `useRef` de React; `Animated`, `LayoutAnimation` (e opcionalmente `Platform`, `UIManager` se necessário — ver fallback abaixo) de `react-native`; `* as Haptics` de `expo-haptics`.
  </behavior>
  <action>
    Refatorar APENAS o componente `Section` (linhas 122-134) e a lista de imports (linhas 1-9, 14-15) em `app/(tabs)/regras.tsx`. Não tocar em mais nada.

    Mudança 1 — Imports (topo do arquivo):
    - Expandir o import de `react` para incluir `useRef`: `import React, { useState, useRef } from 'react';`.
    - Expandir o import de `react-native` para incluir `Animated` e `LayoutAnimation` no destructuring existente (manter `ScrollView`, `View`, `Text`, `StyleSheet`, `ActivityIndicator`, `TouchableOpacity` na mesma cláusula).
    - Adicionar nova linha de import abaixo dos imports `react-native`: `import * as Haptics from 'expo-haptics';`.

    Mudança 2 — Section component (substituir linhas 122-134):
    - Manter assinatura `function Section({ num, title, children }: { num: string; title: string; children: React.ReactNode })`.
    - Manter `const [open, setOpen] = useState(false)`.
    - Adicionar `const rotation = useRef(new Animated.Value(0)).current;`.
    - Criar função `toggle`:
      - Calcula `const nextOpen = !open;`
      - Chama `LayoutAnimation.configureNext(LayoutAnimation.Presets.easeInEaseOut);` para animar expand/collapse.
      - Chama `Animated.timing(rotation, { toValue: nextOpen ? 1 : 0, duration: 200, useNativeDriver: true }).start();` para rotação do chevron.
      - Chama `setOpen(nextOpen);`.
      - Chama `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light);` para feedback háptico (sem guard de Platform — segue padrão de `components/haptic-tab.tsx`).
    - Calcular interpolação: `const rotate = rotation.interpolate({ inputRange: [0, 1], outputRange: ['0deg', '180deg'] });`.
    - JSX:
      - `<TouchableOpacity style={[styles.sectionHeader, open && styles.sectionHeaderOpen]} onPress={toggle} activeOpacity={0.7}>` — substituir `onPress={() => setOpen(o => !o)}` por `onPress={toggle}`.
      - Substituir `<Text style={styles.chevron}>{open ? '▲' : '▼'}</Text>` por `<Animated.Text style={[styles.chevron, { transform: [{ rotate }] }]}>▼</Animated.Text>`. Manter apenas o símbolo `▼` (a rotação simula `▲` quando aberto).
      - Manter `{open && <View style={styles.sectionBody}>{children}</View>}` inalterado.

    Restrições:
    - Não importar `react-native-reanimated` — usar apenas `Animated` da RN core.
    - Não adicionar `Platform`/`UIManager.setLayoutAnimationEnabledExperimental` — projeto tem `newArchEnabled: true`, o que torna esse guard desnecessário (RESEARCH.md Pitfall 1). Se durante teste visual em Android a expansão for instantânea, documentar como deviation Rule 1 e adicionar o guard apenas para Android.
    - `useNativeDriver: true` apenas para `transform: [{ rotate }]` — nunca para backgroundColor (Pitfall 2).
    - Chevron deve ser `Animated.Text` (não `Text`) para o transform funcionar com Animated.Value (Pitfall 3).
    - Não alterar nenhum style — `styles.chevron` permanece o mesmo (color RPG.goldDim, fontSize 11).

    Commit message: `feat(11-01): animação do chevron + haptic feedback no Section colapsável (REG-10,REG-11,REG-12)`.
  </action>
  <verify>
    <automated>
      cd C:/Users/edysano/Desktop/magic-keos &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -E "(error|Error)" | wc -l
      # Deve retornar 0

      # Import de expo-haptics presente
      grep -c "from 'expo-haptics'" app/\(tabs\)/regras.tsx
      # Deve retornar 1

      # Animated e LayoutAnimation importados de react-native
      grep -E "from 'react-native'" app/\(tabs\)/regras.tsx | grep -c "Animated"
      # Deve retornar 1 (no destructuring do import principal OU em import separado)
      grep -E "from 'react-native'" app/\(tabs\)/regras.tsx | grep -c "LayoutAnimation"
      # Deve retornar 1

      # useRef importado de react
      grep -E "from 'react'" app/\(tabs\)/regras.tsx | grep -c "useRef"
      # Deve retornar 1

      # REG-10: Animated.timing presente com useNativeDriver
      grep -c "Animated.timing" app/\(tabs\)/regras.tsx
      # Deve retornar &gt;= 1
      grep -c "useNativeDriver: true" app/\(tabs\)/regras.tsx
      # Deve retornar &gt;= 1
      grep -c "LayoutAnimation.configureNext" app/\(tabs\)/regras.tsx
      # Deve retornar &gt;= 1

      # REG-10: Animated.Text para o chevron (rotação interpolada)
      grep -c "Animated.Text" app/\(tabs\)/regras.tsx
      # Deve retornar &gt;= 1
      grep -cE "outputRange: \['0deg', '180deg'\]" app/\(tabs\)/regras.tsx
      # Deve retornar 1

      # REG-12: Haptics.impactAsync presente
      grep -c "Haptics.impactAsync" app/\(tabs\)/regras.tsx
      # Deve retornar 1
      grep -c "Haptics.ImpactFeedbackStyle.Light" app/\(tabs\)/regras.tsx
      # Deve retornar 1

      # Chevron antigo (text ternário ▲/▼) foi removido — apenas '▼' permanece
      grep -cE "open \? '▲' : '▼'" app/\(tabs\)/regras.tsx
      # Deve retornar 0

      # Section component continua com mesma assinatura
      grep -c "function Section({ num, title, children }" app/\(tabs\)/regras.tsx
      # Deve retornar 1

      # Não foi adicionado import de reanimated
      grep -c "react-native-reanimated" app/\(tabs\)/regras.tsx
      # Deve retornar 0
    </automated>
  </verify>
  <done>
    - Imports atualizados: `useRef` de React; `Animated`, `LayoutAnimation` de `react-native`; `Haptics` de `expo-haptics`
    - Section usa `useRef(new Animated.Value(0))` + `Animated.timing` + `LayoutAnimation.configureNext` na função `toggle`
    - Chevron é `Animated.Text` com `transform: [{ rotate }]` interpolando 0→180deg
    - `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light)` é chamado no `toggle`
    - `npx tsc --noEmit` retorna 0 erros
    - Nenhum import de `react-native-reanimated` adicionado
    - Commit criado com mensagem `feat(11-01): animação do chevron + haptic feedback no Section colapsável (REG-10,REG-11,REG-12)`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User touch → React Native UI | Input de toque humano local; sem dados sensíveis, sem rede, sem persistência |
| App → Device haptic motor | Chamada nativa via expo-haptics; sem permissões de runtime requeridas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Denial of Service | Animated.timing loop | accept | Animação dura 200ms com `useNativeDriver: true`; thread JS não é bloqueada. Sem loop infinito (apenas toggle entre 0 e 1). |
| T-11-02 | Information Disclosure | expo-haptics call | accept | Haptic não expõe dados — apenas dispara vibração. Sem PII, sem state externo. |
| T-11-03 | Tampering | Estado `open` do Section | accept | Estado é puramente UI local (`useState`); sem persistência cross-session; sem impacto fora da view. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma nova dependência instalada nesta phase (expo-haptics ~15.0.8 já em package.json desde versões anteriores; Animated/LayoutAnimation são built-in do RN 0.81.5). RESEARCH.md Package Legitimacy Audit confirmou zero novos pacotes. |
</threat_model>

<verification>
Após Task 1 e Task 2 concluídos:

```bash
# 1. TypeScript clean
npx tsc --noEmit
# Esperado: exit 0, zero erros

# 2. ESLint clean (nenhum erro novo)
npx expo lint
# Esperado: exit 0 ou apenas warnings pré-existentes

# 3. Todos os 6 requirements têm pegada no arquivo
grep -c "RPG.surfaceAlt" app/\(tabs\)/regras.tsx   # REG-03: >= 1
grep -c "StyleSheet.hairlineWidth" app/\(tabs\)/regras.tsx   # REG-04: >= 1
grep -c "RPG.border" app/\(tabs\)/regras.tsx   # REG-04: >= 1
grep -c "Animated.timing" app/\(tabs\)/regras.tsx   # REG-10: >= 1
grep -c "LayoutAnimation.configureNext" app/\(tabs\)/regras.tsx   # REG-10: >= 1
grep -c "sectionHeaderOpen" app/\(tabs\)/regras.tsx   # REG-11: >= 1 (já existente de Phase 10)
grep -c "Haptics.impactAsync" app/\(tabs\)/regras.tsx   # REG-12: 1
```

Verificação visual manual (recomendada após implementação — não bloqueante para SUMMARY):
- Abrir app via `npx expo start` em device físico iOS
- Tocar em qualquer Section da aba Regras → chevron deve rotacionar 180° suavemente
- Conteúdo deve expandir/colapsar com fade/slide (não instantâneo)
- Cabeçalho aberto deve ter background ligeiramente mais claro que fechado
- Haptic leve perceptível no toque (iOS) ou vibração discreta (Android)
- Abrir qualquer seção com tabela (ex.: seção 1 Cores, seção 5 Dados, seção 11 Domínios) → header de tabela deve ter background visivelmente mais escuro que data rows; data rows devem ter linha separadora sutil entre si
</verification>

<success_criteria>
- 6/6 requirements desta phase fechados (REG-03, REG-04, REG-05, REG-10, REG-11, REG-12)
- `app/(tabs)/regras.tsx` é o ÚNICO arquivo de produção modificado
- Zero novas dependências em `package.json`
- `npx tsc --noEmit` retorna 0 erros
- Section component preserva sua assinatura pública (`{ num, title, children }`)
- Nenhum `TH`/`R2`/`R3` foi tocado no JSX — apenas StyleSheet
- 2 commits criados, um por task, mensagens em `feat(11-01): ...` com IDs de requirement
- SUMMARY.md criado em `.planning/phases/11-tabelas-interatividade/11-01-SUMMARY.md` documentando: arquivos modificados, decisões (LayoutAnimation + Animated em vez de Reanimated; haptic sem Platform guard), zero novas dependências, REG-05 marcado como "already satisfied — verified" sem mudança de código
</success_criteria>

<output>
Create `.planning/phases/11-tabelas-interatividade/11-01-SUMMARY.md` when done.

Requirements completed (copy verbatim to SUMMARY frontmatter `requirements-completed`):
- REG-03, REG-04, REG-05, REG-10, REG-11, REG-12
</output>
