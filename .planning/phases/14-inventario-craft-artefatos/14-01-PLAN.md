---
phase: 14-inventario-craft-artefatos
plan: 01
type: execute
wave: 1
depends_on: ["13-01"]
files_modified:
  - data/regras/equipamentos.ts
  - app/(tabs)/mochila.tsx
autonomous: true
requirements: [INV-01, INV-02, CRAFT-01, CRAFT-02, CRAFT-03, ARTE-01]

must_haves:
  truths:
    - "Grade de 20 slots em 2 colunas exibe na aba Mochila, cada slot com TextInput e botão de limpar"
    - "Apagar um slot não afeta nenhum outro slot (setInventarioSlot usa spread imutável)"
    - "Cada slot de equipamento expandido mostra lista de melhorias aplicadas com botão × individual"
    - "Botão 'Adicionar Melhoria' abre craft modal com exatamente 5 opções filtradas pelo tipo do slot"
    - "Cada melhoria no modal tem cor correspondente ao livro (branco/verde/vermelho/preto/azul)"
    - "Toggle 'Básico / Artefato ✦' muda item.tipo; modo Artefato exibe TextInput efeito + NumericStepper durabilidade"
  artifacts:
    - path: "data/regras/equipamentos.ts"
      provides: "MELHORIAS_POR_SLOT e MelhoriaItem — dados de craft estruturados por slot"
      contains: "MELHORIAS_POR_SLOT"
    - path: "app/(tabs)/mochila.tsx"
      provides: "Grade inventário + craft UI + artefato toggle no slotBody expandido"
      contains: "inventarioSlots"
  key_links:
    - from: "app/(tabs)/mochila.tsx"
      to: "data/regras/equipamentos.ts"
      via: "import { MELHORIAS_POR_SLOT }"
      pattern: "MELHORIAS_POR_SLOT"
    - from: "app/(tabs)/mochila.tsx"
      to: "store/CharacterContext.tsx"
      via: "setInventarioSlot(idx, v) e setEquipamentoItem(slot, {...item})"
      pattern: "setInventarioSlot"
---

<objective>
Completar a aba Mochila com três capacidades novas: (1) grade de 20 slots de inventário em 2 colunas com TextInput e limpar individual, (2) sistema de craft nos cards de equipamento — listar melhorias aplicadas, removê-las individualmente, e abrir um segundo modal para adicionar até 3 melhorias filtradas por tipo com cor do livro, e (3) toggle Básico/Artefato nos slots de equipamento que expõe campos de efeito ativável e durabilidade.

Purpose: Fechar todos os requisitos INV-01, INV-02, CRAFT-01, CRAFT-02, CRAFT-03 e ARTE-01 do milestone v1.3 Mochila & Craft.
Output: data/regras/equipamentos.ts com nova constante MELHORIAS_POR_SLOT; app/(tabs)/mochila.tsx com grade de inventário, craft UI e campos de artefato.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-mochila-slots/13-01-SUMMARY.md
</context>

<interfaces>
<!-- Contratos e tipos que o executor precisa. Extraídos diretamente do codebase. -->

De types/character.ts (linhas 9-15):
```typescript
export interface EquipItem {
  nome: string;
  tipo: 'basico' | 'artefato';
  melhorias: string[];   // labels ex: 'Acurácia+1' — string simples, mesmo valor do label
  efeito?: string;
  durabilidade?: number;
}
// Character.inventarioSlots: string[] (20 elementos, Array(20).fill(''))
```

De store/CharacterContext.tsx (linhas 71-72 e 181-184):
```typescript
setInventarioSlot: (idx: number, v: string) => void;
// Implementação: spread imutável — não afeta outros slots
setEquipamentoItem: (slot: keyof Character['equipamentos'], item: EquipItem | null) => void;
// Implementação: spread imutável — não afeta outros slots
```

De components/rpg/NumericStepper.tsx (linhas 5-13):
```typescript
interface Props {
  value: number;
  onChange: (v: number) => void;
  min?: number;
  max?: number;
  label?: string;
  color?: string;
  compact?: boolean;
}
// Uso: <NumericStepper label="Durabilidade" value={item.durabilidade ?? 0} onChange={v => ...} min={0} compact />
```

De data/regras/equipamentos.ts — interface nova a ser adicionada na Task 1:
```typescript
export interface MelhoriaItem {
  label: string;  // string salva em EquipItem.melhorias[] — ex: 'Acurácia+1'
  cor: 'branco' | 'verde' | 'vermelho' | 'preto' | 'azul';
}
export const MELHORIAS_POR_SLOT: Record<
  'arma' | 'escudo' | 'vestimenta' | 'acessorio1' | 'acessorio2',
  MelhoriaItem[]
>;
// Cada chave tem exatamente 5 itens (5 cores do livro)
```

De constants/theme.ts — mapeamento de cor de melhoria para token RPG:
```typescript
// branco   → RPG.branco    '#e8e0cc'
// verde    → RPG.verde     '#3a8a3a'
// vermelho → RPG.vermelho  '#b52020'
// preto    → RPG.pretoLight '#6a5882'  // NÃO usar RPG.preto '#2a2030' — invisível sobre RPG.surface
// azul     → RPG.azul      '#1a5ab0'
```

De app/(tabs)/mochila.tsx — estado e funções existentes (linhas 55-88):
```typescript
// Estado existente
const [pickerSlot, setPickerSlot] = useState<SlotKey | null>(null);
const [expandedSlot, setExpandedSlot] = useState<SlotKey | null>(null);

// Funções existentes (NÃO duplicar):
// pickItem(slot, nome), clearSlot(slot), editNome(slot, nome), toggleExpanded(slot)

// slotBody existente (linhas 136-163) — expandir, não substituir
{isExpanded && (
  <View style={styles.slotBody}>
    <TextInput ... />              // nome personalizado
    <TouchableOpacity ...>         // Escolher do livro
    {item != null && (
      <TouchableOpacity ...>       // Remover item
    )}
  </View>
)}
// Task 2 adiciona APÓS clearBtn e ANTES do fechamento </View> do slotBody
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar MelhoriaItem e MELHORIAS_POR_SLOT em equipamentos.ts</name>
  <files>data/regras/equipamentos.ts</files>
  <action>
Abrir data/regras/equipamentos.ts e adicionar, após a interface MelhoriaRow existente (linha 27) e antes de PropElementalRow, duas novas declarações:

1. Interface MelhoriaItem:
```
export interface MelhoriaItem {
  label: string;
  cor: 'branco' | 'verde' | 'vermelho' | 'preto' | 'azul';
}
```

2. Constante MELHORIAS_POR_SLOT — adicionar no final do arquivo, após a constante `melhorias` existente (linha 84) e antes de `propriedadesElementais`:

```
export const MELHORIAS_POR_SLOT: Record<'arma' | 'escudo' | 'vestimenta' | 'acessorio1' | 'acessorio2', MelhoriaItem[]> = {
  arma: [
    { label: 'Acurácia+1',         cor: 'branco'   },
    { label: 'Acurácia madeira+1', cor: 'verde'    },
    { label: 'Dano físico+1',      cor: 'vermelho' },
    { label: 'Dado dano+1',        cor: 'preto'    },
    { label: 'Dano mágico+1',      cor: 'azul'     },
  ],
  escudo: [
    { label: 'IP Esp+1/Armadura+1',  cor: 'branco'   },
    { label: 'IP Corp+1/Manto+1',    cor: 'verde'    },
    { label: 'IP Corp+1/Armadura+1', cor: 'vermelho' },
    { label: 'IP Esp+1/IP Mental+1', cor: 'preto'    },
    { label: 'IP Mental+1/Manto+1',  cor: 'azul'     },
  ],
  vestimenta: [
    { label: 'Armadura+1/Diplomacia+1/Esgrima+1',    cor: 'branco'   },
    { label: 'Manto+1/Comunhão+1/Pontaria+1',        cor: 'verde'    },
    { label: 'IP Corp+1/Expressão+1/Atletismo+1',    cor: 'vermelho' },
    { label: 'IP Esp+1/Intimidação+1/Furtividade+1', cor: 'preto'    },
    { label: 'IP Mental+1/Lábia+1/Artes Marciais+1', cor: 'azul'     },
  ],
  acessorio1: [
    { label: 'Foco+1/Mecânica+1',           cor: 'branco'   },
    { label: 'Canalização+1/Sobrevivência+1', cor: 'verde'    },
    { label: 'Velocidade+1/Criatividade+1',  cor: 'vermelho' },
    { label: 'Domínio+1/Alquimia+1',         cor: 'preto'    },
    { label: 'Memória+1/Investigação+1',     cor: 'azul'     },
  ],
  acessorio2: [
    { label: 'Foco+1/Mecânica+1',           cor: 'branco'   },
    { label: 'Canalização+1/Sobrevivência+1', cor: 'verde'    },
    { label: 'Velocidade+1/Criatividade+1',  cor: 'vermelho' },
    { label: 'Domínio+1/Alquimia+1',         cor: 'preto'    },
    { label: 'Memória+1/Investigação+1',     cor: 'azul'     },
  ],
};
```

Não remover nem alterar nenhuma das exportações existentes (armas, escudos, vestimentas, acessorios, melhorias, propriedadesElementais, afiadores).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    npx tsc --noEmit sai com exit 0.
    grep -c "MELHORIAS_POR_SLOT" data/regras/equipamentos.ts retorna >= 2 (interface + constante).
    grep -c "MelhoriaItem" data/regras/equipamentos.ts retorna >= 2 (interface + uso no Record).
  </done>
</task>

<task type="auto">
  <name>Task 2: Adicionar craft UI e campos de artefato ao slotBody em mochila.tsx</name>
  <files>app/(tabs)/mochila.tsx</files>
  <action>
Modificar app/(tabs)/mochila.tsx — adicionar imports, estado, funções auxiliares, JSX dentro do slotBody, e entradas no StyleSheet. NÃO alterar nenhum código existente — apenas acrescentar.

--- PASSO 1: Imports ---

Na linha de import de equipamentos (linha 21), adicionar MELHORIAS_POR_SLOT e MelhoriaItem:
  De: import { armas, escudos, vestimentas, acessorios } from '@/data/regras/equipamentos';
  Para: import { armas, escudos, vestimentas, acessorios, MELHORIAS_POR_SLOT, MelhoriaItem } from '@/data/regras/equipamentos';

Adicionar NumericStepper import após o import de SectionHeader (linha 19):
  import NumericStepper from '@/components/rpg/NumericStepper';

--- PASSO 2: Constante de cor fora do componente ---

Após SLOT_ITEMS (após linha 53) e antes do componente MochilaScreen, adicionar:

```
const COR_TOKEN: Record<MelhoriaItem['cor'], string> = {
  branco:   RPG.branco,
  verde:    RPG.verde,
  vermelho: RPG.vermelho,
  preto:    RPG.pretoLight,
  azul:     RPG.azul,
};
```

--- PASSO 3: Estado e funções dentro de MochilaScreen ---

Após o estado existente (linhas 57-58: pickerSlot, expandedSlot), adicionar:
  const [crafterSlot, setCrafterSlot] = useState<SlotKey | null>(null);

Adicionar setInventarioSlot ao destructuring do useCharacter (linha 56):
  De: const { character: c, setEquipamentoItem, isLoaded } = useCharacter();
  Para: const { character: c, setEquipamentoItem, setInventarioSlot, isLoaded } = useCharacter();

Adicionar as funções auxiliares novas após toggleExpanded (após linha 88):

```
function addMelhoria(slot: SlotKey, label: string) {
  const item = c.equipamentos[slot];
  if (!item || item.melhorias.length >= 3) return;
  setEquipamentoItem(slot, { ...item, melhorias: [...item.melhorias, label] });
  setCrafterSlot(null);
}

function removeMelhoria(slot: SlotKey, idx: number) {
  const item = c.equipamentos[slot];
  if (!item) return;
  setEquipamentoItem(slot, { ...item, melhorias: item.melhorias.filter((_, i) => i !== idx) });
}

function toggleTipo(slot: SlotKey) {
  const item = c.equipamentos[slot];
  if (!item) return;
  const novoTipo = item.tipo === 'basico' ? 'artefato' : 'basico';
  setEquipamentoItem(slot, {
    ...item,
    tipo: novoTipo,
    efeito: novoTipo === 'basico' ? undefined : item.efeito,
    durabilidade: novoTipo === 'basico' ? undefined : item.durabilidade,
  });
}
```

--- PASSO 4: activeCrafterSlot fallback (análogo ao activePickerSlot) ---

Logo após a linha `const activePickerSlot = pickerSlot ?? 'arma';` (linha 98), adicionar:
  const activeCrafterSlot = crafterSlot ?? 'arma';

--- PASSO 5: JSX no slotBody ---

Localizar o bloco slotBody existente (linhas 136-163). Dentro de slotBody, após o fechamento do bloco `{item != null && ( <TouchableOpacity clearBtn ... /> )}` e ANTES do `</View>` que fecha slotBody, inserir o bloco de craft + artefato:

```
{item != null && (
  <>
    {/* --- Melhorias existentes --- */}
    {item.melhorias.length > 0 && (
      <View style={styles.melhoriasList}>
        {item.melhorias.map((label, idx) => {
          const melhoriaData = MELHORIAS_POR_SLOT[slot].find(m => m.label === label);
          const cor = melhoriaData ? COR_TOKEN[melhoriaData.cor] : RPG.textMuted;
          return (
            <View key={idx} style={[styles.melhoriaBadge, { borderColor: cor }]}>
              <Text style={[styles.melhoriaLabel, { color: cor }]}>{label}</Text>
              <TouchableOpacity onPress={() => removeMelhoria(slot, idx)} activeOpacity={0.7} style={styles.melhoriaRemove}>
                <Text style={[styles.melhoriaRemoveText, { color: cor }]}>×</Text>
              </TouchableOpacity>
            </View>
          );
        })}
      </View>
    )}
    {/* --- Botão Adicionar Melhoria (desativado se 3 melhorias) --- */}
    <TouchableOpacity
      style={[styles.addMelhoriaBtn, item.melhorias.length >= 3 && styles.addMelhoriaBtnDisabled]}
      onPress={() => { if (item.melhorias.length < 3) setCrafterSlot(slot); }}
      activeOpacity={item.melhorias.length >= 3 ? 1 : 0.8}
    >
      <Text style={styles.addMelhoriaBtnText}>
        {item.melhorias.length >= 3 ? 'Máx. 3 melhorias' : '+ Adicionar Melhoria'}
      </Text>
    </TouchableOpacity>
    {/* --- Toggle Básico / Artefato --- */}
    <TouchableOpacity
      style={[styles.tipoToggle, item.tipo === 'artefato' && styles.tipoToggleActive]}
      onPress={() => toggleTipo(slot)}
      activeOpacity={0.8}
    >
      <Text style={[styles.tipoToggleText, item.tipo === 'artefato' && styles.tipoToggleTextActive]}>
        {item.tipo === 'basico' ? 'Básico' : 'Artefato ✦'}
      </Text>
    </TouchableOpacity>
    {/* --- Campos extras (somente modo artefato) --- */}
    {item.tipo === 'artefato' && (
      <>
        <TextInput
          style={[styles.nomeInput, styles.efeitoInput]}
          value={item.efeito ?? ''}
          onChangeText={v => setEquipamentoItem(slot, { ...item, efeito: v })}
          placeholder="Efeito ativável..."
          placeholderTextColor={RPG.textDark}
          multiline
        />
        <NumericStepper
          label="Durabilidade"
          value={item.durabilidade ?? 0}
          onChange={v => setEquipamentoItem(slot, { ...item, durabilidade: v })}
          min={0}
          compact
        />
      </>
    )}
  </>
)}
```

--- PASSO 6: Modal de Craft ---

Após o Modal de picker existente (após linha 199, antes do fechamento </SafeAreaView> do outer), adicionar um segundo Modal:

```
<Modal
  visible={crafterSlot !== null}
  animationType="slide"
  onRequestClose={() => setCrafterSlot(null)}
>
  <SafeAreaView style={styles.modal} edges={['top', 'bottom']}>
    <View style={styles.modalHeader}>
      <Text style={styles.modalTitle}>
        Melhoria — {SLOT_LABELS[activeCrafterSlot]}
      </Text>
      <TouchableOpacity onPress={() => setCrafterSlot(null)} activeOpacity={0.7}>
        <Text style={styles.modalClose}>✕</Text>
      </TouchableOpacity>
    </View>
    <ScrollView style={styles.modalList} keyboardShouldPersistTaps="handled">
      {MELHORIAS_POR_SLOT[activeCrafterSlot].map((m, i) => {
        const cor = COR_TOKEN[m.cor];
        const jaAplicada = (c.equipamentos[activeCrafterSlot]?.melhorias ?? []).includes(m.label);
        return (
          <TouchableOpacity
            key={i}
            style={[styles.craftItem, jaAplicada && styles.craftItemApplied]}
            onPress={() => { if (!jaAplicada) addMelhoria(activeCrafterSlot, m.label); }}
            activeOpacity={jaAplicada ? 1 : 0.75}
          >
            <View style={[styles.craftCorDot, { backgroundColor: cor }]} />
            <Text style={[styles.craftItemLabel, { color: jaAplicada ? RPG.textDark : RPG.text }]}>
              {m.label}
            </Text>
            {jaAplicada && <Text style={styles.craftItemAppliedMark}>✓</Text>}
          </TouchableOpacity>
        );
      })}
    </ScrollView>
  </SafeAreaView>
</Modal>
```

--- PASSO 7: Seção de Inventário no ScrollView ---

No ScrollView principal, após o fechamento do SLOT_KEYS.map (após linha 165, antes do fechamento </ScrollView>), adicionar a seção de inventário:

```
<SectionHeader title="Inventário" />
<View style={styles.invGrid}>
  {Array.from({ length: 10 }, (_, row) => (
    <View key={row} style={styles.invRow}>
      {[row * 2, row * 2 + 1].map(idx => (
        <View key={idx} style={styles.invCell}>
          <TextInput
            style={styles.invInput}
            value={c.inventarioSlots[idx] ?? ''}
            onChangeText={v => setInventarioSlot(idx, v)}
            placeholder={String(idx + 1)}
            placeholderTextColor={RPG.textDark}
          />
          {(c.inventarioSlots[idx] ?? '').trim() !== '' && (
            <TouchableOpacity
              onPress={() => setInventarioSlot(idx, '')}
              activeOpacity={0.7}
              style={styles.invClearBtn}
            >
              <Text style={styles.invClearText}>✕</Text>
            </TouchableOpacity>
          )}
        </View>
      ))}
    </View>
  ))}
</View>
```

--- PASSO 8: Novos estilos no StyleSheet ---

Adicionar ao StyleSheet.create existente, após o último estilo (modalItemMeta):

```
  // Craft — melhorias
  melhoriasList: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    gap: 6,
  },
  melhoriaBadge: {
    flexDirection: 'row',
    alignItems: 'center',
    borderWidth: 1,
    borderRadius: 4,
    paddingHorizontal: 6,
    paddingVertical: 3,
    gap: 4,
  },
  melhoriaLabel: {
    fontSize: 11,
    fontWeight: '600',
  },
  melhoriaRemove: {
    paddingHorizontal: 2,
  },
  melhoriaRemoveText: {
    fontSize: 14,
    lineHeight: 16,
    fontWeight: '700',
  },
  addMelhoriaBtn: {
    borderWidth: 1,
    borderColor: RPG.goldDim,
    borderRadius: 4,
    paddingVertical: 7,
    alignItems: 'center',
  },
  addMelhoriaBtnDisabled: {
    borderColor: RPG.border,
    opacity: 0.5,
  },
  addMelhoriaBtnText: {
    color: RPG.gold,
    fontSize: 12,
    fontWeight: '600',
  },
  // Artefato toggle
  tipoToggle: {
    borderWidth: 1,
    borderColor: RPG.border,
    borderRadius: 4,
    paddingVertical: 7,
    alignItems: 'center',
  },
  tipoToggleActive: {
    borderColor: RPG.gold,
    backgroundColor: RPG.surfaceAlt,
  },
  tipoToggleText: {
    color: RPG.textMuted,
    fontSize: 12,
    fontWeight: '600',
    letterSpacing: 0.5,
  },
  tipoToggleTextActive: {
    color: RPG.gold,
  },
  efeitoInput: {
    minHeight: 60,
    textAlignVertical: 'top',
  },
  // Craft modal
  craftItem: {
    flexDirection: 'row',
    alignItems: 'center',
    padding: 14,
    gap: 10,
    borderBottomWidth: StyleSheet.hairlineWidth,
    borderBottomColor: RPG.border,
  },
  craftItemApplied: {
    opacity: 0.5,
  },
  craftCorDot: {
    width: 12,
    height: 12,
    borderRadius: 6,
  },
  craftItemLabel: {
    flex: 1,
    fontSize: 14,
    fontWeight: '600',
  },
  craftItemAppliedMark: {
    fontSize: 14,
    color: RPG.textMuted,
  },
  // Inventário grid
  invGrid: {
    marginTop: 4,
    marginBottom: 16,
  },
  invRow: {
    flexDirection: 'row',
    gap: 8,
    marginBottom: 8,
  },
  invCell: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: RPG.surface,
    borderWidth: 1,
    borderColor: RPG.border,
    borderRadius: 4,
    paddingHorizontal: 8,
    paddingVertical: 0,
    minHeight: 38,
  },
  invInput: {
    flex: 1,
    color: RPG.text,
    fontSize: 13,
    paddingVertical: 8,
  },
  invClearBtn: {
    paddingHorizontal: 4,
    paddingVertical: 6,
  },
  invClearText: {
    color: RPG.textDark,
    fontSize: 13,
  },
```
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    npx tsc --noEmit sai com exit 0.
    grep -c "inventarioSlots" app/(tabs)/mochila.tsx retorna >= 2.
    grep -c "setInventarioSlot" app/(tabs)/mochila.tsx retorna >= 2.
    grep -c "melhorias.length" app/(tabs)/mochila.tsx retorna >= 2 (guard + badge no header existente).
    grep -c "removeMelhoria" app/(tabs)/mochila.tsx retorna >= 2 (definição + uso no JSX).
    grep -c "artefato" app/(tabs)/mochila.tsx retorna >= 3 (toggleTipo + tipoToggleActive + condicional JSX).
    grep -c "MELHORIAS_POR_SLOT" app/(tabs)/mochila.tsx retorna >= 2 (import + uso no modal).
    grep -c "crafterSlot" app/(tabs)/mochila.tsx retorna >= 3 (estado + setCrafterSlot + Modal visible).
  </done>
</task>

<task type="auto">
  <name>Task 3: Verificação final e smoke check de compilação</name>
  <files></files>
  <action>
Executar as seguintes verificações e reportar o resultado de cada uma no SUMMARY.md:

1. TypeScript limpo:
   Rodar: npx tsc --noEmit
   Esperado: exit 0

2. CRAFT-02 — dados de melhoria presentes e estruturados:
   Rodar: grep -c "MELHORIAS_POR_SLOT" data/regras/equipamentos.ts
   Esperado: >= 2

3. INV-01/INV-02 — inventário referenciado:
   Rodar: grep -c "inventarioSlots" app/(tabs)/mochila.tsx
   Esperado: >= 2

4. CRAFT-01 — guard de capacidade presente:
   Rodar: grep -c "melhorias.length" app/(tabs)/mochila.tsx
   Esperado: >= 2

5. CRAFT-03 — remoção imutável presente:
   Rodar: grep -c "filter" app/(tabs)/mochila.tsx
   Esperado: >= 1

6. ARTE-01 — toggle e campos condicionais presentes:
   Rodar: grep -c "artefato" app/(tabs)/mochila.tsx
   Esperado: >= 3

Se qualquer verificação falhar, reportar a falha, identificar a causa e corrigir antes de marcar o plano como concluído.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <human-check>
      Iniciar o app no simulador/dispositivo. Verificar:
      1. Aba Mochila carrega sem crash.
      2. Abaixo dos 5 slots de equipamento, a seção "Inventário" aparece com 10 linhas de 2 células (20 slots no total).
      3. Digitar texto em qualquer slot de inventário — texto persiste após navegar para outra aba e voltar.
      4. Com texto preenchido, botão ✕ aparece — ao tocar, limpa apenas aquele slot, os demais permanecem.
      5. Expandir um slot de equipamento (ex.: Arma) com item selecionado — ver botão "+ Adicionar Melhoria".
      6. Tocar "+ Adicionar Melhoria" — modal abre com 5 opções coloridas (branco, verde, vermelho, roxo/preto, azul).
      7. Selecionar uma melhoria — modal fecha, melhoria aparece como badge colorido no slotBody.
      8. Adicionar 3 melhorias — botão passa a exibir "Máx. 3 melhorias" e não abre o modal.
      9. Tocar × em uma melhoria — apenas aquela é removida, as outras permanecem.
      10. Tocar "Básico" toggle — botão muda para "Artefato ✦", campos Efeito ativável e Durabilidade aparecem.
      11. Preencher efeito e durabilidade — dados persistem após navegar para outra aba e voltar.
      12. Tocar "Artefato ✦" novamente — toggle volta para "Básico", campos extras somem.
    </human-check>
  </verify>
  <done>
    Todas as 6 verificações automatizadas retornam os valores esperados.
    npx tsc --noEmit sai com exit 0.
    Smoke test manual confirma os 12 pontos acima.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TextInput → setInventarioSlot | Texto livre do usuário nos 20 slots de inventário |
| TextInput → setEquipamentoItem (efeito) | Texto livre do usuário no campo efeito ativável (multiline) |
| NumericStepper → setEquipamentoItem (durabilidade) | Inteiro controlado pelo stepper com clamp min=0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | TextInput inventário / efeito | accept | App local-only, dados não saem do dispositivo; nenhum PII; usuário escreve para si mesmo |
| T-14-02 | Denial of Service | addMelhoria sem guard de capacidade | mitigate | Guard explícito `if (!item \|\| item.melhorias.length >= 3) return` + botão desativado visualmente quando length >= 3 |
| T-14-03 | Tampering | MELHORIAS_POR_SLOT constante | accept | Constante readonly em módulo TypeScript — imutável em runtime; não exposta a entrada de usuário |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | Fase sem dependências externas novas — nenhuma instalação de pacote |
</threat_model>

<verification>
## Critérios de Sucesso do Roadmap — Phase 14

| # | Critério | Verificação Automatizada | Verificação Manual |
|---|----------|--------------------------|-------------------|
| 1 | Grade de 20 slots em 2 colunas exibe na Mochila; cada slot aceita texto livre (INV-01) | grep -c "inventarioSlots" mochila.tsx >= 2 | Seção Inventário com 20 cells visíveis e editáveis |
| 2 | Usuário apaga conteúdo de slot individualmente sem afetar demais (INV-02) | grep -c "setInventarioSlot" mochila.tsx >= 2 | Botão ✕ limpa apenas aquele slot |
| 3 | Usuário adiciona até 3 melhorias com lista filtrada pelo tipo (CRAFT-01) | grep -c "melhorias.length" mochila.tsx >= 2 | Modal abre com 5 opções para o slot correto; máx 3 funciona |
| 4 | Melhorias correspondem exatamente ao livro por categoria e cor (CRAFT-02) | grep -c "MELHORIAS_POR_SLOT" equipamentos.ts >= 2 | Cores dos badges correspondem branco/verde/vermelho/roxo/azul |
| 5 | Usuário remove melhoria individualmente sem afetar demais (CRAFT-03) | grep -c "filter" mochila.tsx >= 1 | Botão × remove apenas aquele badge |
| 6 | Toggle Básico/Artefato expõe efeito e durabilidade (ARTE-01) | grep -c "artefato" mochila.tsx >= 3 | Campos aparecem/somem corretamente; dados persistem |

## Compilação

`npx tsc --noEmit` exit 0 após cada task e ao final do plano.
</verification>

<success_criteria>
1. npx tsc --noEmit sai com exit 0 após todas as tasks.
2. grep -c "MELHORIAS_POR_SLOT" data/regras/equipamentos.ts retorna >= 2.
3. grep -c "MelhoriaItem" data/regras/equipamentos.ts retorna >= 2.
4. grep -c "inventarioSlots" app/(tabs)/mochila.tsx retorna >= 2.
5. grep -c "setInventarioSlot" app/(tabs)/mochila.tsx retorna >= 2.
6. grep -c "melhorias.length" app/(tabs)/mochila.tsx retorna >= 2.
7. grep -c "filter" app/(tabs)/mochila.tsx retorna >= 1 (removeMelhoria).
8. grep -c "artefato" app/(tabs)/mochila.tsx retorna >= 3.
9. grep -c "crafterSlot" app/(tabs)/mochila.tsx retorna >= 3.
10. Smoke test manual confirma os 12 pontos da Task 3 human-check.
</success_criteria>

<output>
Criar `.planning/phases/14-inventario-craft-artefatos/14-01-SUMMARY.md` ao concluir todas as tasks.
</output>
