---
phase: 14-inventario-craft-artefatos
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - app/(tabs)/_layout.tsx
autonomous: true
requirements: [INV-01]

must_haves:
  truths:
    - "Aba Mochila ocupa a 3ª posição na tab bar: Ficha | Magia | Mochila | Grimório | Regras | Notas"
    - "A ordem das outras 5 abas permanece inalterada"
  artifacts:
    - path: "app/(tabs)/_layout.tsx"
      provides: "Tab order with mochila at index 3"
      contains: "mochila"
  key_links:
    - from: "app/(tabs)/_layout.tsx"
      to: "app/(tabs)/mochila.tsx"
      via: "Tabs.Screen name='mochila'"
      pattern: "name=\"mochila\""
---

<objective>
Mover a aba Mochila da 6ª posição (atual, última) para a 3ª posição na tab bar, conforme D-19/D-20.

Purpose: A Mochila é usada durante sessões RPG junto com Ficha e Magia — posição 3 faz sentido ergonômico.
Output: app/(tabs)/_layout.tsx com nova ordem de Tabs.Screen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Estado atual de app/(tabs)/_layout.tsx — ordem existente -->

Ordem atual dos Tabs.Screen (linhas 28-70):
1. name="index"     → title="Ficha"
2. name="magia"     → title="Magia"
3. name="grimorio"  → title="Grimório"
4. name="regras"    → title="Regras"
5. name="notas"     → title="Notas"
6. name="mochila"   → title="Mochila"  ← mover para posição 3
7. name="explore"   → href={null}       ← sempre por último

Ordem alvo (D-19):
1. name="index"     → title="Ficha"
2. name="magia"     → title="Magia"
3. name="mochila"   → title="Mochila"  ← AQUI
4. name="grimorio"  → title="Grimório"
5. name="regras"    → title="Regras"
6. name="notas"     → title="Notas"
7. name="explore"   → href={null}
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Reposicionar Tabs.Screen mochila para 3ª posição em _layout.tsx</name>
  <files>app/(tabs)/_layout.tsx</files>
  <read_first>app/(tabs)/_layout.tsx</read_first>
  <action>
Editar app/(tabs)/_layout.tsx. A mudança é mover o bloco Tabs.Screen com name="mochila" da posição 6 (após notas) para a posição 3 (após magia, antes de grimorio).

A ordem final dos Tabs.Screen dentro do componente Tabs deve ser exatamente:
1. Tabs.Screen name="index" — Ficha (sem mudança)
2. Tabs.Screen name="magia" — Magia (sem mudança)
3. Tabs.Screen name="mochila" — Mochila (movido de posição 6 para aqui)
4. Tabs.Screen name="grimorio" — Grimório (sem mudança)
5. Tabs.Screen name="regras" — Regras (sem mudança)
6. Tabs.Screen name="notas" — Notas (sem mudança)
7. Tabs.Screen name="explore" options={{ href: null }} (sempre por último, sem mudança)

O bloco mochila a mover é:
```
<Tabs.Screen
  name="mochila"
  options={{
    title: 'Mochila',
    tabBarIcon: ({ color }) => <IconSymbol size={26} name="bag.fill" color={color} />,
  }}
/>
```

Não alterar nenhuma propriedade de nenhum Tabs.Screen — apenas reordenar os blocos.
Não alterar screenOptions nem nenhuma prop do componente Tabs.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    npx tsc --noEmit exit 0.
    grep -n "mochila" app/(tabs)/_layout.tsx retorna linha antes de "grimorio".
    Ordem verificada: index → magia → mochila → grimorio → regras → notas → explore.
  </done>
  <acceptance_criteria>
    1. npx tsc --noEmit sai com exit 0
    2. grep -n "name=" app/(tabs)/_layout.tsx lista as abas na ordem: index, magia, mochila, grimorio, regras, notas, explore
    3. Nenhuma propriedade de nenhuma aba foi alterada (apenas a posição do bloco mochila)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tab layout → router | Ordem de Tabs.Screen define ordem de renderização e índice das abas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma instalação de pacote nesta fase |
</threat_model>

<verification>
## Verificação Manual

Iniciar o app. A tab bar exibe na ordem: Ficha | Magia | Mochila | Grimório | Regras | Notas.
Tocar Mochila navega corretamente para a tela de inventário e equipamentos.
As demais abas continuam funcionando sem crash.
</verification>

<success_criteria>
1. npx tsc --noEmit exit 0
2. Tab bar exibe Mochila em 3ª posição visualmente
3. Nenhuma aba existente deixa de funcionar
</success_criteria>

<output>
Criar `.planning/phases/14-inventario-craft-artefatos/14-02-SUMMARY.md` ao concluir.
</output>
