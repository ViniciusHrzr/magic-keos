---
phase: 14-inventario-craft-artefatos
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - types/inventory.ts
  - types/character.ts
  - store/CharacterContext.tsx
autonomous: true
requirements: [INV-01, INV-02]

must_haves:
  truths:
    - "IQuickNote e IStructuredGear existem como interfaces TypeScript exportadas"
    - "type InventoryItem = IQuickNote | IStructuredGear é a union discriminada por type: 'note' | 'gear'"
    - "Character.inventarioItems: InventoryItem[] substitui inventarioSlots: string[]"
    - "migrate() converte inventarioSlots string[] existente para InventoryItem[] sem perda de dados"
    - "CharacterContext expõe addInventarioItem, removeInventarioItem, updateInventarioItem"
    - "setInventarioSlot preservado e funcional — será removido em Plan 04 junto com a grid antiga"
  artifacts:
    - path: "types/inventory.ts"
      provides: "IQuickNote, IStructuredGear, InventoryItem — contratos de tipo"
      exports: ["IQuickNote", "IStructuredGear", "InventoryItem"]
    - path: "types/character.ts"
      provides: "Character interface atualizada com inventarioItems"
      contains: "inventarioItems"
    - path: "store/CharacterContext.tsx"
      provides: "migrate() guard C + novos setters de inventário"
      contains: "addInventarioItem"
  key_links:
    - from: "store/CharacterContext.tsx"
      to: "types/inventory.ts"
      via: "import { InventoryItem } from '@/types/inventory'"
      pattern: "InventoryItem"
    - from: "store/CharacterContext.tsx"
      to: "types/character.ts"
      via: "import { Character } from '@/types/character'"
      pattern: "inventarioItems"
---

<objective>
Migrar o schema de inventário de string[] (20 slots fixos) para InventoryItem[] (lista FlatList híbrida com union discriminada), conforme D-12–D-15. Criar o arquivo de tipos, atualizar Character, adicionar guard na migrate() e expor os três novos setters no CharacterContext.

Purpose: Habilitar cards tipados (IQuickNote para itens simples, IStructuredGear para equipamentos com stats) necessários para os planos de UI hexagonal e drag-and-drop.
Output: types/inventory.ts (novo), types/character.ts (atualizado), store/CharacterContext.tsx (migrate guard C + novos setters).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-inventario-craft-artefatos/14-01-SUMMARY.md
</context>

<interfaces>
<!-- Contratos extraídos do codebase existente -->

De types/character.ts (estado atual):
```typescript
export interface Character {
  // ...
  inventarioSlots: string[];  // SERÁ SUBSTITUÍDO por inventarioItems: InventoryItem[]
  // ...
}
export const defaultCharacter: Character = {
  // ...
  inventarioSlots: Array(20).fill('') as string[],
  // ...
}
```

De store/CharacterContext.tsx — migrate() existente (linhas 13-47):
```typescript
function migrate(raw: any): Character {
  // Guard A: mana number → { base, total }
  // Guard B: magicasReceitas string → array / inventario string → inventarioSlots[0]
  // Guard C (a ADICIONAR): inventarioSlots string[] → inventarioItems InventoryItem[]
  return { ...defaultCharacter, ...parsed };
}
```

Setters existentes a PRESERVAR (não remover — mochila.tsx ainda usa setInventarioSlot até Plan 05):
```typescript
setInventarioSlot: (idx: number, v: string) => void;
```

Novos setters a ADICIONAR ao contexto (D-12):
```typescript
addInventarioItem: (item: InventoryItem) => void;
removeInventarioItem: (id: string) => void;
updateInventarioItem: (id: string, patch: Partial<InventoryItem>) => void;
```

IDs únicos para InventoryItem: usar Date.now().toString(36) + Math.random().toString(36).slice(2,5) — mesmo padrão do genId() existente no CharacterContext.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Criar types/inventory.ts com IQuickNote, IStructuredGear e InventoryItem</name>
  <files>types/inventory.ts</files>
  <read_first>types/character.ts</read_first>
  <action>
Criar o arquivo types/inventory.ts com as seguintes declarações (D-13):

Tipo DieColor (cor de afinidade) — importar de types/character.ts:
  import { type DieColor } from '@/types/character';

Interface IQuickNote (item simples — nota rápida):
  export interface IQuickNote {
    id: string;                    // UUID gerado em addInventarioItem
    type: 'note';                  // discriminante
    text: string;                  // texto do item (ex: "Poção Menor")
    qty: number;                   // quantidade (default 1)
  }

Interface IStructuredGear (equipamento estruturado com stats):
  export interface IStructuredGear {
    id: string;                    // UUID gerado em addInventarioItem
    type: 'gear';                  // discriminante
    name: string;                  // nome do item (ex: "Espada Longa")
    type_equip: 'arma' | 'escudo' | 'vestimenta' | 'acessorio' | 'outro';
    damage?: string;               // ex: "1d6+2" (para armas)
    defense?: string;              // ex: "IP Corp 2" (para escudos/vestimentas)
    affinity?: DieColor;           // cor de afinidade mágica do item
    melhorias: string[];           // labels de melhorias aplicadas (mesmo formato de EquipItem)
    lore?: string;                 // texto colapsável de regras/lore
  }

Union discriminada:
  export type InventoryItem = IQuickNote | IStructuredGear;

Type guard functions:
  export function isQuickNote(item: InventoryItem): item is IQuickNote {
    return item.type === 'note';
  }
  export function isStructuredGear(item: InventoryItem): item is IStructuredGear {
    return item.type === 'gear';
  }

Nenhuma outra dependência. Arquivo puro de tipos — sem imports de React ou RN.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    npx tsc --noEmit exit 0.
    grep -c "IQuickNote" types/inventory.ts retorna >= 2 (interface + type guard).
    grep -c "IStructuredGear" types/inventory.ts retorna >= 2.
    grep -c "InventoryItem" types/inventory.ts retorna >= 2 (union + tipo de retorno).
  </done>
  <acceptance_criteria>
    1. npx tsc --noEmit exit 0
    2. types/inventory.ts existe e exporta IQuickNote, IStructuredGear, InventoryItem, isQuickNote, isStructuredGear
    3. grep -c "type: 'note'" types/inventory.ts retorna >= 1
    4. grep -c "type: 'gear'" types/inventory.ts retorna >= 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Atualizar types/character.ts e store/CharacterContext.tsx com nova schema e migrate guard</name>
  <files>types/character.ts, store/CharacterContext.tsx</files>
  <read_first>types/character.ts, store/CharacterContext.tsx, types/inventory.ts</read_first>
  <action>
PARTE A — types/character.ts:

1. Adicionar import no topo do arquivo:
   import { InventoryItem } from '@/types/inventory';

2. Na interface Character, adicionar inventarioItems SEM remover inventarioSlots (manter como deprecated opcional):
   inventarioSlots?: string[];  // deprecated — mantido para migrate() guard; removido em Plan 04
   inventarioItems: InventoryItem[];

3. No defaultCharacter, adicionar inventarioItems (manter inventarioSlots como ausente — undefined é ok para campo opcional):
   inventarioItems: [],

ATENÇÃO: NÃO remover inventarioSlots da interface Character — torná-lo opcional (?). Isso mantém mochila.tsx compilando até Plan 04 substituir a grid. A migrate() verifica a presença de inventarioSlots para converter dados legados.

---

PARTE B — store/CharacterContext.tsx:

1. Adicionar import:
   import { InventoryItem } from '@/types/inventory';

2. Na função migrate(), após o último guard existente (migração de equipamentos, linha ~45) e antes do return, adicionar Guard C:

   // Guard C: inventarioSlots string[] → inventarioItems InventoryItem[]
   if (Array.isArray((parsed as any).inventarioSlots) && !Array.isArray(parsed.inventarioItems)) {
     const slots: string[] = (parsed as any).inventarioSlots;
     parsed.inventarioItems = slots
       .filter((s: string) => typeof s === 'string' && s.trim() !== '')
       .map((s: string) => ({
         id: Date.now().toString(36) + Math.random().toString(36).slice(2, 5) + Math.random().toString(36).slice(2, 4),
         type: 'note' as const,
         text: s.trim(),
         qty: 1,
       }));
     delete (parsed as any).inventarioSlots;
   }
   if (!Array.isArray(parsed.inventarioItems)) {
     parsed.inventarioItems = [];
   }

3. Na interface CharacterContextType, após setInventarioSlot, adicionar os três novos setters:
   addInventarioItem: (item: InventoryItem) => void;
   removeInventarioItem: (id: string) => void;
   updateInventarioItem: (id: string, patch: Partial<InventoryItem>) => void;

   MANTER setInventarioSlot na interface por compatibilidade com mochila.tsx (Plan 05 vai substituí-lo).

4. Dentro do CharacterProvider, após o setInventarioSlot useCallback existente, adicionar as três implementações:

   const addInventarioItem = useCallback((item: InventoryItem) =>
     update(p => ({ ...p, inventarioItems: [...(p.inventarioItems ?? []), item] })), [update]);

   const removeInventarioItem = useCallback((id: string) =>
     update(p => ({ ...p, inventarioItems: (p.inventarioItems ?? []).filter(i => i.id !== id) })), [update]);

   const updateInventarioItem = useCallback((id: string, patch: Partial<InventoryItem>) =>
     update(p => ({
       ...p,
       inventarioItems: (p.inventarioItems ?? []).map(i =>
         i.id === id ? { ...i, ...patch } as InventoryItem : i
       ),
     })), [update]);

5. Adicionar os três novos setters ao contextValue useMemo (tanto no objeto quanto nos deps):
   addInventarioItem, removeInventarioItem, updateInventarioItem

IMPORTANTE: setInventarioSlot existente deve continuar funcionando até Plan 04 substituir a grid. Como inventarioSlots é agora opcional em Character, a implementação existente compila corretamente (o campo pode ser undefined mas o spread é seguro). NÃO converter para no-op — mochila.tsx do Plan 01 ainda usa este setter para persistir dados até ser substituído pelo Plan 04.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    npx tsc --noEmit exit 0.
    grep -c "inventarioItems" types/character.ts retorna >= 2 (interface + defaultCharacter).
    grep -c "inventarioItems" store/CharacterContext.tsx retorna >= 3 (Guard C + addInventarioItem + removeInventarioItem).
    grep -c "addInventarioItem" store/CharacterContext.tsx retorna >= 3 (interface + impl + contextValue).
    grep -c "InventoryItem" store/CharacterContext.tsx retorna >= 2 (import + uso em tipos).
  </done>
  <acceptance_criteria>
    1. npx tsc --noEmit exit 0 — sem erros de tipo
    2. grep -c "inventarioItems" types/character.ts retorna >= 2
    3. grep -c "Guard C" store/CharacterContext.tsx retorna >= 1 (comentário)
    4. grep -c "addInventarioItem" store/CharacterContext.tsx retorna >= 3
    5. grep -c "removeInventarioItem" store/CharacterContext.tsx retorna >= 3
    6. grep -c "updateInventarioItem" store/CharacterContext.tsx retorna >= 3
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AsyncStorage → migrate() | Dados legados (inventarioSlots: string[]) lidos de AsyncStorage e convertidos para InventoryItem[] |
| addInventarioItem | item recebe ID gerado localmente — não há input externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Tampering | migrate() Guard C | mitigate | Guard só executa se inventarioSlots existe E inventarioItems não existe — idempotente, não apaga dados já migrados |
| T-14-03-02 | Denial of Service | ID collision em addInventarioItem | accept | Colisão de ID localmente é estatisticamente negligível; app local sem sync multi-dispositivo |
| T-14-SC | Tampering | npm installs | accept | Sem instalação de pacotes |
</threat_model>

<verification>
## Verificação TypeScript

npx tsc --noEmit deve retornar exit 0 após ambas as tasks.

## Verificação de Migração

Lógica do Guard C deve:
- Converter strings não-vazias de inventarioSlots em IQuickNote com text = string.trim(), qty = 1
- Ignorar slots vazios (filter)
- Não executar se inventarioItems já existe (guard duplo)
- Deletar inventarioSlots do parsed object após migração
</verification>

<success_criteria>
1. npx tsc --noEmit exit 0
2. types/inventory.ts exporta IQuickNote, IStructuredGear, InventoryItem, isQuickNote, isStructuredGear
3. Character.inventarioItems: InventoryItem[] em types/character.ts
4. migrate() em CharacterContext.tsx converte inventarioSlots legado sem crash
5. CharacterContext expõe addInventarioItem, removeInventarioItem, updateInventarioItem
</success_criteria>

<output>
Criar `.planning/phases/14-inventario-craft-artefatos/14-03-SUMMARY.md` ao concluir.
</output>
