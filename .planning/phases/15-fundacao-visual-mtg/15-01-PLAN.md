---
phase: 15-fundacao-visual-mtg
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - constants/theme.ts
autonomous: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "@shopify/react-native-skia importável via `import { Canvas, Path, Skia } from '@shopify/react-native-skia'` sem erro TypeScript"
    - "constants/theme.ts contém RPG.branco='#F8F2E2', RPG.verde='#00733E', RPG.vermelho='#D3202A', RPG.preto='#150B00', RPG.azul='#0E68AB', RPG.incolor='#A6ADB5'"
    - "Tokens *Light atualizados para refletir novas cores base (sem regressão visual nos steppers de mana)"
    - "npx tsc --noEmit passa sem erros após as mudanças"
  artifacts:
    - path: "constants/theme.ts"
      provides: "Paleta MTG hex exata nos 6 tokens de mana do objeto RPG"
      contains: "branco: '#F8F2E2'"
    - path: "package.json"
      provides: "Dependência @shopify/react-native-skia registrada"
      contains: "@shopify/react-native-skia"
  key_links:
    - from: "components/rpg/LegendaryFrame.tsx (Plan 02)"
      to: "@shopify/react-native-skia"
      via: "import { Canvas, Path, Skia }"
      pattern: "react-native-skia"
    - from: "constants/theme.ts"
      to: "Todos os consumidores de RPG.*"
      via: "import { RPG } from '@/constants/theme'"
      pattern: "RPG\\.branco|RPG\\.verde|RPG\\.vermelho|RPG\\.preto|RPG\\.azul|RPG\\.incolor"
---

<objective>
Instalar @shopify/react-native-skia via expo install e atualizar os 6 tokens de cor de mana em constants/theme.ts para os valores hexadecimais canônicos do MTG, incluindo ajuste dos tokens *Light correspondentes.

Purpose: Prover as duas fundações que o Plan 02 depende — a biblioteca Skia disponível para import e os tokens de cor MTG corretos consumidos por LegendaryFrame e todos os outros componentes de mana.
Output: package.json com Skia registrado; constants/theme.ts com paleta MTG exata.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-fundacao-visual-mtg/15-RESEARCH.md
@.planning/phases/15-fundacao-visual-mtg/15-PATTERNS.md
@constants/theme.ts
</context>

<interfaces>
<!-- Estado atual de constants/theme.ts (lido 2026-05-17) -->
<!-- Mana block lines 20-33: -->

RPG object mana tokens — BEFORE (valores aproximados, a substituir):
  branco: '#e8e0cc'
  verde: '#3a8a3a'
  vermelho: '#b52020'
  preto: '#2a2030'
  azul: '#1a5ab0'
  brancoLight: '#f5f0e0'
  verdeLight: '#4aaa4a'
  vermelhoLight: '#e03030'
  pretoLight: '#6a5882'
  azulLight: '#2878e0'
  incolor: '#888070'

RPG object mana tokens — AFTER (valores MTG canônicos, alvo desta task):
  branco: '#F8F2E2'
  verde: '#00733E'
  vermelho: '#D3202A'
  preto: '#150B00'
  azul: '#0E68AB'
  brancoLight: '#FBF8F0'   (clarear base branco — era quase igual ao base)
  verdeLight: '#1A9E5A'    (tint do novo verde MTG — mais saturado)
  vermelhoLight: '#E84050' (tint do novo vermelho MTG)
  pretoLight: '#4A3828'    (tint do novo preto — sair do roxo para marrom escuro)
  azulLight: '#2E8FD0'     (tint do novo azul MTG)
  incolor: '#A6ADB5'       (pedra cinza MTG — substituição direta, sem Light variant existente)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Instalar @shopify/react-native-skia via expo install</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (versão atual do projeto, lista de dependências existentes — confirmar que react-native-reanimated ~4.1.1 já está presente como peer dep requerida pelo Skia)
    - .planning/phases/15-fundacao-visual-mtg/15-RESEARCH.md (Package Legitimacy Audit — confirmar Skia aprovado [OK]; seção Common Pitfalls 2 — SDK 54 issue auto-resolvido no 2.6.2)
  </read_first>
  <action>
    Executar `npx expo install @shopify/react-native-skia` na raiz do projeto. O comando determina automaticamente a versão compatível com Expo SDK 54 e registra a entrada em `package.json` com prefixo `~` (ex.: `"@shopify/react-native-skia": "~2.6.2"`). Não editar package.json manualmente.

    Verificar após instalação: `npm view @shopify/react-native-skia version` deve retornar 2.6.2 ou superior.

    Não é necessário nenhuma alteração em metro.config.js, app.json ou babel.config.js — o SDK 54 import-ordering issue foi auto-corrigido no pacote Skia 2.6.2+ (per RESEARCH.md Pitfall 2 e github.com/expo/expo/issues/39277 status "outdated").

    Não é necessário registrar plugin em app.json — Skia está incluído no Expo Go para SDK 54 (per RESEARCH.md Environment Availability).
  </action>
  <verify>
    <automated>node -e "require('./node_modules/@shopify/react-native-skia/package.json')" && echo "OK" && npx tsc --noEmit</automated>
  </verify>
  <done>package.json lista "@shopify/react-native-skia" em dependencies; node_modules/@shopify/react-native-skia/package.json existe; npx tsc --noEmit passa (nenhum novo erro de tipo)</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar paleta MTG hex em constants/theme.ts</name>
  <files>constants/theme.ts</files>
  <read_first>
    - constants/theme.ts (estado atual completo — ler inteiro, confirmar linha exata de cada token antes de editar)
    - .planning/phases/15-fundacao-visual-mtg/15-PATTERNS.md (seção "constants/theme.ts" — delta exato dos 6 tokens base + análise de downstream consumers)
    - .planning/phases/15-fundacao-visual-mtg/15-RESEARCH.md (Pitfall 3 — RPG.preto='#150B00' é quase-preto; usar apenas como accent/border, não como text color em superfícies escuras; Pitfall 5 — *Light variants precisam de ajuste visual)
  </read_first>
  <action>
    Editar constants/theme.ts substituindo os 11 tokens de mana dentro do objeto RPG pelos valores abaixo. As demais keys do objeto RPG (bg, surface, border, gold, text, etc.) NÃO devem ser alteradas.

    Tokens a substituir — valores EXATOS (maiúsculas/minúsculas preservadas como indicado):

    Tokens base MTG canônicos:
      branco: '#F8F2E2'
      verde: '#00733E'
      vermelho: '#D3202A'
      preto: '#150B00'
      azul: '#0E68AB'
      incolor: '#A6ADB5'

    Tokens *Light (tints para steppers de mana em index.tsx) — ajustados para novas bases:
      brancoLight: '#FBF8F0'
      verdeLight: '#1A9E5A'
      vermelhoLight: '#E84050'
      pretoLight: '#4A3828'
      azulLight: '#2E8FD0'

    Regra de contraste (RESEARCH.md Pitfall 3): RPG.preto='#150B00' é quase-preto — não introduzir nenhum uso de RPG.preto como cor de texto em superfícies escuras. Esta task apenas atualiza o valor; nenhum novo uso é criado aqui.

    Estrutura do objeto RPG e export não mudam — é uma substituição de valores in-place.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    constants/theme.ts contém exatamente:
      branco: '#F8F2E2'
      verde: '#00733E'
      vermelho: '#D3202A'
      preto: '#150B00'
      azul: '#0E68AB'
      incolor: '#A6ADB5'
    Verificável via: grep -c "F8F2E2\|00733E\|D3202A\|150B00\|0E68AB\|A6ADB5" constants/theme.ts — deve retornar 6.
    npx tsc --noEmit e npx expo lint passam sem novos erros.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | Pacote baixado de registry público durante install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | @shopify/react-native-skia postinstall script (node scripts/install-libs.js) | accept | Shopify-maintained, ~4 anos no registry, slopcheck [OK], mecanismo oficial de distribuição de binários C++ pré-compilados (citado RESEARCH.md Security Domain) |
| T-15-SC | Tampering | npm install | mitigate | Package Legitimacy Audit completo em RESEARCH.md — @shopify/react-native-skia aprovado [OK]; nenhum pacote [ASSUMED] ou [SUS] nesta task |
</threat_model>

<verification>
Após concluir ambas as tasks:
1. `npx tsc --noEmit` — zero erros
2. `npx expo lint` — zero erros
3. `grep "F8F2E2" constants/theme.ts` — retorna a linha com branco
4. `grep "react-native-skia" package.json` — retorna a linha com a dependência
</verification>

<success_criteria>
- @shopify/react-native-skia registrado em package.json e instalado em node_modules
- constants/theme.ts contém os 6 tokens MTG canônicos com os hex exatos especificados
- Tokens *Light atualizados para tints coerentes com as novas bases
- npx tsc --noEmit passa sem erros
- Nenhum arquivo além de package.json, package-lock.json e constants/theme.ts é modificado
</success_criteria>

<output>
Criar `.planning/phases/15-fundacao-visual-mtg/15-01-SUMMARY.md` quando concluído.
</output>
