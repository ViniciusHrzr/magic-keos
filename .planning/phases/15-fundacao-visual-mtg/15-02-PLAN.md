---
phase: 15-fundacao-visual-mtg
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - components/rpg/LegendaryFrame.tsx
  - app/(tabs)/index.tsx
autonomous: true
requirements:
  - UI-03

must_haves:
  truths:
    - "LegendaryFrame renderiza no header de index.tsx com borda chanfrada (8 segmentos em 45°) visível via Skia Path"
    - "Nome do personagem editável via TextInput dentro do frame (placeholder 'Nome do personagem')"
    - "Sabedoria Acumulada exibida como 'SAB: {n}' dentro do frame"
    - "Botão 'Fichas' preservado e funcional no canto direito do frame"
    - "Canvas nunca renderiza com width=0 (onLayout guard ativo)"
    - "npx tsc --noEmit passa sem erros após integração"
    - "index.tsx não contém mais os styles titleBar, titleRow, gameTitle, fichasBtn, fichasBtnText, nameInput (movidos para LegendaryFrame.tsx)"
  artifacts:
    - path: "components/rpg/LegendaryFrame.tsx"
      provides: "Componente Skia com chamfer border, TextInput nome, Text sabedoria, botão Fichas"
      exports: ["LegendaryFrameProps", "default (React.memo(LegendaryFrame))"]
    - path: "app/(tabs)/index.tsx"
      provides: "Header substituído — LegendaryFrame no lugar do bloco titleBar"
      contains: "LegendaryFrame"
  key_links:
    - from: "app/(tabs)/index.tsx"
      to: "components/rpg/LegendaryFrame.tsx"
      via: "import LegendaryFrame from '@/components/rpg/LegendaryFrame'"
      pattern: "LegendaryFrame"
    - from: "LegendaryFrame.tsx Canvas"
      to: "@shopify/react-native-skia"
      via: "import { Canvas, Path, Skia } from '@shopify/react-native-skia'"
      pattern: "Canvas|Path|Skia"
---

<objective>
Criar components/rpg/LegendaryFrame.tsx como componente Skia com borda chanfrada MTG e integrá-lo em app/(tabs)/index.tsx substituindo o bloco titleBar existente.

Purpose: Entregar UI-03 — o header do personagem com identidade visual MTG (Legendary Frame), preservando toda a funcionalidade existente (nome editável, botão Fichas) e acrescentando sabedoria Acumulada no frame.
Output: LegendaryFrame.tsx funcional + index.tsx atualizado sem os styles e JSX do titleBar antigo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-fundacao-visual-mtg/15-RESEARCH.md
@.planning/phases/15-fundacao-visual-mtg/15-PATTERNS.md
@.planning/phases/15-fundacao-visual-mtg/15-01-SUMMARY.md
@components/rpg/HexSlot.tsx
@app/(tabs)/index.tsx
@constants/theme.ts
</context>

<interfaces>
<!-- Contrato completo de LegendaryFrameProps a criar em Task 1 -->

Interface exportada que index.tsx usará em Task 2:

  export interface LegendaryFrameProps {
    nome: string;
    onNomeChange: (v: string) => void;
    sabedoriaAcumulada: number;
    onFichasPress: () => void;
    width: number;   // medido via onLayout em index.tsx — evita Canvas com size zero
  }

Função utilitária interna (não exportada):

  function makeChamferPath(w: number, h: number, c: number): SkiaPath
  // retorna Skia.Path.Make() com 8 segmentos lineTo (chanfrado 45°)
  // CHAMFER = 12 (constante de módulo)
  // FRAME_HEIGHT = 80 (constante de módulo)

Exports do módulo:
  export interface LegendaryFrameProps { ... }
  export default React.memo(LegendaryFrame)

<!-- Imports necessários de index.tsx (estado pós-Plan 01): -->

  import LegendaryFrame from '@/components/rpg/LegendaryFrame';  // adicionar
  // const [frameWidth, setFrameWidth] = React.useState(0);       // adicionar

<!-- Fonte de dados que index.tsx passa para o componente: -->
  c.nome              → prop nome
  setNome             → prop onNomeChange
  c.sabedoria.acumulada → prop sabedoriaAcumulada
  () => setShowManager(true) → prop onFichasPress

<!-- Bloco JSX a substituir em index.tsx (linhas 39-54 atuais): -->
  View style={styles.titleBar} contendo titleRow + TextInput nome → substituído por wrapper View com onLayout + LegendaryFrame condicional

<!-- Styles a remover de index.tsx após extração (linhas 278-326): -->
  titleBar, titleRow, gameTitle, fichasBtn, fichasBtnText, nameInput
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Criar components/rpg/LegendaryFrame.tsx</name>
  <files>components/rpg/LegendaryFrame.tsx</files>
  <read_first>
    - components/rpg/HexSlot.tsx (padrão de import, interface Props exportada, StyleSheet no final, React.memo export — linhas 1-34 e linha 295)
    - .planning/phases/15-fundacao-visual-mtg/15-PATTERNS.md (seção "LegendaryFrame.tsx" — estrutura completa: imports, interface, makeChamferPath, JSX Canvas+overlay, StyleSheet values copiados de index.tsx)
    - .planning/phases/15-fundacao-visual-mtg/15-RESEARCH.md (Pitfall 1 — usar Skia.Path.Make() não Skia.PathBuilder; Pitfall 4 — Canvas nunca com width=0; Pattern 3 — Canvas+absoluteFill overlay; Pattern 1 — makeChamferPath código verificado)
    - constants/theme.ts (tokens RPG disponíveis após Plan 01 — RPG.headerBg, RPG.gold, RPG.text, RPG.textDark, RPG.borderLight, RPG.textMuted)
  </read_first>
  <action>
    Criar components/rpg/LegendaryFrame.tsx do zero seguindo o template 5-pontos de components/rpg/:

    1. IMPORTS (grupo React/RN, depois Skia, depois @/constants):
       - React from 'react'
       - View, Text, TextInput, StyleSheet, TouchableOpacity from 'react-native'
       - Canvas, Path, Skia from '@shopify/react-native-skia'
       - RPG from '@/constants/theme'

    2. CONSTANTES DE MÓDULO (antes do componente, não em StyleSheet):
       - const FRAME_HEIGHT = 80
       - const CHAMFER = 12

    3. FUNÇÃO HELPER (antes do componente):
       makeChamferPath(w, h, c) que usa Skia.Path.Make() com sequência:
       moveTo(c, 0) → lineTo(w-c, 0) → lineTo(w, c) → lineTo(w, h-c) → lineTo(w-c, h) → lineTo(c, h) → lineTo(0, h-c) → lineTo(0, c) → close()
       ATENÇÃO: usar Skia.Path.Make() — NÃO Skia.PathBuilder (Pitfall 1 do RESEARCH.md).

    4. INTERFACE (exportada):
       export interface LegendaryFrameProps {
         nome: string;
         onNomeChange: (v: string) => void;
         sabedoriaAcumulada: number;
         onFichasPress: () => void;
         width: number;
       }
       Nomes de props em camelCase inglês (padrão do projeto para props de componente).

    5. COMPONENTE function LegendaryFrame(props: LegendaryFrameProps):
       - Desestruturar todas as props
       - JSX:
         View com height=FRAME_HEIGHT e position='relative' (wrapper)
           Canvas com style=StyleSheet.absoluteFill
             Path fill: path=makeChamferPath(width, FRAME_HEIGHT, CHAMFER), color=RPG.headerBg, style="fill"
             Path stroke: mesmo path, color=RPG.gold, style="stroke", strokeWidth={2}
           View com [StyleSheet.absoluteFill, styles.overlay]
             View com styles.titleRow
               Text "Magic Kéos" com styles.gameTitle
               TouchableOpacity onPress=onFichasPress style=styles.fichasBtn activeOpacity=0.7
                 Text "Fichas" com styles.fichasBtnText
             TextInput
               style=styles.nameInput
               value=nome
               onChangeText=onNomeChange
               placeholder="Nome do personagem"
               placeholderTextColor=RPG.textDark
             Text "{`SAB: ${sabedoriaAcumulada}`}" com styles.sabedoriaText

       IMPORTANTE — sabedoria: exibir apenas sabedoriaAcumulada nesta fase (dual counter Acumulada/Disponível é escopo da Phase 17). Label: "SAB: {n}".

    6. STYLESHEET (ao final do arquivo, após o componente):
       const styles = StyleSheet.create({
         overlay: { flexDirection: 'column', alignItems: 'center', justifyContent: 'center', paddingHorizontal: 12, gap: 4 },
         titleRow: { width: '100%', alignItems: 'center', justifyContent: 'center' },
         gameTitle: { textAlign: 'center', color: RPG.gold, fontSize: 20, fontFamily: 'serif', fontWeight: 'bold', letterSpacing: 3, textTransform: 'uppercase' },
         fichasBtn: { position: 'absolute', right: 0, paddingHorizontal: 10, paddingVertical: 4, borderWidth: 1, borderColor: RPG.gold, borderRadius: 4 },
         fichasBtnText: { color: RPG.gold, fontSize: 11, fontWeight: '700', letterSpacing: 0.5 },
         nameInput: { color: RPG.text, fontSize: 16, borderBottomWidth: 1, borderBottomColor: RPG.borderLight, paddingVertical: 4, paddingHorizontal: 8, minWidth: 200, textAlign: 'center', fontFamily: 'serif', fontStyle: 'italic' },
         sabedoriaText: { color: RPG.textMuted, fontSize: 11, letterSpacing: 0.5 },
       })
       Valores copiados do bloco titleBar de index.tsx (linhas 278-326) — sem alterar nenhum valor numérico ou de cor para preservar continuidade visual.

    7. EXPORT:
       export default React.memo(LegendaryFrame)

    TypeScript strict: sem `any`, sem tipos implícitos. SkiaPath type vem de '@shopify/react-native-skia' — se necessário para tipagem do retorno de makeChamferPath, importar SkPath (ou usar inferência de tipo pois Skia.Path.Make() retorna tipo inferido — preferir inferência para não sobrecomplicar).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    - components/rpg/LegendaryFrame.tsx existe com export default React.memo(LegendaryFrame)
    - Arquivo exporta interface LegendaryFrameProps com os 5 props: nome, onNomeChange, sabedoriaAcumulada, onFichasPress, width
    - Importa Canvas, Path, Skia de '@shopify/react-native-skia' sem erro TS
    - makeChamferPath usa Skia.Path.Make() (não Skia.PathBuilder)
    - npx tsc --noEmit passa sem erros
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrar LegendaryFrame em app/(tabs)/index.tsx e limpar styles antigos</name>
  <files>app/(tabs)/index.tsx</files>
  <read_first>
    - app/(tabs)/index.tsx (ler inteiro — confirmar estado atual das linhas 1-60 JSX header e linhas 270-330 StyleSheet antes de editar; verificar que frameWidth state ainda não existe)
    - .planning/phases/15-fundacao-visual-mtg/15-PATTERNS.md (seção "app/(tabs)/index.tsx" — import a adicionar, state frameWidth, replacement pattern do titleBar, styles a remover)
    - components/rpg/LegendaryFrame.tsx (Task 1 output — confirmar interface LegendaryFrameProps para passar props corretas)
  </read_first>
  <action>
    Fazer 4 edições cirúrgicas em app/(tabs)/index.tsx:

    EDIÇÃO 1 — Adicionar import do LegendaryFrame após o bloco de imports existente (linha ~19, após o último import atual):
      import LegendaryFrame from '@/components/rpg/LegendaryFrame';

    EDIÇÃO 2 — Adicionar estado frameWidth após a linha `const [showManager, setShowManager] = useState(false)` (linha ~22):
      const [frameWidth, setFrameWidth] = React.useState(0);

    EDIÇÃO 3 — Substituir o bloco header (linhas 39-54) pelo wrapper com onLayout + LegendaryFrame condicional:
      {/* ── HEADER ── */}
      <View onLayout={e => setFrameWidth(e.nativeEvent.layout.width)}>
        {frameWidth > 0 && (
          <LegendaryFrame
            nome={c.nome}
            onNomeChange={setNome}
            sabedoriaAcumulada={c.sabedoria.acumulada}
            onFichasPress={() => setShowManager(true)}
            width={frameWidth}
          />
        )}
      </View>

    EDIÇÃO 4 — Remover do StyleSheet.create as 6 keys que foram movidas para LegendaryFrame.tsx:
      titleBar, titleRow, gameTitle, fichasBtn, fichasBtnText, nameInput
      (estas keys estão nas linhas 278-326 do arquivo original)

    VERIFICAÇÃO DE SEGURANÇA antes de remover cada style: confirmar via grep que a key removida não aparece em nenhum outro lugar no JSX de index.tsx além do bloco header já substituído. Text, TextInput e TouchableOpacity permanecem importados pois são usados em outros lugares na tela (seções de mana, vida, etc.) — NÃO remover esses imports.

    ATENÇÃO — sabedoria: passar c.sabedoria.acumulada (não c.sabedoria.disponivel e não o objeto inteiro). O dual counter é Phase 17.

    ATENÇÃO — React.useState vs useState: index.tsx usa `import React, { useState } from 'react'` na linha 1. Para consistência, adicionar o state frameWidth usando `useState(0)` (já desestruturado), não `React.useState(0)`. Verificar a forma de import existente antes de escrever.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx expo lint</automated>
  </verify>
  <done>
    - app/(tabs)/index.tsx importa LegendaryFrame de '@/components/rpg/LegendaryFrame'
    - Bloco {/* ── HEADER ── */} usa View com onLayout + LegendaryFrame condicional (frameWidth > 0)
    - styles.titleBar, styles.titleRow, styles.gameTitle, styles.fichasBtn, styles.fichasBtnText, styles.nameInput não existem mais no StyleSheet de index.tsx
    - npx tsc --noEmit e npx expo lint passam sem erros
    - Props passadas para LegendaryFrame: nome=c.nome, onNomeChange=setNome, sabedoriaAcumulada=c.sabedoria.acumulada, onFichasPress={() => setShowManager(true)}, width=frameWidth
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>LegendaryFrame integrado ao header da Ficha. Canvas Skia renderiza fundo escuro (RPG.headerBg) + borda chanfrada dourada (RPG.gold), com TextInput de nome do personagem, label "SAB: {n}" e botão "Fichas" no canto direito sobrepostos via absoluteFill.</what-built>
  <how-to-verify>
    1. Abrir o app no Expo Go (iOS ou Android)
    2. Navegar para a aba Ficha (primeira aba)
    3. Verificar:
       a. Header exibe "MAGIC KÉOS" em texto dourado centralizado
       b. Borda chanfrada (8 segmentos retos com cortes diagonais nos 4 cantos) é visível em dourado
       c. Campo de nome do personagem está editável (tocar → teclado abre)
       d. Label "SAB: {n}" aparece abaixo do nome com o valor atual de sabedoria acumulada
       e. Botão "Fichas" no canto direito superior abre o CharacterManager ao tocar
       f. Nenhum crash ou tela branca
       g. Layout abaixo do header (Sabedoria steppers, etc.) permanece inalterado
    4. Se o header aparecer como tela vazia por um frame antes de renderizar: isso é esperado (frameWidth=0 guard) e correto
  </how-to-verify>
  <resume-signal>Digite "aprovado" se o LegendaryFrame estiver visível e funcional, ou descreva qualquer problema visual ou crash observado</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TextInput (nome) → CharacterContext | Input do usuário passa pelo handler setNome (existente, sem mudanças) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Tampering | LegendaryFrame TextInput (nome) | accept | Campo nome existia antes — mesmo handler setNome, sem nova superfície de ataque; nenhum dado de rede envolvido |
| T-15-03 | Denial of Service | Skia Canvas com width=0 | mitigate | onLayout guard: `{frameWidth > 0 && <LegendaryFrame width={frameWidth} .../>}` — Canvas nunca renderiza sem dimensão válida (per RESEARCH.md Pitfall 4) |
</threat_model>

<verification>
Após concluir as 3 tasks (incluindo checkpoint aprovado):
1. `npx tsc --noEmit` — zero erros
2. `npx expo lint` — zero erros
3. `grep "LegendaryFrame" app/(tabs)/index.tsx` — retorna import + JSX (2+ linhas)
4. `grep "titleBar" app/(tabs)/index.tsx` — zero resultados (style removido)
5. `grep "Canvas\|Path\|Skia" components/rpg/LegendaryFrame.tsx` — retorna linhas de import e uso
6. Verificação visual no Expo Go: borda chanfrada visível, nome editável, sabedoria exibida, botão Fichas funcional
</verification>

<success_criteria>
- components/rpg/LegendaryFrame.tsx criado com Canvas Skia + chamfer border + overlay com nome/sabedoria/Fichas
- app/(tabs)/index.tsx usa LegendaryFrame no header sem os styles antigos do titleBar
- Nenhum style dead code (titleBar, titleRow, gameTitle, fichasBtn, fichasBtnText, nameInput) permanece em index.tsx
- npx tsc --noEmit e npx expo lint passam sem erros
- LegendaryFrame aprovado em inspeção visual no Expo Go (checkpoint)
</success_criteria>

<output>
Criar `.planning/phases/15-fundacao-visual-mtg/15-02-SUMMARY.md` quando concluído.
</output>
