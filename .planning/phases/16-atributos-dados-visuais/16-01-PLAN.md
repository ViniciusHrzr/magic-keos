---
phase: 16-atributos-dados-visuais
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/rpg/DiceTrack.tsx
  - app/(tabs)/index.tsx
autonomous: true
requirements:
  - ATTR-01
  - ATTR-03

must_haves:
  truths:
    - "DiceTrack exibe ícone de dado (d4/d6/d8/d10/d12) baseado na contagem de bubbles preenchidas"
    - "InstanceBlock tem visual de card MTG: fundo RPG.surface, borda RPG.goldDim, marginHorizontal 8, marginVertical 6, borderRadius 4"
    - "npx tsc --noEmit passa sem erros"
    - "InstanceBlock com 0 dados preenchidos não exibe ícone de dado (count=0 → sem label)"
  artifacts:
    - path: "components/rpg/DiceTrack.tsx"
      provides: "DiceIcon badge (d4/d6/d8/d10/d12) próximo ao label do atributo"
      contains: "dieLabel"
    - path: "app/(tabs)/index.tsx"
      provides: "InstanceBlock com estilo card MTG"
      contains: "instanceBlock"
  key_links:
    - from: "DiceTrack count"
      to: "dieLabel map"
      via: "COUNT_TO_DIE[count]"
      pattern: "d4|d6|d8|d10|d12"
---

<objective>
Atualizar DiceTrack para exibir o tipo de dado (d4–d12) baseado na quantidade de bubbles preenchidas, e redesenhar o InstanceBlock em index.tsx com estética de card MTG.

Purpose: ATTR-01 — dado visual por atributo; ATTR-03 — cards das instâncias com visual MTG.
Output: DiceTrack.tsx atualizado + estilos instanceBlock atualizados em index.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@constants/theme.ts
@components/rpg/DiceTrack.tsx
@app/(tabs)/index.tsx
</context>

<interfaces>
<!-- Mapeamento count → tipo de dado (lógica do jogo: 1 dado = d4, ..., 5 dados = d12) -->
COUNT_TO_DIE:
  0: null  (sem badge — atributo sem dados equipados)
  1: 'd4'
  2: 'd6'
  3: 'd8'
  4: 'd10'
  5: 'd12'

<!-- DiceTrack layout após mudança: -->
[label (104px)] [track: DieBubbles...] [count] [dieBadge: "d12"]

<!-- InstanceBlock card MTG — styles a atualizar em index.tsx: -->
  instanceBlock:
    backgroundColor: RPG.surface
    borderWidth: 1
    borderColor: RPG.goldDim
    borderRadius: 4
    marginHorizontal: 8
    marginVertical: 6
    overflow: 'hidden'    (clip filhos dentro do borderRadius)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar badge de tipo de dado em DiceTrack.tsx</name>
  <files>components/rpg/DiceTrack.tsx</files>
  <read_first>
    - components/rpg/DiceTrack.tsx (ler completo — confirmar estrutura atual: label, track, count)
    - constants/theme.ts (RPG.gold, RPG.goldDim, RPG.bg — cores para o badge)
  </read_first>
  <action>
    Adicionar ao componente DiceTrack:

    1. Constante de módulo (antes do componente):
       const COUNT_TO_DIE: Record<number, string | null> = { 0: null, 1: 'd4', 2: 'd6', 3: 'd8', 4: 'd10', 5: 'd12' };

    2. No componente, computar:
       const dieLabel = COUNT_TO_DIE[count] ?? null;

    3. Na JSX, após o `<Text style={styles.count}>{count}</Text>`:
       {dieLabel && (
         <View style={styles.dieBadge}>
           <Text style={styles.dieBadgeText}>{dieLabel}</Text>
         </View>
       )}

    4. No StyleSheet, adicionar:
       dieBadge: {
         marginLeft: 4,
         backgroundColor: RPG.goldDim,
         borderRadius: 3,
         paddingHorizontal: 4,
         paddingVertical: 1,
       },
       dieBadgeText: {
         color: RPG.text,
         fontSize: 10,
         fontWeight: 'bold',
         fontFamily: 'serif',
         letterSpacing: 0.5,
       },

    Não alterar: label, track (DieBubbles), count text, espaçamentos existentes.
    Badge só aparece quando count > 0 (atributo tem pelo menos um dado equipado).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    - components/rpg/DiceTrack.tsx contém COUNT_TO_DIE e dieBadge style
    - Badge "d4"/"d6"/"d8"/"d10"/"d12" renderiza apenas quando count > 0
    - npx tsc --noEmit passa
  </done>
</task>

<task type="auto">
  <name>Task 2: Redesenhar InstanceBlock com estética MTG card em index.tsx</name>
  <files>app/(tabs)/index.tsx</files>
  <read_first>
    - app/(tabs)/index.tsx (ler apenas a seção StyleSheet — confirmar `instanceBlock` e `instanceContent` atuais, linhas ~270+)
    - constants/theme.ts (RPG.surface, RPG.goldDim, RPG.surfaceAlt — disponíveis)
  </read_first>
  <action>
    Localizar `instanceBlock` no StyleSheet.create de index.tsx e substituir seu valor por:

    instanceBlock: {
      backgroundColor: RPG.surface,
      borderWidth: 1,
      borderColor: RPG.goldDim,
      borderRadius: 4,
      marginHorizontal: 8,
      marginVertical: 6,
      overflow: 'hidden',
    },

    Verificar que `overflow: 'hidden'` não quebra nenhum componente filho (DieBubble, SkillRow, NumericStepper são todos retangulares e não transbordam).

    Não alterar: `instanceContent`, `diceCol`, `skillsCol`, `skillsDivider`, `ipRow`, `skillHeader`, `skillHeaderText` — apenas `instanceBlock` muda.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    - styles.instanceBlock em index.tsx contém backgroundColor: RPG.surface, borderWidth: 1, borderColor: RPG.goldDim, borderRadius: 4, marginHorizontal: 8, marginVertical: 6
    - npx tsc --noEmit passa
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>DiceTrack agora exibe badge "d4"/"d6"/"d8"/"d10"/"d12" ao lado do contador de dados de cada atributo. InstanceBlock (Corpo/Mente/Espírito) tem visual de card com fundo surface e borda goldDim.</what-built>
  <how-to-verify>
    1. Abrir Expo Go, aba Ficha
    2. Rolar até a seção Corpo/Mente/Espírito
    3. Verificar:
       a. Cada atributo (Força, Reflexos, Vigor, etc.) que tem dados equipados mostra um badge pequeno dourado (ex: "d8") ao lado do número
       b. Atributos sem dados equipados (count=0) NÃO mostram badge
       c. Os três blocos CORPO/MENTE/ESPÍRITO têm borda dourada tênue e fundo ligeiramente mais claro que o scroll background
       d. Layout geral não quebrou (texto não cortado, inputs funcionam)
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva qualquer problema visual</resume-signal>
</task>

</tasks>

<verification>
1. npx tsc --noEmit — zero erros
2. grep "COUNT_TO_DIE" components/rpg/DiceTrack.tsx — retorna a constante
3. grep "dieBadge" components/rpg/DiceTrack.tsx — retorna style
4. grep "goldDim" app/(tabs)/index.tsx — retorna instanceBlock borderColor
5. Aprovação visual no Expo Go
</verification>

<success_criteria>
- DiceTrack exibe badge de tipo de dado (d4–d12) baseado no count de bubbles preenchidas
- InstanceBlock tem card look MTG: surface bg + goldDim border + borderRadius 4 + margin
- npx tsc --noEmit passa limpo
- Aprovado na verificação visual
</success_criteria>

<output>
Criar `.planning/phases/16-atributos-dados-visuais/16-01-SUMMARY.md` quando concluído.
</output>
