---
phase: 16-atributos-dados-visuais
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/rpg/ProficienciasSection.tsx
  - components/rpg/HabilidadesSection.tsx
autonomous: true
requirements:
  - ATTR-02

must_haves:
  truths:
    - "Badge circular nos slots de ProficienciasSection tem 32x32px, borda gold (filled) ou goldDim (empty), opacidade 1.0 (filled) ou 0.35 (empty)"
    - "Badge circular nos slots de HabilidadesSection tem mesma especificação visual"
    - "Número do slot permanece visível dentro do badge"
    - "npx tsc --noEmit passa sem erros"
  artifacts:
    - path: "components/rpg/ProficienciasSection.tsx"
      provides: "Power Crest badge 32x32 gold circular no slot"
      contains: "powerCrest"
    - path: "components/rpg/HabilidadesSection.tsx"
      provides: "Power Crest badge 32x32 gold circular no slot"
      contains: "powerCrest"
---

<objective>
Atualizar os badges numéricos nos slots de ProficienciasSection e HabilidadesSection para terem visual de Power Crest: círculos maiores (32×32px), borda dourada, opacidade diferenciada por estado ativo/inativo.

Purpose: ATTR-02 — Power Crests circulares como containers visuais para proficiências e habilidades.
Output: Badges atualizados em ambos os componentes. Sem novo arquivo de componente — mudança cirúrgica de estilo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@constants/theme.ts
@components/rpg/ProficienciasSection.tsx
@components/rpg/HabilidadesSection.tsx
</context>

<interfaces>
<!-- Power Crest spec: -->
powerCrest (badge):
  width: 32
  height: 32
  borderRadius: 16     (círculo perfeito)
  borderWidth: 1.5
  alignItems: 'center'
  justifyContent: 'center'
  marginTop: 2
  flexShrink: 0

powerCrestFilled:
  backgroundColor: RPG.goldDim
  borderColor: RPG.gold
  opacity: 1.0        (implícito — não setar, apenas não setar low opacity)

powerCrestEmpty:
  backgroundColor: RPG.border   (fundo muito sutil)
  borderColor: RPG.goldDim
  opacity: 0.35

<!-- Número dentro do crest: -->
  fontSize: 11 (era 10)
  fontWeight: '700'
  (cores mantidas: goldLight filled, textMuted empty)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar badge → Power Crest em ProficienciasSection.tsx</name>
  <files>components/rpg/ProficienciasSection.tsx</files>
  <read_first>
    - components/rpg/ProficienciasSection.tsx (focar nas styles: badge, badgeFilled, badgeEmpty, badgeNum, badgeNumFilled, badgeNumEmpty — confirmar valores atuais antes de editar)
  </read_first>
  <action>
    Substituir os 6 styles do badge no StyleSheet.create de ProficienciasSection.tsx:

    ANTES:
      badge: { width: 20, height: 20, borderRadius: 10, alignItems: 'center', justifyContent: 'center', marginTop: 2, flexShrink: 0 },
      badgeFilled: { backgroundColor: RPG.goldDim },
      badgeEmpty: { backgroundColor: RPG.border },
      badgeNum: { fontSize: 10, fontWeight: '700' },
      badgeNumFilled: { color: RPG.goldLight },
      badgeNumEmpty: { color: RPG.textMuted },

    DEPOIS:
      badge: { width: 32, height: 32, borderRadius: 16, borderWidth: 1.5, alignItems: 'center', justifyContent: 'center', marginTop: 2, flexShrink: 0 },
      badgeFilled: { backgroundColor: RPG.goldDim, borderColor: RPG.gold },
      badgeEmpty: { backgroundColor: RPG.border, borderColor: RPG.goldDim, opacity: 0.35 },
      badgeNum: { fontSize: 11, fontWeight: '700' },
      badgeNumFilled: { color: RPG.goldLight },
      badgeNumEmpty: { color: RPG.textMuted },

    Nenhuma mudança em JSX — apenas os 6 values de style mudam.
    Verificar: o badge View em JSX usa [styles.badge, key ? styles.badgeFilled : styles.badgeEmpty] — esse pattern permanece igual.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    - styles.badge em ProficienciasSection.tsx tem width: 32, height: 32, borderRadius: 16, borderWidth: 1.5
    - styles.badgeEmpty tem opacity: 0.35
    - npx tsc --noEmit passa
  </done>
</task>

<task type="auto">
  <name>Task 2: Atualizar badge → Power Crest em HabilidadesSection.tsx</name>
  <files>components/rpg/HabilidadesSection.tsx</files>
  <read_first>
    - components/rpg/HabilidadesSection.tsx (focar nas styles: badge, badgeFilled, badgeEmpty — confirmar valores exatos antes de editar)
  </read_first>
  <action>
    Aplicar a mesma substituição de 6 styles do badge no StyleSheet.create de HabilidadesSection.tsx.

    O padrão de estilo é idêntico ao de ProficienciasSection — aplicar os mesmos valores DEPOIS:
      badge: { width: 32, height: 32, borderRadius: 16, borderWidth: 1.5, alignItems: 'center', justifyContent: 'center', marginTop: 2, flexShrink: 0 },
      badgeFilled: { backgroundColor: RPG.goldDim, borderColor: RPG.gold },
      badgeEmpty: { backgroundColor: RPG.border, borderColor: RPG.goldDim, opacity: 0.35 },
      badgeNum: { fontSize: 11, fontWeight: '700' },
      badgeNumFilled: { color: RPG.goldLight },
      badgeNumEmpty: { color: RPG.textMuted },

    Nenhuma mudança em JSX.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx expo lint</automated>
  </verify>
  <done>
    - styles.badge em HabilidadesSection.tsx tem width: 32, height: 32, borderRadius: 16
    - npx tsc --noEmit e npx expo lint passam
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Badges de slot nas seções Proficiências e Habilidades agora são Power Crests circulares maiores (32px) com borda dourada. Slots preenchidos: círculo gold sólido. Slots vazios: círculo opaco (35%) com borda goldDim.</what-built>
  <how-to-verify>
    1. Abrir Expo Go, aba Ficha
    2. Rolar até Proficiências e Habilidades
    3. Verificar:
       a. Badges dos slots são círculos visivelmente maiores e mais polidos que antes
       b. Slots preenchidos: círculo dourado com borda ouro visível
       c. Slots vazios: círculo desbotado (semi-transparente, difícil de ler o número)
       d. Toque no slot ainda abre o modal de seleção normalmente
       e. Funcionalidade de adicionar/remover proficiências e habilidades intacta
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva qualquer problema</resume-signal>
</task>

</tasks>

<verification>
1. npx tsc --noEmit — zero erros
2. npx expo lint — zero erros
3. grep "borderRadius: 16" components/rpg/ProficienciasSection.tsx — retorna badge style
4. grep "borderRadius: 16" components/rpg/HabilidadesSection.tsx — retorna badge style
5. grep "opacity: 0.35" components/rpg/ProficienciasSection.tsx — retorna badgeEmpty
</verification>

<success_criteria>
- Power Crest circular (32×32, borderRadius 16) em todos os slots de Proficiências e Habilidades
- Estado ativo: gold border + goldDim background + opacity 1.0
- Estado vazio: goldDim border + opacity 0.35
- npx tsc --noEmit e npx expo lint passam limpos
- Aprovado na verificação visual
</success_criteria>

<output>
Criar `.planning/phases/16-atributos-dados-visuais/16-02-SUMMARY.md` quando concluído.
</output>
