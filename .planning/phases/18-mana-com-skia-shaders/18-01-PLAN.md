---
plan: "18-01"
phase: "18-mana-com-skia-shaders"
wave: 1
status: pending
requirements: [MANA-02]
files_created:
  - components/rpg/shaders/ManaShadersLib.ts
  - components/rpg/ManaRow.tsx
files_modified:
  - app/(tabs)/index.tsx
autonomous: true
depends_on: []

must_haves:
  truths:
    - "Cada uma das 6 cores de mana exibe um efeito visual Skia único na sua linha (44×44 dp)"
    - "Vermelho/Azul/Verde animam (fogo, ondas, partículas) quando total > 0"
    - "Branco/Preto/Incolor exibem efeito estático (sem animação)"
    - "Steppers Base e Total continuam funcionais por linha"
    - "Quando total = 0 a animação pausa (não consome GPU)"
    - "Se Skia não está disponível (Expo Go sem JSI), o app não quebra — fallback ao layout atual"
    - "TypeScript strict permanece limpo (`npx tsc --noEmit` sem erros)"
  artifacts:
    - path: "components/rpg/shaders/ManaShadersLib.ts"
      provides: "4 RuntimeEffect compilados em module scope + estratégia por cor"
      exports: ["fireEffect", "wavesEffect", "particlesEffect", "neutralDistortionEffect", "MANA_VISUAL_STRATEGY"]
    - path: "components/rpg/ManaRow.tsx"
      provides: "Linha única de mana com canvas Skia + dual steppers (memoizada)"
      exports: ["default ManaRow"]
    - path: "app/(tabs)/index.tsx"
      provides: "Substitui o map manaTypes.map(...) por <ManaRow> por cor"
  key_links:
    - from: "components/rpg/ManaRow.tsx"
      to: "components/rpg/shaders/ManaShadersLib.ts"
      via: "import { fireEffect, wavesEffect, ... }"
      pattern: "from '@/components/rpg/shaders/ManaShadersLib'"
    - from: "app/(tabs)/index.tsx"
      to: "components/rpg/ManaRow.tsx"
      via: "import ManaRow + render por cor"
      pattern: "<ManaRow"
    - from: "components/rpg/ManaRow.tsx"
      to: "store/CharacterContext"
      via: "setMana via prop onChange"
      pattern: "onBaseChange|onTotalChange"
---

<objective>
Implementar MANA-02: cada cor de mana exibe um shader Skia visual único (vermelho=fogo Perlin, azul=ondas concêntricas, verde=partículas ascendentes, branco=brilho etéreo, preto=sombra profunda, incolor=distorção neutra). A linha de mana ganha um canvas 44×44 dp à esquerda do losango/label, com os steppers Base/Total continuando à direita.

Purpose: Identidade visual MTG aprofundada — mana deixa de ser um losango plano e ganha "personalidade" por cor.
Output: Biblioteca de shaders + componente ManaRow + integração na Ficha (`index.tsx`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-mana-com-skia-shaders/18-RESEARCH.md
@constants/theme.ts
@types/character.ts

<!-- Interfaces e padrões existentes que o executor precisa conhecer (sem precisar abrir os arquivos) -->

<interfaces>
<!-- Skia.RuntimeEffect.Make assinatura — @shopify/react-native-skia 2.2.12 -->
Skia.RuntimeEffect.Make(sksl: string): SkRuntimeEffect | null

<!-- Componentes Skia usados -->
import { Canvas, Fill, Shader, Skia, useClock } from '@shopify/react-native-skia';
import { useDerivedValue } from 'react-native-reanimated';

<!-- Pattern usado em LegendaryFrame.tsx — replicar EXATAMENTE -->
const skiaAvailable = Skia != null && typeof (Skia as any).Path?.Make === 'function';

<!-- Cores RPG relevantes (constants/theme.ts) -->
RPG.vermelho = '#D3202A'   // RGB normalizado: (0.827, 0.125, 0.165)
RPG.azul     = '#0E68AB'   // RGB normalizado: (0.055, 0.408, 0.671)
RPG.verde    = '#00733E'   // RGB normalizado: (0.000, 0.450, 0.243)
RPG.branco   = '#F8F2E2'
RPG.preto    = '#150B00'
RPG.incolor  = '#A6ADB5'
RPG.surface  = '#181210'

<!-- Tipos do Character (types/character.ts) -->
mana: {
  incolor:  { base: number; total: number };
  branco:   { base: number; total: number };
  verde:    { base: number; total: number };
  vermelho: { base: number; total: number };
  preto:    { base: number; total: number };
  azul:     { base: number; total: number };
}

<!-- Setter no CharacterContext -->
setMana(k: keyof Character['mana'], field: 'base' | 'total', v: number): void

<!-- NumericStepper assinatura (compact é booleano, color é string opcional) -->
interface NumericStepperProps {
  value: number;
  onChange: (v: number) => void;
  min?: number;
  max?: number;
  label?: string;
  color?: string;
  compact?: boolean;
}

<!-- Estilos atuais relevantes em index.tsx (apenas referência — manter) -->
styles.manaTable        // wrap externo
styles.manaHeaderRow    // header Base/Total
styles.manaColorCol     // largura 70, flex-row, gap 6
styles.manaStepperCell  // flex:1, alignItems:center
styles.manaDiamond      // 12×12 rotate 45deg
styles.manaLabel        // fontSize 11
styles.manaTableRow     // flexDirection:'row', alignItems:'center', paddingVertical:5

<!-- manaTypes array atual (index.tsx) -->
const manaTypes = [
  { key: 'incolor', label: 'Inc', color: RPG.incolor, diamondColor: RPG.incolor },
  { key: 'branco', label: 'Bco', color: RPG.branco, diamondColor: RPG.branco },
  { key: 'verde', label: 'Vde', color: RPG.verdeLight, diamondColor: RPG.verde },
  { key: 'vermelho', label: 'Vm', color: RPG.vermelhoLight, diamondColor: RPG.vermelho },
  { key: 'preto', label: 'Pto', color: RPG.pretoLight, diamondColor: RPG.preto },
  { key: 'azul', label: 'Azul', color: RPG.azulLight, diamondColor: RPG.azul },
];
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar ManaShadersLib (RuntimeEffects + estratégia por cor)</name>
  <files>components/rpg/shaders/ManaShadersLib.ts</files>
  <action>
Criar o arquivo `components/rpg/shaders/ManaShadersLib.ts` que compila TODOS os RuntimeEffect SKSL em module scope (uma vez por process — não dentro de componente). Exportar também um mapa `MANA_VISUAL_STRATEGY` indicando, por cor, se usa shader animado, distorção estática (FractalNoise) ou apenas RadialGradient + BlurMask.

Requisitos do arquivo:

1. Imports:
   ```ts
   import { Skia } from '@shopify/react-native-skia';
   ```

2. Guard `skiaAvailable` (mesmo pattern de LegendaryFrame) — declarar como `export` diretamente (uma única declaração):
   ```ts
   export const skiaAvailable = Skia != null && typeof (Skia as any).RuntimeEffect?.Make === 'function';
   function makeEffect(sksl: string) {
     if (!skiaAvailable) return null;
     try { return Skia.RuntimeEffect.Make(sksl) ?? null; } catch { return null; }
   }
   ```

3. Compilar 3 shaders animados (SKSL — código exato no campo `<implementation>` abaixo):
   - `fireEffect` — Vermelho (ruído tipo fogo, ascendente)
   - `wavesEffect` — Azul (ondas concêntricas saindo do centro)
   - `particlesEffect` — Verde (partículas ascendentes)

4. Compilar 1 shader estático:
   - `neutralDistortionEffect` — Incolor (distorção neutra baseada em hash simples)

5. Para branco e preto não há SKSL — a estratégia será RadialGradient + BlurMask aplicada no `ManaRow` via componentes Skia padrão.

6. Exportar `MANA_VISUAL_STRATEGY`:
   ```ts
   export type ManaKey = 'incolor' | 'branco' | 'verde' | 'vermelho' | 'preto' | 'azul';
   export interface ManaVisualConfig {
     kind: 'shader-animated' | 'shader-static' | 'radial-glow';
     glowColor?: string;    // só usado quando kind='radial-glow'
     glowAlpha?: number;    // 0..1, para BlurMask blend
   }
   export const MANA_VISUAL_STRATEGY: Record<ManaKey, ManaVisualConfig> = {
     vermelho: { kind: 'shader-animated' },
     azul:     { kind: 'shader-animated' },
     verde:    { kind: 'shader-animated' },
     incolor:  { kind: 'shader-static' },
     branco:   { kind: 'radial-glow', glowColor: '#F8F2E2', glowAlpha: 0.85 },
     preto:    { kind: 'radial-glow', glowColor: '#150B00', glowAlpha: 0.95 },
   };
   ```

7. SKSL exato a usar (do RESEARCH.md — todos com `uniform float2 iResolution; uniform float iTime;`):

   **fireEffect (Vermelho)**:
   ```glsl
   uniform float2 iResolution;
   uniform float iTime;
   half4 main(float2 pos) {
     float2 uv = pos / iResolution;
     float noise = fract(sin(dot(uv * 8.0 + iTime * 0.5, float2(12.9898, 78.233))) * 43758.5453);
     float fire = smoothstep(0.4, 0.9, noise * (1.0 - uv.y));
     return half4(0.827, 0.125, 0.165, fire * 0.85);
   }
   ```

   **wavesEffect (Azul)**:
   ```glsl
   uniform float2 iResolution;
   uniform float iTime;
   half4 main(float2 pos) {
     float2 uv = pos / iResolution - 0.5;
     float dist = length(uv);
     float wave = sin(dist * 20.0 - iTime * 3.0) * 0.5 + 0.5;
     float alpha = smoothstep(0.5, 0.0, dist) * wave;
     return half4(0.055, 0.408, 0.671, alpha);
   }
   ```

   **particlesEffect (Verde)**:
   ```glsl
   uniform float2 iResolution;
   uniform float iTime;
   float particle(float2 uv, float2 center, float radius) {
     return smoothstep(radius, 0.0, length(uv - center));
   }
   half4 main(float2 pos) {
     float2 uv = pos / iResolution;
     float t = iTime * 0.4;
     float p = clamp(
       particle(uv, float2(fract(0.3 + t * 0.7), 1.0 - fract(t * 1.1)),      0.10) +
       particle(uv, float2(fract(0.7 + t * 0.5), 1.0 - fract(t * 0.9 + 0.3)), 0.08) +
       particle(uv, float2(fract(0.5 + t * 0.6), 1.0 - fract(t * 1.3 + 0.6)), 0.06),
       0.0, 1.0);
     return half4(0.0, 0.45, 0.243, p);
   }
   ```

   **neutralDistortionEffect (Incolor — estático, sem iTime mas mantém uniform iTime=0 por simetria de uniforms)**:
   ```glsl
   uniform float2 iResolution;
   uniform float iTime;
   half4 main(float2 pos) {
     float2 uv = pos / iResolution;
     float n = fract(sin(dot(uv * 12.0, float2(12.9898, 78.233))) * 43758.5453);
     float v = smoothstep(0.35, 0.85, n);
     return half4(0.65, 0.68, 0.71, v * 0.55);
   }
   ```

8. Exportações finais (named exports — NÃO default export). `skiaAvailable` já foi exportado no item 2; não repetir:
   ```ts
   export const fireEffect = makeEffect(/* SKSL Vermelho */);
   export const wavesEffect = makeEffect(/* SKSL Azul */);
   export const particlesEffect = makeEffect(/* SKSL Verde */);
   export const neutralDistortionEffect = makeEffect(/* SKSL Incolor */);
   // skiaAvailable já exportado no item 2 acima
   ```

NÃO importar nada de Reanimated ou React aqui — este arquivo é puro: compila shaders e exporta config. O componente ManaRow é quem orquestra animação (clock + useDerivedValue).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    Sem erros TS. Conferir manualmente que o arquivo exporta `fireEffect`, `wavesEffect`, `particlesEffect`, `neutralDistortionEffect`, `MANA_VISUAL_STRATEGY`, `skiaAvailable`.
  </verify>
  <done>
Arquivo `components/rpg/shaders/ManaShadersLib.ts` existe, exporta os 4 effects (ou null se Skia indisponível), exporta `MANA_VISUAL_STRATEGY` com 6 cores, `tsc` limpo. Nenhum efeito é compilado dentro de função/componente.
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar componente ManaRow com canvas Skia + dual steppers</name>
  <files>components/rpg/ManaRow.tsx</files>
  <action>
Criar `components/rpg/ManaRow.tsx` — componente React memoizado que renderiza UMA linha de mana com:
- canvas Skia 44×44 dp à esquerda (visual da cor)
- losango colorido + label (mantém visual atual do `manaColorCol`)
- dois NumericStepper compact (Base, Total)

Interface obrigatória (props):
```ts
import type { ManaKey } from '@/components/rpg/shaders/ManaShadersLib';
export interface ManaRowProps {
  manaKey: ManaKey;
  label: string;          // 'Inc' | 'Bco' | 'Vde' | 'Vm' | 'Pto' | 'Azul'
  color: string;          // RPG.xLight (texto/stepper) — ex.: RPG.vermelhoLight
  diamondColor: string;   // RPG.x (preenchimento do losango) — ex.: RPG.vermelho
  base: number;
  total: number;
  onBaseChange: (v: number) => void;
  onTotalChange: (v: number) => void;
}
```

Imports obrigatórios:
```ts
import React, { useMemo } from 'react';
import { View, Text, StyleSheet } from 'react-native';
import { Canvas, Fill, Shader, Group, RadialGradient, Circle, vec, BlurMask, Skia } from '@shopify/react-native-skia';
import { useClock, useDerivedValue } from 'react-native-reanimated';
import NumericStepper from '@/components/rpg/NumericStepper';
import { RPG } from '@/constants/theme';
import {
  fireEffect,
  wavesEffect,
  particlesEffect,
  neutralDistortionEffect,
  MANA_VISUAL_STRATEGY,
  skiaAvailable,
} from '@/components/rpg/shaders/ManaShadersLib';
```

Comportamento:

1. **Tamanho do canvas:** fixo 44×44 dp (não usar flex). Constante local `CANVAS_SIZE = 44`.

2. **Active gate (otimização de performance):** `const active = total > 0`. Quando `!active`, o canvas mostra apenas a fill de fundo (`RPG.surface`) sem shader/animation — economiza GPU em cores zeradas. NUNCA condicionar `useClock`/`useDerivedValue` — hooks sempre executam (regra do React).

3. **Animação (useClock + useDerivedValue):**
   ```ts
   const clock = useClock();
   const uniforms = useDerivedValue(() => ({
     iResolution: [CANVAS_SIZE, CANVAS_SIZE],
     iTime: clock.value / 1000,
   }));
   ```

4. **Seleção de efeito por strategy:**
   ```ts
   const strategy = MANA_VISUAL_STRATEGY[manaKey];
   const effect = useMemo(() => {
     switch (manaKey) {
       case 'vermelho': return fireEffect;
       case 'azul':     return wavesEffect;
       case 'verde':    return particlesEffect;
       case 'incolor':  return neutralDistortionEffect;
       default:         return null;
     }
   }, [manaKey]);
   ```

5. **Render do canvas — três caminhos:**

   **(a) Skia indisponível OU !active:** renderizar um `<View>` cinza/surface com tamanho fixo (fallback puro RN). Apenas mostra o losango/label sem canvas. Use:
   ```tsx
   <View style={[styles.canvasFallback, !active && styles.canvasInactive]} />
   ```

   **(b) strategy.kind === 'shader-animated' OU 'shader-static' (vermelho/azul/verde/incolor):**
   ```tsx
   <Canvas style={{ width: CANVAS_SIZE, height: CANVAS_SIZE }}>
     <Fill color={RPG.surface} />
     {effect && (
       <Fill>
         <Shader source={effect} uniforms={uniforms} />
       </Fill>
     )}
   </Canvas>
   ```
   Observação: para incolor a animação roda mas a saída do SKSL é praticamente idêntica frame a frame (não usa iTime). Tudo bem — performance está coberta pelo active gate.

   **(c) strategy.kind === 'radial-glow' (branco/preto):**
   ```tsx
   <Canvas style={{ width: CANVAS_SIZE, height: CANVAS_SIZE }}>
     <Fill color={RPG.surface} />
     <Group>
       <Circle cx={CANVAS_SIZE / 2} cy={CANVAS_SIZE / 2} r={CANVAS_SIZE / 2}>
         <RadialGradient
           c={vec(CANVAS_SIZE / 2, CANVAS_SIZE / 2)}
           r={CANVAS_SIZE / 2}
           colors={[strategy.glowColor!, 'transparent']}
         />
         <BlurMask blur={8} style="solid" />
       </Circle>
     </Group>
   </Canvas>
   ```

6. **Estrutura JSX (linha completa):**
   ```tsx
   <View style={styles.row}>
     <View style={styles.canvasWrap}>{/* canvas/fallback */}</View>
     <View style={styles.colorCol}>
       <View style={[styles.diamond, { backgroundColor: diamondColor, borderColor: color }]} />
       <Text style={[styles.label, { color }]}>{label}</Text>
     </View>
     <View style={styles.stepperCell}>
       <NumericStepper compact value={base} onChange={onBaseChange} color={color} />
     </View>
     <View style={styles.stepperCell}>
       <NumericStepper compact value={total} onChange={onTotalChange} color={color} />
     </View>
   </View>
   ```

7. **Estilos (StyleSheet local — mantém visual da `manaTableRow` original):**
   ```ts
   const styles = StyleSheet.create({
     row: {
       flexDirection: 'row',
       alignItems: 'center',
       paddingVertical: 5,
       borderBottomWidth: 1,
       borderBottomColor: RPG.border,
       gap: 6,
     },
     canvasWrap: {
       width: 44,
       height: 44,
       borderWidth: 1,
       borderColor: RPG.goldDim,
       overflow: 'hidden',
     },
     canvasFallback: {
       flex: 1,
       backgroundColor: RPG.surface,
     },
     canvasInactive: {
       opacity: 0.4,
     },
     colorCol: {
       flexDirection: 'row',
       alignItems: 'center',
       gap: 6,
       width: 60,
     },
     diamond: {
       width: 12,
       height: 12,
       transform: [{ rotate: '45deg' }],
       borderRadius: 1,
       borderWidth: 1.5,
     },
     label: {
       fontSize: 11,
       textTransform: 'uppercase',
       letterSpacing: 0.3,
     },
     stepperCell: {
       flex: 1,
       alignItems: 'center',
     },
   });
   ```

8. **Export:** `export default React.memo(ManaRow);` (memoização obrigatória — evita re-render quando outras cores mudam).

9. **Sem efeitos colaterais:** nada de useEffect, sem fetch, sem AsyncStorage. Componente puro de apresentação.

Atenção: NÃO inline-compilar SKSL aqui. O componente apenas consome os effects exportados por `ManaShadersLib`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    Sem erros TS. `ManaRow` é `React.memo`-wrapped. Hooks (`useClock`, `useDerivedValue`, `useMemo`) sempre chamados (não dentro de condicionais).
  </verify>
  <done>
Arquivo `components/rpg/ManaRow.tsx` existe, componente recebe props tipadas, usa `MANA_VISUAL_STRATEGY` para escolher renderização, fallback gracioso quando `!skiaAvailable` ou `total === 0`, `tsc` limpo.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrar ManaRow em index.tsx (substituir manaTypes.map)</name>
  <files>app/(tabs)/index.tsx</files>
  <action>
Modificar `app/(tabs)/index.tsx` substituindo o bloco `manaTypes.map(...)` (linhas ~87–100) por renderização via `<ManaRow>`. Manter cabeçalho `manaHeaderRow` (Base/Total) e o wrap externo `styles.manaTable`.

Passos:

1. **Adicionar import** no topo do arquivo (próximo aos outros imports `@/components/rpg/*`):
   ```ts
   import ManaRow from '@/components/rpg/ManaRow';
   ```

2. **Substituir o JSX da seção `── MANA ──`** (linhas ~79–101 atualmente). Layout final:
   ```tsx
   {/* ── MANA ── */}
   <SectionHeader title="Mana" />
   <View style={styles.manaTable}>
     <View style={styles.manaHeaderRow}>
       <View style={styles.manaCanvasHeaderCol} />
       <View style={styles.manaColorCol} />
       <Text style={styles.manaHeaderCell}>Base</Text>
       <Text style={styles.manaHeaderCell}>Total</Text>
     </View>
     {manaTypes.map(({ key, label, color, diamondColor }) => (
       <ManaRow
         key={key}
         manaKey={key}
         label={label}
         color={color}
         diamondColor={diamondColor}
         base={c.mana[key].base}
         total={c.mana[key].total}
         onBaseChange={v => setMana(key, 'base', v)}
         onTotalChange={v => setMana(key, 'total', v)}
       />
     ))}
   </View>
   ```

3. **Adicionar UM estilo em StyleSheet.create** (na seção styles) — alinhar header com a coluna do canvas:
   ```ts
   manaCanvasHeaderCol: { width: 44 + 6 }, // largura do canvas + gap da row
   ```
   Mantém `manaColorCol: { ... width: 70 }` e os demais inalterados. Pode ajustar `manaColorCol` width para 60 (mesmo do ManaRow) se for necessário para alinhamento; senão deixar como está.

4. **NÃO REMOVER:**
   - O array `manaTypes` (helper local — continua sendo a fonte de verdade)
   - Os estilos `manaTable`, `manaHeaderRow`, `manaHeaderCell`, `manaColorCol` (header ainda usa)

5. **Os estilos antigos que podem ser removidos** (não mais usados em index.tsx — moveram para ManaRow):
   - `manaTableRow`
   - `manaStepperCell`
   - `manaDiamond`
   - `manaLabel`
   Remover essas 4 entries de `StyleSheet.create` para evitar dead code. Confirmar via `grep` (mental) que nenhum outro lugar do arquivo as referencia antes de remover.

6. **Tipagem de `manaTypes`:** o tipo de `key` no array deve coincidir com `ManaKey` de `ManaShadersLib`. Como ambos derivam das mesmas 6 strings literais, o TS aceita por compatibilidade estrutural. Se houver erro, adicionar import:
   ```ts
   import type { ManaKey } from '@/components/rpg/shaders/ManaShadersLib';
   ```
   E tipar: `const manaTypes: { key: ManaKey; label: string; color: string; diamondColor: string }[] = [ ... ];`
  </action>
  <verify>
    <automated>npx tsc --noEmit ; npx expo lint</automated>
    `tsc` limpo. ESLint sem warnings novos. Conferir que a seção Mana renderiza 6 linhas e que mudar `total` de uma cor não causa re-render visível de outras (memoização).
  </verify>
  <done>
`app/(tabs)/index.tsx` usa `<ManaRow>` para cada cor. Header `Base/Total` alinhado. Estilos órfãos removidos. `tsc` e `expo lint` limpos. Mana visual exibe shaders únicos por cor quando `total > 0` (validação visual manual no app — não automatizável).
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` → 0 erros
2. `npx expo lint` → sem regressões
3. Visual manual no Expo Go / Dev Client:
   - Linha Vermelho com total > 0: deve mostrar ruído tipo fogo subindo
   - Linha Azul com total > 0: ondas concêntricas pulsando
   - Linha Verde com total > 0: 3 partículas pequenas subindo lentamente
   - Linha Branco: brilho radial creme/claro estático
   - Linha Preto: sombra radial escura estática
   - Linha Incolor: distorção/textura neutra estática
   - Linha com total = 0: canvas opaco/cinza (sem animação)
   - Steppers Base/Total continuam funcionais e persistem (AsyncStorage)
4. Em Expo Go sem JSI: app não crasha — fallback puro RN sem canvas
</verification>

<success_criteria>
- [ ] `ManaShadersLib.ts` compila 4 RuntimeEffect em module scope
- [ ] `ManaRow.tsx` é `React.memo` e usa `useClock` + `useDerivedValue`
- [ ] `index.tsx` consome `<ManaRow>` para todas as 6 cores
- [ ] `npx tsc --noEmit` passa
- [ ] `npx expo lint` sem regressões
- [ ] `skiaAvailable` guard presente (fallback gracioso)
- [ ] Animação pausa quando `total === 0`
- [ ] Hooks sempre executados (não condicionais)
- [ ] Estilos órfãos removidos de `index.tsx`
- [ ] Commit: `feat(18-01): ManaRow + ManaShadersLib — Skia shaders por cor de mana (MANA-02)`
</success_criteria>

<output>
Após implementar, criar `.planning/phases/18-mana-com-skia-shaders/18-01-SUMMARY.md` documentando:
- Arquivos criados/modificados
- Snippets SKSL finais (pode ter divergido por tuning visual)
- Notas de performance observadas no device
- Commit hash
</output>
