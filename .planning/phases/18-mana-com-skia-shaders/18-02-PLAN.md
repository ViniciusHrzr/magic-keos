---
plan: "18-02"
phase: "18-mana-com-skia-shaders"
wave: 1
status: completed
requirements: [MANA-03]
files_created:
  - components/rpg/CanalizacaoGrid.tsx
files_modified:
  - app/(tabs)/magia.tsx
autonomous: true
depends_on: []

must_haves:
  truths:
    - "Canalização exibe grid de checkboxes chamfrados estilo MTG (não 22×22 planos)"
    - "Cada checkbox tem cantos chanfrados via Skia.Path (mesmo padrão visual do LegendaryFrame)"
    - "Quando marcado, o checkbox enche com cor mágica (gold/azul) + stroke gold"
    - "Quando desmarcado, fica vazio (RPG.surface) com stroke gold dim"
    - "Toque alterna o estado (TouchableOpacity wrap)"
    - "Velocidade continua usando o CheckboxGrid antigo (inalterado)"
    - "Se Skia não está disponível, fallback gracioso (quadrados retangulares dourados)"
    - "TypeScript strict permanece limpo (`npx tsc --noEmit` sem erros)"
  artifacts:
    - path: "components/rpg/CanalizacaoGrid.tsx"
      provides: "Grid de checkboxes chamfrados MTG (substitui CheckboxGrid em Canalização)"
      exports: ["default CanalizacaoGrid"]
    - path: "app/(tabs)/magia.tsx"
      provides: "Substitui <CheckboxGrid> por <CanalizacaoGrid> apenas no bloco Canalização"
  key_links:
    - from: "components/rpg/CanalizacaoGrid.tsx"
      to: "@shopify/react-native-skia"
      via: "Canvas + Path + Skia.Path.Make()"
      pattern: "import.*Canvas.*Path.*Skia.*from '@shopify/react-native-skia'"
    - from: "app/(tabs)/magia.tsx"
      to: "components/rpg/CanalizacaoGrid.tsx"
      via: "import + render dentro da seção Canalização"
      pattern: "<CanalizacaoGrid"
    - from: "app/(tabs)/magia.tsx"
      to: "components/rpg/CheckboxGrid"
      via: "import mantido APENAS para Velocidade (não remover)"
      pattern: "<CheckboxGrid"
---

<objective>
Implementar MANA-03: substituir o `<CheckboxGrid>` da seção **Canalização** (em `app/(tabs)/magia.tsx`) por um novo `<CanalizacaoGrid>` com visual MTG — checkboxes chanfrados (chamfered corners) desenhados via Skia.Path, mesmo padrão estético do LegendaryFrame.

Purpose: Reforço da identidade visual MTG iniciada na Phase 15 — Canalização ganha "selos" mágicos em vez de quadrados genéricos.
Output: Componente novo `CanalizacaoGrid.tsx` + integração em `magia.tsx` (apenas Canalização — Velocidade fica intacta).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-mana-com-skia-shaders/18-RESEARCH.md
@constants/theme.ts

<interfaces>
<!-- Pattern existente em LegendaryFrame.tsx — replicar EXATAMENTE para chamfer -->
import { Canvas, Path, Skia } from '@shopify/react-native-skia';

const skiaAvailable = Skia != null && typeof (Skia as any).Path?.Make === 'function';

function makeChamferPath(w: number, h: number, c: number) {
  const path = Skia.Path.Make();
  path.moveTo(c, 0);
  path.lineTo(w - c, 0);
  path.lineTo(w, c);
  path.lineTo(w, h - c);
  path.lineTo(w - c, h);
  path.lineTo(c, h);
  path.lineTo(0, h - c);
  path.lineTo(0, c);
  path.close();
  return path;
}

<!-- Render padrão (LegendaryFrame): -->
<Canvas style={...}>
  <Path path={chamferPath} color={fillColor} style="fill" />
  <Path path={chamferPath} color={strokeColor} style="stroke" strokeWidth={1.5} />
</Canvas>

<!-- CheckboxGrid atual — assinatura idêntica que CanalizacaoGrid deve manter (drop-in): -->
interface CheckboxGridProps {
  boxes: boolean[];
  onChange: (boxes: boolean[]) => void;
  cols?: number; // default 5 — manter compatibilidade
}

<!-- Cores RPG usadas no checkbox MTG -->
RPG.surface  = '#181210'  // fundo desmarcado
RPG.gold     = '#c9a84c'  // stroke + fill marcado
RPG.goldLight = '#e8c96a' // stroke marcado (variante)
RPG.goldDim  = '#7a6020'  // stroke desmarcado

<!-- Uso atual em magia.tsx (linhas ~127–132) que será substituído -->
<View style={styles.gridWrap}>
  <CheckboxGrid
    boxes={c.canalizacao.boxes}
    onChange={boxes => setCanalizacao({ boxes })}
  />
</View>

<!-- Uso de CheckboxGrid em Velocidade (linhas ~99–104) que NÃO deve mudar -->
<View style={styles.gridWrap}>
  <CheckboxGrid
    boxes={c.velocidade.boxes}
    onChange={boxes => setVelocidade({ boxes })}
  />
</View>

<!-- Schema do Character (relevante) -->
canalizacao: { base: number; temp: number; boxes: boolean[] }; // boxes.length === 15
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar CanalizacaoGrid (checkboxes chanfrados via Skia)</name>
  <files>components/rpg/CanalizacaoGrid.tsx</files>
  <action>
Criar `components/rpg/CanalizacaoGrid.tsx` — grid de checkboxes chanfrados, drop-in compatível com a interface do `CheckboxGrid` (mesmas props `boxes`, `onChange`, `cols`).

Imports obrigatórios:
```ts
import React, { useMemo } from 'react';
import { View, TouchableOpacity, StyleSheet } from 'react-native';
import { Canvas, Path, Skia } from '@shopify/react-native-skia';
import { RPG } from '@/constants/theme';
```

Interface (idêntica ao CheckboxGrid antigo — drop-in):
```ts
interface Props {
  boxes: boolean[];
  onChange: (boxes: boolean[]) => void;
  cols?: number;          // default 5 (mesmo do CheckboxGrid)
}
```

Constantes locais:
```ts
const CHECKBOX_SIZE = 22;   // mesmo tamanho do CheckboxGrid (consistência)
const CHAMFER = 5;          // chanfro proporcional (~22% do lado)
const STROKE_WIDTH = 1.5;
```

Guard Skia (mesmo pattern do LegendaryFrame — em module scope, NÃO dentro do componente):
```ts
const skiaAvailable = Skia != null && typeof (Skia as any).Path?.Make === 'function';

function makeChamferPath(w: number, h: number, c: number) {
  const path = Skia.Path.Make();
  path.moveTo(c, 0);
  path.lineTo(w - c, 0);
  path.lineTo(w, c);
  path.lineTo(w, h - c);
  path.lineTo(w - c, h);
  path.lineTo(c, h);
  path.lineTo(0, h - c);
  path.lineTo(0, c);
  path.close();
  return path;
}
```

Componente:

```tsx
export default function CanalizacaoGrid({ boxes, onChange, cols = 5 }: Props) {
  // Path compilado UMA vez — todos os checkboxes compartilham
  const chamferPath = useMemo(() => {
    if (!skiaAvailable) return null;
    try {
      return makeChamferPath(CHECKBOX_SIZE, CHECKBOX_SIZE, CHAMFER);
    } catch {
      return null;
    }
  }, []);

  const toggle = (i: number) => {
    const next = [...boxes];
    next[i] = !next[i];
    onChange(next);
  };

  return (
    <View style={styles.grid}>
      {boxes.map((checked, i) => (
        <TouchableOpacity
          key={i}
          onPress={() => toggle(i)}
          activeOpacity={0.7}
          style={styles.cell}
        >
          {skiaAvailable && chamferPath ? (
            <Canvas style={styles.canvas}>
              <Path
                path={chamferPath}
                color={checked ? RPG.gold : RPG.surface}
                style="fill"
              />
              <Path
                path={chamferPath}
                color={checked ? RPG.goldLight : RPG.goldDim}
                style="stroke"
                strokeWidth={STROKE_WIDTH}
              />
            </Canvas>
          ) : (
            // Fallback puro RN: quadrado sem chanfro, mesma cor
            <View
              style={[
                styles.fallbackBox,
                checked && styles.fallbackBoxChecked,
              ]}
            />
          )}
        </TouchableOpacity>
      ))}
    </View>
  );
}
```

Estilos (manter visual equivalente ao CheckboxGrid antigo — flex-wrap row, gap 4, padding 2 por cell):
```ts
const styles = StyleSheet.create({
  grid: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    gap: 4,
  },
  cell: {
    padding: 2,
  },
  canvas: {
    width: CHECKBOX_SIZE,
    height: CHECKBOX_SIZE,
  },
  fallbackBox: {
    width: CHECKBOX_SIZE,
    height: CHECKBOX_SIZE,
    borderWidth: 1.5,
    borderColor: RPG.goldDim,
    backgroundColor: RPG.surface,
  },
  fallbackBoxChecked: {
    backgroundColor: RPG.gold,
    borderColor: RPG.goldLight,
  },
});
```

Observações importantes:

1. **NÃO modificar `components/rpg/CheckboxGrid.tsx`** — ele continua sendo usado em Velocidade (e potencialmente em outros locais futuros).
2. **`cols` prop:** é aceita por compatibilidade de interface, mas o layout real é flex-wrap (mesmo do CheckboxGrid original — `cols` está documentado mas não usado na implementação atual; manter o mesmo comportamento).
3. **Memo path:** `useMemo` retorna o `SkPath` uma vez por mount — todos os 15 checkboxes reutilizam a mesma referência. NÃO compilar dentro do `.map()`.
4. **Sem animação:** este componente é puramente estático (não usa useClock / useDerivedValue). Toggle muda fill via re-render normal do React.
5. **Sem React.memo no wrapper raiz:** boxes mudam em conjunto, memoizar não traz benefício.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    Sem erros TS. `chamferPath` calculado via useMemo (uma vez). Fallback path quando `!skiaAvailable`. Interface `Props` idêntica à do CheckboxGrid (boxes, onChange, cols).
  </verify>
  <done>
Arquivo `components/rpg/CanalizacaoGrid.tsx` existe, default export, drop-in compatível com CheckboxGrid, usa Skia.Path para chanfro, fallback gracioso, `tsc` limpo.
  </done>
</task>

<task type="auto">
  <name>Task 2: Trocar CheckboxGrid por CanalizacaoGrid em magia.tsx (apenas Canalização)</name>
  <files>app/(tabs)/magia.tsx</files>
  <action>
Modificar `app/(tabs)/magia.tsx` substituindo o `<CheckboxGrid>` que está dentro da seção Canalização (linhas ~127–132) por `<CanalizacaoGrid>`. **NÃO mexer** no `<CheckboxGrid>` da seção Velocidade (linhas ~99–104).

Passos:

1. **Adicionar import** abaixo do import existente de `CheckboxGrid` (linha 8):
   ```ts
   import CheckboxGrid from '@/components/rpg/CheckboxGrid';
   import CanalizacaoGrid from '@/components/rpg/CanalizacaoGrid'; // ← NOVO
   ```
   Manter `CheckboxGrid` importado — continua sendo usado em Velocidade.

2. **Localizar o bloco Canalização** (procurar pelo texto `<SectionHeader title="Canalização" />` — está dentro de `<View style={{ flex: 1 }}>` no `twoCol`). O grid alvo está aqui:
   ```tsx
   <View style={styles.gridWrap}>
     <CheckboxGrid
       boxes={c.canalizacao.boxes}
       onChange={boxes => setCanalizacao({ boxes })}
     />
   </View>
   ```

3. **Substituir APENAS este uso** por:
   ```tsx
   <View style={styles.gridWrap}>
     <CanalizacaoGrid
       boxes={c.canalizacao.boxes}
       onChange={boxes => setCanalizacao({ boxes })}
     />
   </View>
   ```

4. **Confirmar (mental check) que o uso em Velocidade ficou intacto:**
   ```tsx
   {/* Velocidade — NÃO MEXER */}
   <View style={styles.gridWrap}>
     <CheckboxGrid
       boxes={c.velocidade.boxes}
       onChange={boxes => setVelocidade({ boxes })}
     />
   </View>
   ```

5. **NÃO remover o import de `CheckboxGrid`** — Velocidade ainda usa.

6. **Não criar/remover estilos.** `styles.gridWrap` (padding: 8, backgroundColor: RPG.surface) já está alinhado com o tamanho do CanalizacaoGrid (22×22 dp por cell). O wrap externo permanece idêntico.
  </action>
  <verify>
    <automated>npx tsc --noEmit ; npx expo lint</automated>
    `tsc` limpo. `expo lint` sem regressões. Conferir manualmente:
    - Canalização renderiza 15 checkboxes chanfrados (não quadrados)
    - Velocidade continua com quadrados planos (CheckboxGrid original)
    - Toque alterna estado normalmente
    - Estado persiste após recarregar app (AsyncStorage)
  </verify>
  <done>
Em `magia.tsx`, Canalização usa `<CanalizacaoGrid>` e Velocidade continua com `<CheckboxGrid>`. `tsc` e `expo lint` limpos. Validação visual manual confirma chanfro nos checkboxes de Canalização.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` → 0 erros
2. `npx expo lint` → sem regressões
3. Validação visual manual:
   - Seção Canalização: 15 checkboxes com cantos chanfrados (octogonais)
   - Desmarcados: fundo escuro (`RPG.surface`), stroke dourado fosco (`RPG.goldDim`)
   - Marcados: fundo gold, stroke gold claro (`RPG.goldLight`)
   - Seção Velocidade: 15 checkboxes quadrados sem chanfro (visual antigo intacto)
   - Toque alterna estado
   - Estado persiste após F5/reabrir app
4. Em Expo Go sem JSI: fallback puro RN — quadrados gold sem chanfro mas funcionais
</verification>

<success_criteria>
- [ ] `CanalizacaoGrid.tsx` existe com chanfro via Skia.Path
- [ ] `skiaAvailable` guard + fallback puro RN presente
- [ ] `chamferPath` calculado via `useMemo` (uma vez por mount)
- [ ] `magia.tsx` usa `<CanalizacaoGrid>` em Canalização
- [ ] `magia.tsx` continua usando `<CheckboxGrid>` em Velocidade
- [ ] `components/rpg/CheckboxGrid.tsx` NÃO foi modificado
- [ ] `npx tsc --noEmit` passa
- [ ] `npx expo lint` sem regressões
- [ ] Interface `Props` de `CanalizacaoGrid` é drop-in compatível (boxes, onChange, cols)
- [ ] Commit: `feat(18-02): CanalizacaoGrid — checkboxes chamfrados MTG (MANA-03)`
</success_criteria>

<output>
Após implementar, criar `.planning/phases/18-mana-com-skia-shaders/18-02-SUMMARY.md` documentando:
- Arquivos criados/modificados
- Constantes finais (CHAMFER, CHECKBOX_SIZE) — caso tenha precisado tunar
- Confirmação de que CheckboxGrid antigo continua funcional em Velocidade
- Commit hash
</output>
